package com.sharedfate.inventory;

/**
 * <b>바탕을 스스로 그려야 하는 칸</b>이라는 표시다.
 *
 * <p>바닐라 칸은 창 배경 그림에 이미 네모가 그려져 있다. 이 모드가 새로 만든 칸은
 * 그림에 없는 자리에 있으므로 화면 쪽에서 네모를 직접 채워 넣어야 한다.
 */
public interface SelfPaintedSlot {
}
