# 변경 내역

## 0.29.2-dev — 2026-09-22

**통신 규약은 29 그대로입니다.** 0.29.0-dev·0.29.1-dev 와 서로 접속됩니다.

### 「교환」 세트의 보호 시간이 4초 → 10초가 됐습니다

| | 전 | 후 |
|---|--:|--:|
| **교환 2** — 신속 · 재생 II · 흡수 II · 저항 II | 4초 | **10초** |
| **교환 3** — 저항 IV(사실상 무적) | 4초 | **10초** |

위치가 바뀌면 낯선 자리에 떨어지는데, 4초는 **주위를 보기도 전에 끝나는** 길이였습니다.
특히 교환 3의 무적은 「떨어진 곳이 용암 위였다」 같은 경우를 구제하려는 것인데 4초로는
빠져나오지 못했습니다.

두 단계를 **같은 길이로** 맞췄습니다. 어긋나 있으면 받는 사람이 언제까지 안전한지 알 수
없습니다. 위치 교환 주기는 가장 짧아도 1분이라 두 창이 겹칠 일은 없습니다.

## 0.29.1-dev — 2026-09-22

**통신 규약은 29 그대로입니다.** 0.29.0-dev 와 서로 접속됩니다.

### 굴림을 최대로 잡은 팀은 「도박」 세트가 통째로 죽어 있었습니다

팀을 만들 때 **다시 뽑기를 15회로 잡으면** 도박 증강을 아무리 모아도 **한 회도 더 받지
못했습니다.** 「도박 2 — 한 판 더」(5회)도, 「도박 3 — 어차피 프리즘」(3회)도 그랬습니다.

세트 몫을 `총 상한 15 − 회차당 허용치` 로 접고 있었기 때문입니다. 허용치가 15면 남는 자리가
0 이라 아무것도 못 얹습니다. 그리고 **그 잘림은 아무 말도 하지 않았습니다** — 채팅에도 로그에도
한 줄이 안 나가서, 받는 쪽에서는 그냥 보상이 없는 것처럼 보였습니다.

**굴림을 좋아해서 최대로 고른 사람이 정확히 굴림을 주는 유형 다섯 장을 죽은 카드로 받는
꼴이었습니다.** 도박 유형이 그만큼 통째로 뜻을 잃었습니다.

**이제 세트 몫은 회차당 허용치 위에 얹힙니다.**

| 회차당 허용치 | 도박 2 | 도박 3 | 한 회차 합계 |
|--:|--:|--:|--:|
| 3 (기본) | +5 | +3 | **11** |
| 15 (최대) | +5 | +3 | **23** |

허용치를 몇으로 잡았든 도박 세트를 온전히 받습니다. 팀을 만들 때 고를 수 있는 범위는
**0~15 그대로**이고, 바뀐 것은 「세트로 얹히는 몫을 거기서 빼지 않는다」 하나입니다.

> **이미 굴림 15로 만들어 둔 팀도 살아납니다.** 서버를 올리고 나서 다음 증강을 고르는 순간
> 밀려 있던 몫이 그 자리에서 들어옵니다. 팀을 다시 만들 필요가 없습니다.

### 팀을 해체하면 월드 스폰으로 돌아갑니다

해체는 **아이템도 경험치도 없애는** 사건인데 사람은 있던 자리에 그대로 남았습니다. 네더나
엔드에 나가 있었다면 **빈손으로 갇힙니다.** 이제 팀원 전원이 월드 스폰으로 돌아갑니다.

- **해체 때 접속하지 않았던 사람은 다음에 들어올 때** 옮겨집니다. 경험치 정리가 이미 그렇게
  동작하고 있어서 같은 장치를 씁니다 — 서버를 껐다 켜도 살아남습니다
- **운영자 초기화(`/shareteam reset`)도 같은 길**을 지납니다
- 해체 확인 창에 「팀원 전원이 월드 스폰으로 돌아갑니다」 한 줄이 늘었습니다. 멀리 나가 있던
  사람에게는 그 자체가 큰 일이라 경고 없이 끌려가면 안 됩니다

> **경험치는 예전부터 0이 되고 있었습니다.** 해체 확인 창에도 그렇게 적혀 있습니다. 이번에
> 바뀐 것은 위치뿐입니다.

### 「환골탈태」가 도박 유형에서 빠졌습니다

가진 증강을 **통째로 갈아엎는** 증강입니다. 그것이 도박 세트를 채우면 **자기가 만든 세트를
자기가 부수는** 모양이 됩니다 — 셋째 장으로 「환골탈태」를 골라 도박 3이 열리는 순간, 그
효과가 도박 증강들을 골드로 바꿔 버려 세트가 바로 풀립니다.

이제 유형이 없습니다. 도박 유형은 5개 → **4개**, 유형 없는 증강은 12개 → **13개**입니다.
**세트 3단계는 그대로 닿습니다**(셋이 필요한데 넷이 남았습니다).

### 서버 설명(MOTD)을 모드가 채웁니다

받아서 자기 서버를 연 사람의 서버 목록에 **「A Minecraft Server」**가 그대로 떴습니다. 예전
`overrideServerMotd` 는 **이미 적어 둔 문구 안의 「증강 N개」 숫자만** 갈아 끼웠기 때문입니다.

**이제 아무도 안 쓴 서버는 채웁니다.**

```
SharedFate · 체력·허기·인벤 공유 · 증강 94개
```

- **사람이 쓴 글은 덮어쓰지 않습니다.** 제 문구가 있으면 예전처럼 「증강 N개」만 맞추고, 그
  모양이 없으면 아무것도 하지 않습니다
- 그래서 **기본값을 켜짐으로** 돌렸습니다. 예전에는 남의 글에만 손대는 기능이라 꺼 두었습니다
- 모드가 채운 문구도 끝이 「증강 N개」라, 다음 판에 증강이 늘면 **스스로 따라갑니다**

## 0.29.0-dev — 2026-09-22

> ### ⚠ 통신 규약이 28 → **29** 로 올랐습니다
>
> **0.28.1-dev 이하 클라이언트는 접속이 막힙니다.** 서버와 플레이어 전원이 함께 올려야 합니다.
> 이번에는 실제로 **패킷에 칸이 하나 늘었습니다**(아래 두 번째 항목).

### 26.3 이 피격 쿨타임 칸을 옮긴 것을 따라가지 못하고 있었습니다

마인크래프트가 26.3 에서 「맞은 직후 잠깐 무적」을 세던 칸을 **둘로 쪼갰습니다.**

| | 26.2 | 26.3 |
|---|---|---|
| `Entity.invulnerableTime` | 피해 판정이 이 칸을 봄 | **피해와 무관해짐.** 다른 용도로 남음 |
| `LivingEntity.damageCooldownTime` | 없음 | **신설.** 피해 판정이 보는 것은 이쪽 |

0.27.0-dev 로 26.3 에 올릴 때 옛 칸이 `private` 이 되어 컴파일이 깨졌고, 접근자로 갈아
끼웠습니다. 그런데 **바닐라가 그 값의 쓰임 자체를 옮긴 것**은 보지 못했습니다. 컴파일도
시험도 통과하는 종류라 조용했습니다. 두 군데가 영향을 받았습니다.

- **「호위」가 헛되이 소모됐습니다.** 좀비 셋에게 동시에 맞으면 실제로 아픈 것은 첫 대뿐인데,
  「어차피 버려질 대인가」를 가리는 판정이 죽어 있어서 **0 이 될 두 번째 대에 쿨타임이
  타 버렸습니다.** 이제 그런 대에는 쓰지 않고 아껴 둡니다
- **「완충」이 분산 중에 몹 피해를 부당하게 막았습니다.** 미뤄 둔 몫을 넣을 때 쿨타임을
  풀었다가 되돌리는 코드가 **둘 다 아무 일도 하지 않고 있었고**, 그 바람에 몫을 넣을 때마다
  쿨타임이 새로 찼습니다. 이제 의도대로 몫만 들어갑니다

앞으로 같은 일이 생기면 시험이 먼저 잡습니다. 바닐라 클래스에 그 칸이 있는지, 우리 코드가
옛 칸을 다시 묻지 않는지를 **클래스 파일을 뒤져** 확인합니다.

### 서버를 초기화하면 「게임 오버」가 뜨던 것

운영자가 `/shareteam reset` 을 확정하면 5초 카운트다운이 도는데, 그동안 **살아 있는 팀원
전원의 화면 한가운데에 「게임 오버」가 크게 떴습니다.** 아무도 죽지 않았는데 전멸한 줄
압니다.

초기화가 전멸 연출을 그대로 빌려 쓰는데 **클라이언트에는 두 경로를 가를 근거가 없었습니다** —
회차도 남은 시간도 양쪽이 똑같이 채웁니다. 그래서 서버가 「왜 내려가는가」를 함께 보내게
했습니다. **칸이 하나 늘어 통신 규약이 29 로 올랐습니다.**

| 자리 | 전멸 | 초기화 |
|---|---|---|
| 화면 큰 글씨 | 게임 오버 | **서버 초기화** |
| 사망 화면 제목 | 게임 오버 · N회차 | **서버 초기화** |
| 아래 작은 글씨 | 5초 후 서버가 종료됩니다 | **5초 후 서버가 종료되고 새 월드로 다시 열립니다** |

## 0.28.1-dev — 2026-09-21

> **통신 규약은 28 그대로입니다.** 0.28.0-dev 와 서로 접속됩니다. 다만 아래 첫 항목이
> **서버가 안 열리는 상태를 만들 수 있는 결함**이라, 0.28.0-dev 를 쓰고 계시면 올리십시오.

### 서버 초기화가 반쪽만 실행되던 것을 막았습니다

`/shareteam reset` 은 팀을 해체하고 기록을 비운 **5초 뒤에** 월드 표식을 썼습니다. 그 사이
표식 쓰기가 실패하면 **서버가 내려가지 않는데 팀과 기록은 이미 사라진 뒤**였습니다. 월드는
그대로인데 팀만 없는, 이 명령이 막으려던 바로 그 상태입니다.

실패하는 경우가 셋 있었습니다.

| 언제 | 왜 |
|---|---|
| **싱글플레이·LAN** | 월드가 `saves/<월드>` 라 「서버 루트 바로 아래」 조건에 걸립니다 |
| 지난 초기화의 표식이 남아 있을 때 | 표식은 덮어쓰지 않고 만들기만 하므로 실패합니다 |
| 루프 스크립트가 월드 삭제에 실패한 뒤 | 위와 같은 상태가 됩니다 |

이제 **아무것도 지우기 전에 먼저 검사합니다.** 지울 수 없는 상태면 팀도 기록도 건드리지 않고
거절하며, 무엇이 문제인지와 어떻게 해야 하는지를 알려 줍니다. 그래도 표식 쓰기가 실패하면
**회차 표식을 되돌려** 다음 기동에 회차가 엉뚱하게 1로 눌리지 않게 합니다.

> **싱글플레이·LAN 에서는 이제 `/shareteam reset` 이 아예 거절됩니다.** 전용 서버 전용
> 명령입니다.

### 지난 초기화의 표식이 남으면 알려 줍니다

`.sharedfate-world-reset.pending` 이 남아 있으면 **재시작 루프 스크립트가 서버를 아예 켜지
않습니다.** 그런데 모드는 이 파일을 쓰기만 하고 읽지 않아, 운영자는 서버가 왜 안 뜨는지 알
길이 없었습니다.

이제 서버가 뜰 때 **지워야 할 파일의 경로를 로그에 남깁니다.** 지우지는 않습니다 — 모드가
멋대로 없애면 전멸하고도 월드가 안 지워집니다.

### 그 밖

- **서버 켜는 파일이 첫 줄에 오류를 뱉던 것** — `start-sharedfate-server.bat` 의 줄바꿈이
  배포본에서 LF 로 나가 한국어 Windows 에서 `'주석'은(는) 내부 또는 외부 명령...` 이 떴습니다.
  실행은 이어졌지만 처음 켜는 사람은 설치가 틀린 줄 압니다
- **`/shareteam help` 에 `reset` 과 `version` 이 빠져 있던 것**
- 팀이 여럿일 때 한 팀 해체가 실패해도 **나머지 팀은 계속 해체**합니다
- 회차 표식을 임시 파일 + 원자적 이동으로 씁니다. 갑작스러운 종료에 반쯤 쓰인 표식이 남지
  않습니다
- 26.2 에서 올라오는 분을 위한 **Fabric API 제거 단계**를 README 와 릴리스 노트에 넣었습니다.
  파일 이름이 달라 덮어써지지 않고, 둘이 함께 남으면 게임이 아예 켜지지 않습니다

## 0.28.0-dev — 2026-09-21

> ### ⚠ 통신 규약이 27 → **28** 로 올랐습니다
>
> **0.27.1-dev 이하 클라이언트는 접속이 막힙니다.** 서버와 플레이어 전원이 함께 올려야 합니다.
>
> 이번 판에 고치고 넣은 것은 **둘 다 서버 안쪽**이라 패킷 형식은 하나도 바뀌지 않았습니다.
> 그런데도 번호를 올린 것은 **판이 섞이는 것을 막기 위해서**입니다. 규약을 그대로 두면 서버만
> 올린 채 옛 클라이언트가 조용히 섞여 들어오고, 누가 어떤 판인지는 `/shareteam version all`
> 을 쳐 봐야만 알 수 있습니다.
>
> 마인크래프트 26.3 전용인 것도 그대로입니다.

### 침대로 드래곤을 잡으면 승리 기록이 통째로 사라지던 것을 고쳤습니다

엔드에서 **침대를 터뜨려** 드래곤을 잡으면 승리는 선언되는데 **승리 책이 나오지 않고 그 회차가
기록에도 남지 않았습니다.** 채팅에는 팀 이름 대신 「'모험가' 팀이 승리했습니다」가 떴습니다.

원인은 바닐라가 처치자를 기억하는 조건에 있었습니다. 마인크래프트는 피해를 일으킨 주체가
**사람일 때만** 처치자를 적어 둡니다. 그런데 침대 폭발은 터뜨린 사람을 싣지 않습니다.

```
BedBlock:  level.explode(null, badRespawnPointExplosion(위치), null, …)
                         ────                                 ────
                      일으킨 주체 없음                    직접 원인도 없음
```

그러면서도 침대 피해는 「폭발」로 분류되어 **드래곤에게는 그대로 들어갑니다.** 잡히기는 잡히는데
잡은 사람은 아무도 기억되지 않는, 정확히 어긋난 구조였습니다. 같은 일이 **명령으로 소환한
TNT**, **연쇄 점화로 소유자를 잃은 TNT**, 그리고 이 모드의 **「폭발 교환」**에서도 일어납니다.

이제 처치자를 네 단계로 찾습니다.

| | 찾는 것 |
|---|---|
| 1 | 피해를 일으킨 주체가 사람인가 |
| 2 | 직접 원인(TNT·화살)의 **소유자**가 사람인가 |
| 3 | 드래곤이 기억하는 마지막 가해자 |
| 4 | **회차를 시작한 팀이 하나뿐이면 그 팀** |

침대는 1·2·3이 모두 비므로 **4단계가 받습니다.** 팀이 둘 이상이면 아무 팀도 찍지 않고 예전처럼
「모험가」로 남습니다 — 한 대도 때리지 않은 팀에게 승리 책을 쥐여 주는 것보다 낫습니다.
「아직 게임을 시작하지 않은 팀이 잡으면 승리로 세지 않는다」는 규칙은 그대로입니다.

### 서버를 처음 상태로 되돌리는 명령이 생겼습니다 — `/shareteam reset`

그전까지 서버를 초기화하려면 **서버를 내리고 파일 넷을 손으로 지워야** 했습니다. 그중
`sharedfate-team-roster.json` 을 빠뜨리면 **새 월드인데 팀이 살아 있는** 이상한 상태가 됐고,
오류도 나지 않아 한참 뒤에야 알아차렸습니다.

```
/shareteam reset     무엇이 지워지는지 먼저 보여 주고 기다립니다
/yes   또는  /수락    실제로 지웁니다 (30초 안에, 친 사람만)
```

- **운영자(권한 2 이상)만** 쓸 수 있고, `/shareteam yes` · `/st 수락` 으로도 확정됩니다
- 확정하면 **회차·팀·보유 증강·피해 기록·월드**가 전부 사라지고 **1회차**로 돌아갑니다
- 끊기는 방식은 전멸 때와 같습니다 — 5초 카운트다운 뒤 서버가 종료되고, **재시작 루프
  스크립트**가 월드를 지우고 다시 엽니다

> ⚠ **재시작 루프 스크립트로 켠 서버에서만 쓰십시오.** `java -jar` 로 직접 띄운 서버는
> 카운트다운이 끝나면 **꺼진 채로 돌아오지 않습니다.** 확인 문구에도 같은 경고가 나옵니다.

명령은 파일을 직접 지우지 않습니다. 서버는 종료할 때 팀 명단을 다시 쓰기 때문에, 지워 봐야
나가는 길에 되살아납니다. 대신 **메모리를 비워 종료 저장이 빈 상태를 적게** 합니다. 팀을 해체할
때는 게임 안의 「팀 해체」와 같은 길을 지나므로, 증강이 몸에 걸어 둔 능력치와 상태이상도 함께
걷힙니다.

### 통신 규약을 28로 올렸습니다

위 둘은 **패킷 형식을 하나도 바꾸지 않았습니다.** 그런데도 번호를 올린 이유는 **판을 강제로
맞추기 위해서**입니다. 서버만 올리고 클라이언트를 그대로 두면 아무 오류 없이 접속되어, 누가
어떤 판을 쓰는지 알기 어려워집니다.

**0.27.1-dev 이하 클라이언트는 이제 접속 단계에서 막힙니다.** 서버와 플레이어 전원을 같은
날 함께 올리십시오.

## 0.27.1-dev — 2026-09-20

> ### ⚠ 0.27.0-dev 를 쓰고 계시면 반드시 올리십시오
>
> **0.27.0-dev 에서는 증강을 고를 수 없습니다.** 선택 화면은 멀쩡히 뜨는데 카드가 눌리지
> 않습니다. 회차마다 여덟 번 지나는 화면이라 그 판으로는 게임이 굴러가지 않습니다.
> 0.27.1-dev 는 그 하나만 고친 판입니다.

### 증강 카드가 눌리지 않던 것을 고쳤습니다

26.3 이 입력 처리를 GLFW 에서 SDL 로 갈아 끼우면서 **마우스 버튼 번호가 통째로 바뀌었습니다.**

```
          26.2   26.3
왼쪽        0  →   1
가운데      2  →   2
오른쪽      1  →   3
```

증강 선택 화면이 「왼쪽 버튼」을 숫자 `0` 으로 적어 두고 있었습니다. 26.3 에서 `0` 은
**아무 버튼도 아닙니다.** 그래서 카드를 눌러도 아무 일이 일어나지 않았습니다.

**빌드도 시험도 통과하고 로그도 조용한 종류입니다.** 화면은 정상으로 뜨고 「다시 뽑기」처럼
바닐라 위젯을 쓰는 단추는 멀쩡히 눌리기 때문에, 실제로 카드를 눌러 봐야만 알 수 있었습니다.

같은 판에서 키 코드도 바뀌었지만(ESC 가 256 → 41) 그쪽은 처음부터 상수를 썼기 때문에 다시
컴파일하는 것만으로 따라갔습니다. **차이는 상수를 썼느냐 숫자를 박았느냐 하나뿐**이었습니다.
코드 전체를 훑어 숫자를 박아 둔 곳은 그 한 줄뿐이었고, 판정을 순수 함수로 떼어 시험으로
못박았습니다.

**그 밖에 달라진 것은 없습니다.** 증강 94개, 세트 13유형 27단계, 통신 규약 27 그대로입니다.

## 0.27.0-dev — 2026-09-20

> ⚠ **마인크래프트 26.3 전용입니다. 26.2 에서는 아예 켜지지 않습니다.**
> 서버와 모든 클라이언트가 **마인크래프트 자체를 26.3 으로** 올려야 합니다.
> 26.2 에 머무를 분은 0.26.7-dev 를 그대로 쓰십시오.

> **통신 규약은 27 그대로입니다.** 다만 **그것과 무관하게** 26.2 클라이언트와 26.3 서버는
> 애초에 같은 게임이 아니라 함께 쓸 수 없습니다. 규약 숫자를 보고 「올려도 되겠다」고
> 판단하지 마십시오.

### 마인크래프트 26.3 으로 올렸습니다

| | 전 | 후 |
|---|---|---|
| Minecraft | 26.2 | **26.3** |
| Fabric Loader | 0.19.3 | **0.19.5** |
| Fabric API | 0.156.0+26.2 | **0.161.0+26.3** |
| Java | 25 | 25 |

**이번 판은 26.3 대응이 전부입니다.** 새 기능도 밸런스 변경도 없습니다. 증강은 94개 그대로
(실버 34 · 골드 35 · 프리즘 25)이고, 세트도 13유형 27단계 그대로입니다. 설정 파일도
명령어도 달라진 것이 없습니다 — **JAR 만 바꾸고 마인크래프트를 26.3 으로 올리면 됩니다.**

### 26.3 에 맞춰 고친 것

바깥에서 보이는 차이는 없고, 26.3 에서 이름이나 모양이 바뀐 자리를 따라간 것입니다.

- `Entity.invulnerableTime` 이 private 이 되어 접근자를 거쳐 읽습니다
- `Player.hurtMarked` 가 `Entity.syncVelocity` 로 이름이 바뀌었습니다
- 아이템을 떨구는 API 의 셋째 인자가 `boolean` 에서 `Prediction` 열거형이 되었습니다
- 월드 시각을 읽는 자리가 `getTotalTicks(clock)` 에서
  `getInstance(clock).totalTicks()` 로 바뀌었습니다
- 믹스인 두 곳(인벤토리 비우기 · 블록 파괴 경험치)의 대상 시그니처가 바뀌어 맞췄습니다

### 좌표 표시에 「얼룩빛 숲」이 나옵니다

26.3 에서 새로 들어온 바이옴 `dappled_forest` 를 화면 왼쪽 위 좌표 표시의 한글 이름 표에
넣었습니다. 66 → **67개**입니다. 표에 없는 바이옴은 영문 id 로 뜨던 자리입니다.

## 0.26.7-dev — 2026-09-20

> **통신 규약은 27 그대로입니다.**

> ✅ **이번 판부터 `config/` 의 정의 파일을 지울 필요가 없습니다.** 아래를 보십시오.

### 판을 올릴 때 정의 파일을 지우는 일이 없어졌습니다

지금까지 판을 올릴 때마다 서버의 `config/sharedfate-perks.json` 과
`config/sharedfate-sets.json` 을 **손으로 둘 다 지워야** 했습니다. 두 파일이 모드 안의
정의보다 우선하는데 판이 올라가도 덮어써지지 않았기 때문입니다.

**하나만 지우면 더 나빴습니다** — 반쪽이 옛 정의로 읽히는데 오류가 나지 않아, 증강 카드에
세트 줄이 안 뜨는 것 같은 엉뚱한 증상으로만 드러났습니다.

**이제 증강·세트 정의는 모드 안에만 있습니다.** 설정 폴더를 보지 않으므로 그 사고가
일어날 수 없습니다. **JAR 만 바꾸면 끝입니다.**

- 이전 판에서 올라와 두 파일이 남아 있어도 **읽지 않습니다.** 그대로 두어도 해가 없고,
  지우셔도 됩니다. 서버가 뜰 때 한 번 알려 줍니다
- 개수를 견줘 「낡았을 수 있습니다」라고 경고하던 장치는 **없앴습니다.** 값만 바뀌고 개수는
  그대로인 판(바로 지난 0.26.6-dev 이 그랬습니다)은 애초에 못 잡았고, 이제 잡을 대상 자체가
  없습니다

> ⚠ **`config/` 의 정의 파일을 직접 고쳐 쓰고 계셨다면 그 내용은 이제 반영되지 않습니다.**
> 값을 바꾸시려면 저장소의 `sharedfate-perks-default.json`·`sharedfate-sets-default.json` 을
> 고쳐 다시 빌드하십시오. `config/sharedfate.json`(모드 설정)은 예전 그대로입니다.

## 0.26.6-dev — 2026-09-20

> **통신 규약은 27 그대로입니다.** 0.26.5-dev 와 같은 판끼리는 그대로 접속됩니다.

> ⚠ **증강 정의가 바뀌었습니다**(「비술 공방」·「흡혈귀」). 서버를 올릴 때
> `config/sharedfate-perks.json` 과 `config/sharedfate-sets.json` 을 **둘 다 지우십시오.**

### 인챈트 값이 다이아몬드 10개가 되었습니다

5개는 너무 쌌습니다. **기본값이 10개**가 되고, 「비술 공방」을 가진 팀만 **2개**입니다
(전에는 1개였습니다). 비술 공방의 할인 폭이 5배가 되어 **등급도 골드에서 프리즘으로**
올랐습니다. 증강 개수는 94개 그대로이고 분포가 실버 34 · 골드 35 · 프리즘 25 가 됩니다.

### 모루 값이 「다이아몬드 10개」로 보입니다

0.26.5-dev 부터 모루는 다이아몬드 10개를 걷는데, 화면에는 바닐라 그대로
**「마법부여 비용: 10」**이라는 초록 글씨가 떴습니다. 경험치를 안 쓰는데 경험치처럼
보였습니다. 이제 **「다이아몬드 10개」**로 뜹니다.

걷는 값은 달라지지 않았습니다. 「비용이 너무 비쌉니다」를 없앤 장치도 그대로입니다.

### 승리 연출이 느긋해졌습니다

정산이 너무 빨리 지나갔습니다.

- 드래곤을 잡고 **첫 장까지 5초 → 10초**
- 한 장에서 **제목이 먼저 뜨고 3.5초 뒤에 숫자가 따라 붙습니다.** 「고른 증강」이 뜨고
  잠깐 있다가 「합계 17개」가 붙는 식입니다. 한 장이 5초에서 8.5초가 됩니다
- 폭죽도 길어진 연출 내내 계속 오릅니다

세 값 모두 `config/sharedfate.json` 에서 조절할 수 있습니다 — `victoryTitleDelayTicks`(200) ·
`victorySubtitleDelayTicks`(70) · `victoryFireworkDelayTicks`(100, 장 사이 간격).

### 승리 폭죽 소리가 들립니다

발사음이 안 난다는 이야기가 있었는데, 실제로는 바닐라가 내고 있었습니다. 다만 범주가
**`AMBIENT`** 라 동굴 소리 때문에 「주변 환경」 슬라이더를 내려 둔 사람에게는 들리지
않았습니다. **`PLAYERS`** 범주로 한 번 더 냅니다 — 연출의 다른 소리와 같은 슬라이더입니다.

### 「흡혈귀」의 흡수가 충전되는 방패가 되었습니다

원래 의도는 **「깎이면 다시 차는 방패」**였는데, 0.26.5-dev 까지는 「2분마다 5초 동안만
흡수 II」였습니다. 5초가 지나면 방패가 통째로 사라지니 원하던 것과 달랐습니다.

이제 이렇습니다.

- **흡수 방패 4하트**를 두르고, **5분마다 가득 찹니다**
- 안 닳았으면 **계속 달고 있습니다.** 절반쯤 닳아 있어도 주기가 오면 가득 찹니다
- **피해로 다 닳으면 그 자리에서 다시 생기지 않습니다.** 다음 5분까지 없는 채로 버팁니다

`periodic` 으로는 만들 수 없었습니다 — 구간이 끝나는 순간 흡수가 0으로 떨어지기 때문에,
구간을 주기 전체로 잡으면 상시 흡수가 되고 짧게 잡으면 그 몇 초만 방패가 있습니다.
새 효과 타입 **`absorption_recharge`** 를 만들었습니다(효과 타입 88 → 89종).

체력이 팀 공유이므로 **흡수도 팀 하나가 나눠 쓰는 값**입니다. 4인 팀이라고 4배가 되지
않습니다 — 채우는 자리가 팀원 순회 바깥에 한 번뿐이고, 더하는 것이 아니라 최대치와
견주는 방식이라 실수로 여러 번 불러도 답이 같습니다.

## 0.26.5-dev — 2026-09-19

> **통신 규약이 26 → 27 로 올랐습니다.** 모루에 다이아몬드 칸이 하나 늘었습니다.
> **0.26.4-dev 이하 클라이언트는 접속이 막힙니다.** 서버와 모든 클라이언트를 함께 갱신하십시오.

> ⚠ **세트·증강 정의가 바뀌었습니다**(「흡혈귀」). 서버를 올릴 때
> `config/sharedfate-perks.json` 과 `config/sharedfate-sets.json` 을 **둘 다 지우십시오.**

### 조합법 책을 없앴습니다

제작대나 인벤토리에서 조합법 책을 펴면 **칸과 아이템이 통째로 사라지던** 문제가 있었습니다.
26.2 바이트코드를 보면 원인이 분명합니다.

```
책이 펴져 있고 && 화면 폭 < 379px  →  배경과 인형만 그린다
그 밖                              →  칸·아이템·이 모드의 추가 27칸 판
```

바닐라 자체의 갈림길이라 모드가 비집고 들어갈 자리가 좁고, 추가 칸 판도 그 안에서만 그려집니다.
그래서 **책을 아예 열 수 없게** 했습니다 — 단추를 만들지 않고, 예전에 펴 둔 채 저장한 사람은
화면을 열 때 닫습니다(그러지 않으면 바닐라가 매 틱 저장값으로 되돌려 곧바로 다시 펴집니다).

**조합 자체는 그대로입니다.** 사라지는 것은 레시피 검색과 자동 배치뿐입니다.

### 발전과제로 경험치가 들어오던 것을 막았습니다

이 모드는 경험치를 팀이 나눠 쓰고 그 레벨에서 증강을 고릅니다. 발전과제 보상 경험치는
**증강을 공짜로 얻는 길**이었습니다. 알림은 예전부터 꺼져 있었지만 달성과 보상은 그대로
돌고 있었습니다.

**달성 자체는 막지 않고 경험치만 0으로 만듭니다.** 26.2 는 조합법 해금조차 숨은 발전과제로
처리하므로(`rewards.recipes`), 달성을 막으면 레시피가 영영 안 풀립니다. 전리품·함수 보상도
건드리지 않습니다.

### 모루 값이 다이아몬드 10개가 되었습니다

인챈트 탁자와 같은 방식입니다. **경험치를 쓰지 않습니다.**

- 값은 작업 내용과 무관하게 **언제나 다이아몬드 10개**입니다
- **「비용이 너무 비쌉니다」가 더 이상 뜨지 않습니다.** 경험치를 안 쓰는데 경험치 상한으로
  막을 이유가 없습니다. 여러 번 고친 장비도 계속 쓸 수 있습니다
- 화면에 보이는 값도 10으로 바뀝니다

칸이 하나 늘어 **통신 규약이 올랐습니다.**

### 팀을 만들 때 설정에 설명이 뜹니다

일곱 가지 설정에 마우스를 올리면 **무엇이 달라지는지** 뜹니다. 문구는 그 설정을 실제로
소비하는 코드를 읽고 적은 것입니다.

**사망 알림**에는 대가까지 적었습니다 — 끄면 죽은 기록이 채팅에도 서버 로그에도 남지 않아
나중에 전멸 원인을 알 수 없습니다. 다른 여섯과 달리 이 설정에만 있는 대가이고 어디에도
적혀 있지 않았습니다.

### 고친 버그

- **추가 칸에 있는 화살로 활이 당겨지지 않았습니다.** 화살을 찾는 코드를 넓힌 자리가 있었지만
  **서버에서만** 돌았습니다. 활을 당기는 판정은 클라이언트에서도 먼저 도는데, 로컬이 화살을
  못 찾으면 **화면에 아무 반응이 없습니다.** 석궁도 같은 자리를 지나 함께 고쳐집니다.
  그 김에 **잠긴 칸의 화살까지 쓰이던 것**도 막았습니다 — 넣는 길은 막아 두고 읽는 길이
  뚫려 있었습니다
- **「흡혈귀」의 흡수가 사실상 상시였습니다.** 2분 주기 중 **2399틱** 동안 흡수 III 라
  흡수 체력이 계속 채워졌고, 「자연 재생 없음」이라는 대가가 뜻을 잃었습니다.
  **2분마다 5초 흡수 II** 로 되돌렸습니다

## 0.26.4-dev — 2026-09-18

> **통신 규약은 26 그대로입니다.** 고친 곳이 전부 서버 안쪽이라 서버 jar 만 갈면 됩니다.

> ⚠ **0.26.3-dev 에는 회차 시작 전에 죽을 수 있는 구멍이 있습니다.** 그 판을 쓰고 있다면
> 올리는 편이 좋습니다.

### 회차 시작 전에 팀 없이 죽던 것을 고쳤습니다

시작 전에는 피해를 통째로 버려 아무도 죽지 않아야 하는데, **팀을 만들기 전에는 그 보호가
걸리지 않았습니다.** 스폰 반경 50칸에 갇혀 블록도 못 부수는 사람이 **떨어져 죽었습니다.**

원인은 판정이 둘로 갈려 있던 것입니다.

| | 전 | 후 |
|---|---|---|
| 막는 것 (보더·파괴·시각·허기) | 팀이 없어도 걸림 | 그대로 |
| **지켜 주는 것 (무적)** | **팀이 없으면 안 걸림** | **팀이 없어도 걸림** |

이제 둘 다 `GameStartManager.preStart` **하나만** 봅니다 — 「회차가 시작됐는가」입니다.
팀을 아직 만들지 않은 사람도 회차를 시작하지 않은 것이므로 똑같이 지켜 줍니다.

> **막는 규칙과 지켜 주는 규칙이 같은 물음을 보지 않으면 반드시 이런 구멍이 생깁니다.**
> 시험 `막는_규칙과_지켜_주는_규칙이_같은_판정을_본다` 가 세 경우(팀 없음·대기·시작) 모두에서
> 두 판정이 같은 답을 내는지 확인합니다. 한쪽만 고치면 빌드가 먼저 깨집니다.

### 회차 시작 전에는 적대 몹이 생기지 않습니다

시작 전에는 아무도 죽지 않고 스폰 50칸을 벗어날 수도 없어, 그 안에 몹이 쌓이기만 했습니다.
「게임 시작」을 누르는 순간 준비도 안 된 팀이 그동안 모인 몹에 둘러싸였습니다.

**막는 것은 적대 몹뿐입니다.** 소·양·주민과 물속 몹은 그대로 생깁니다 — 시작 전에 주변을
둘러보고 자리를 고르는 것이 이 시간에 할 일이고, 동물이 없으면 그 판단을 할 수 없습니다.

**이미 생겨 있는 몹은 지우지 않습니다.** 월드가 만들어질 때 생긴 것들은 남아 있고, 플레이어
에게서 멀어지면 바닐라 규칙대로 사라집니다. 막는 것은 「새로 생기는 것」뿐입니다.

자연 스폰을 가로채는 자리가 이미 있어 **새 Mixin 은 늘지 않았습니다**
(`NaturalSpawnerRateMixin`). 증강의 스폰율 배율보다 **먼저** 묻습니다 — 배율을 아무리 올려
두었어도 시작 전이라면 답은 「생기지 않는다」 하나입니다.

## 0.26.3-dev — 2026-09-18

> **통신 규약은 26 그대로입니다.** 패킷 형식이 바뀌지 않아 0.26.2-dev 클라이언트로도 접속은
> 됩니다. 다만 아래 화면 두 가지가 보이지 않으므로 함께 갱신하는 편이 낫습니다.

> ⚠ **세트 보상 값이 바뀌었습니다.** 서버를 올릴 때 `config/sharedfate-perks.json` 과
> `config/sharedfate-sets.json` 을 **둘 다 지우십시오.** 하나만 지우면 반쪽이 옛 정의로
> 읽히는데 오류가 나지 않습니다.

### 회차를 시작하기 전에는 스폰 근처에 머뭅니다

「게임 시작」을 누르기 전까지 넷이 걸립니다.

| 무엇 | |
|---|---|
| **스폰 반경 50칸** | 오버월드 월드보더가 스폰을 중심으로 좁혀집니다. 빨간 벽이 보이고 실제로 넘어갈 수 없습니다 |
| **블록 파괴 금지** | 부술 수 없고, 왜 막혔는지 액션바에 뜹니다(1초에 한 번) |
| **시각 고정** | 아침에 붙들려 있습니다 |
| **허기 고정** | 배가 고파지지 않습니다 |

**팀을 아직 만들지 않았어도 걸립니다.** 기준은 「대기 중인 팀인가」가 아니라 **「회차가
시작됐는가」** 하나입니다 — 팀을 만들지 않은 사람도 회차를 시작하지 않은 것은 마찬가지이고,
그렇게 하지 않으면 팀을 만들기 전까지 스폰 밖으로 나가 땅을 파도 아무도 막지 않습니다.

「게임 시작」을 누르면 넷 다 풀리고 월드보더도 원래 크기로 돌아갑니다.

> 월드보더와 시계는 **서버에 하나뿐**입니다. 팀을 여럿 만들 수 있게 해 둔 서버에서는 한 팀이
> 시작하는 순간 서버 전체의 보더와 시계가 풀리고, 아직 시작하지 않은 팀은 블록 파괴와 허기만
> 묶인 채로 남습니다.

### 팀을 해체하면 물건이 바닥에 떨어지지 않고 사라집니다

예전에는 해체한 사람 발밑에 공유 아이템이 전부 쏟아졌고, 공유 경험치도 그 사람에게
몰렸습니다. 팀을 없애는 것이 곧 **한 사람이 전부 챙기는** 길이었습니다.

이제 아이템도 경험치도 그대로 사라집니다. **바닥에 이미 떨어져 있던 아이템과 경험치 오브도
함께 치웁니다** — 인벤토리만 비우면 「전부 지웠다」고 안내해 놓고 주우러 다니면 되살아납니다.
같은 청소가 **회차를 시작할 때도** 돕니다.

- 지우는 것은 떨어진 아이템과 경험치 오브뿐입니다. **아이템 액자·방어구 거치대·떨어지는
  모래는 다른 엔티티라 건드리지 않습니다.**
- 경험치를 공유하지 않는 서버(`shareExperience=false`)에서는 개인 경험치를 건드리지
  않습니다. 팀과 무관한 것을 해체 한 번으로 빼앗지 않기 위해서입니다.

### 「팀 해체」에 확인 창이 생겼습니다

무엇이 사라지는지 — 아이템·증강·회차 진행 상황 — 를 줄로 나열하고, 되돌릴 수 없다고
알립니다. 실수로 지나치지 않도록 셋을 겹쳐 뒀습니다.

- **취소가 기본 선택**입니다(Tab 이 먼저 닿는 곳)
- 창이 뜬 뒤 **0.5초 동안은 확인 단추가 눌리지 않습니다**
- **ESC 는 취소**이고, 팀 화면으로 돌아옵니다

확인 단추는 화면 가운데에 있어 **해체를 부른 단추와 겹치지 않습니다.** 같은 자리에 두면
두 번 눌린 딸깍이 그대로 통과합니다.

### 화력 세트가 바뀌었습니다

| 단계 | 전 | 후 |
|---|---|---|
| **화력 2** | 공격력 +1 | **공격력 +2** |
| **화력 3** | 준 피해의 **5%** 회복 | 준 피해의 **15%** 회복 |
| **화력 4** | 흡혈 +10% · 공격력 +0.5 · **몹 스폰 ×1.35** | **근접 사거리 +50%** |

**화력 4의 대가가 없어졌습니다.** 단계는 누적이므로 넷을 모으면 공격력 +2 · 흡혈 15% ·
사거리 +50% 가 함께 걸립니다.

사거리는 바닐라 속성 `minecraft:entity_interaction_range` 로, 기본 3.0 이 **4.5** 가 됩니다.
클라이언트까지 자동으로 내려가는 값이라 새 패킷이 필요 없습니다 — 공격이 닿는지는 클라이언트가
먼저 재므로, 내려가지 않는 값으로 만들었으면 서버만 아는 숫자가 되어 아무 일도 일어나지
않았을 것입니다.

### 팀이 없을 때 인벤토리 단추가 「팀 생성」 하나가 됩니다

팀이 없으면 인벤토리(E) 왼쪽에 **「팀 생성」 하나만** 뜨고, 누르면 팀 화면의 **「팀」 탭**이
펴진 채로 열립니다. 팀이 생기면 예전처럼 「SharedFate」와 「현재 증강」 둘로 돌아갑니다.

팀이 생기거나 사라지는 순간은 **인벤토리를 열어 둔 채로도** 오므로 매 프레임 다시 고릅니다.
단추 객체는 화면을 만들 때 셋 다 만들어 두고 보이는 것만 바꿉니다.

「팀 생성」은 자리가 모자라도 **감춰지지 않습니다.** 맨 앞 단추가 사라지면 팀이 없는 사람이
팀을 만들러 갈 길이 끊깁니다.

### 내 판을 게임 안에서 확인할 수 있습니다

```
/shareteam version        내 클라이언트와 서버가 같은 판인지
/shareteam version all    접속 중인 전원 (운영자 전용)
```

```
SharedFate — 통신 규약 26
· 서버: 0.26.3-dev
· 내 클라이언트: 0.26.3-dev
· 같은 판입니다.
```

접속은 되는데 화면 한쪽이 안 뜨거나 값이 이상할 때, 그것이 **낡은 클라이언트 때문인지 진짜
문제인지** 가르는 것이 언제나 첫 단계였습니다. 그 답이 여태 서버 로그에만 있어서 서버를 켤 수
있는 사람이 아니면 자기 판조차 확인할 수 없었습니다.

- **판을 모를 때 「같다」고 답하지 않습니다.** 자기 판을 알리는 패킷은 0.25.4-dev 에서
  생겼으므로, 조용한 클라이언트는 그보다 낡다는 뜻입니다. 그대로 알려 줍니다
- `version all` 은 **판이 어긋난 사람을 먼저** 적습니다. 목록이 길어도 찾을 것이 맨 위에
  있습니다
- 이 값으로는 **아무 판단도 하지 않습니다.** 접속을 막는 일은 규약 번호가 합니다 — 밖에서 온
  문자열을 믿고 무언가를 허용하면 그 자리가 곧 구멍이 됩니다

### 그 밖

- 팀 해체 안내 문구 세 곳이 「아이템을 드랍했습니다」였습니다. 실제 동작과 어긋나 고쳤습니다.
  팀에 **가입**할 때 개인 아이템이 드랍되는 것은 그대로입니다
- 「몹 스폰율」 효과 타입(`mob_spawn_rate`)은 기본 정의 어디에서도 쓰이지 않게 됐습니다.
  타입 자체는 그대로 있어 증강·세트 정의에서 계속 쓸 수 있습니다

## 0.26.2-dev — 2026-09-17

> **통신 규약이 25 → 26 으로 올랐습니다.** 보유 증강 묶음에 세트 유형 칸이 하나 늘었습니다.
> **0.26.1-dev 이하 클라이언트는 접속이 막힙니다.** 서버와 모든 클라이언트를 함께 갱신하십시오.

### 증강 목록에 세트 유형이 함께 보입니다

보유 증강 목록에서 이름 뒤에 그 증강이 속한 유형이 흐린 글씨로 붙습니다.

```
· 짐꾼      가호
· 살기      사냥
· 요행      도박
· 비상식량            ← 유형이 없는 증강 열둘에는 안 붙습니다
```

**팀 화면의 「증강」 탭과 선택 화면의 「현재 증강」 모달 둘 다**입니다. 무엇을 모으는 중인지
세트 판과 견주지 않고도 한눈에 보입니다.

클라이언트는 증강 풀을 읽지 않아 유형을 스스로 셀 수 없으므로 서버가 함께 보냅니다 —
그래서 규약이 올랐습니다.

### 인벤토리에 「현재 증강」 단추가 생겼습니다

인벤토리(E) 왼쪽 「SharedFate」 단추 **옆**입니다. 누르면 증강 탭이 펴진 채로 열립니다.
예전에는 「SharedFate → 증강」 두 번을 눌러야 했습니다.

글자가 「증강」이 아니라 「현재 증강」인 것은, 「증강」만 적으면 증강을 **고르는** 창처럼
읽히기 때문입니다. 선택 화면의 같은 단추와도 글자를 맞췄습니다.

자리가 모자란 화면에서는 이 단추를 **만들지 않습니다.** 억지로 끼우면 둘 다 글자가 잘려
무엇을 누르는지 알 수 없습니다.

### 「해시계」가 「엑스레이」가 되고, 10초 동안 켜져 있습니다

이름을 바꾸고 발동 방식을 **비행 부적과 같은 지속형**으로 바꿨습니다.

| | 전 | 후 |
|---|---|---|
| 우클릭했을 때 | **한 번 반짝이고 끝** | **10초 동안** 광석 표시가 유지됨(1초마다 갱신) |
| 쿨타임 30초 | **누르는 순간** 시작 | **10초가 끝난 뒤** 시작 |

액션바에 남은 시간이 함께 뜹니다 — 「가장 가까운 다이아몬드: X 128 Y 12 Z −302 (18칸) · 7초」.

**id 는 `sharedfate:diamond_sundial` 그대로입니다.** 아이템에 박힌 표식과 쿨타임 묶음 이름도
그대로라, 이미 나가 있는 시계가 계속 작동합니다. 이름만 바뀝니다.

> 갱신 주기를 1초로 잡은 것은 26.2 파티클 수명(최소 60틱)을 확인하고 정한 값입니다. 그보다
> 길면 표시가 깜빡깜빡 끊깁니다.

### 「종결곡」의 값과 대가를 바꿨습니다

| | 전 | 후 |
|---|---|---|
| 곡괭이 | 효율 **VI** · 행운 **III** | 효율 **V** · 행운 **I** (내구성 X 그대로) |
| 대가 | 몹 **이동 속도** +15% | 몹이 하는 **모든 행동** +20% |

「모든 행동」은 말 그대로 전부입니다 — 이동·공격 간격·**크리퍼가 부풀어 터지는 시간**·
스켈레톤 활 간격·불에 타는 속도까지.

프리즘 「성역」이 몹의 모든 행동을 −40% 로 늦추는데, 그 거울입니다. 성역이 `Mob.tick()` 을
버리고 **서버가 엔티티에게 틱을 주는 입구**를 고른 근거가 이쪽에도 그대로 섭니다 —
크리퍼는 부풀기를 `super.tick()` 보다 먼저 끝내므로, 그 입구가 아니면 크리퍼만 조용히 빠집니다.

**「다섯 틱에 한 번 더 준다」를 확률이 아니라 누적으로 셉니다.** 매 틱 난수를 뽑으면 평균만
20%라, 운 나쁜 크리퍼가 연달아 걸려 잠깐 두 배로 빨라지는 구간이 생깁니다. 터지는 시각을
눈으로 재는 몹에게 그것은 그대로 억울함입니다. 몹마다 위상을 어긋뜨려 무리가 같은 틱에
한꺼번에 두 번 도는 일도 막습니다.

> **`mob_speed` 를 함께 두지 않았습니다.** 틱을 20% 더 주면 이동은 이미 정확히 20% 빨라집니다
> (걸음 폭은 그대로, 걸음 수만 늘어남). 옛 효과를 남기면 걸음 폭과 걸음 수에 각각 곱해져
> **이동만 ×1.44** 가 되어 설명과 어긋납니다.

낙하는 두 배로 먹지 않습니다 — 틱 하나에 더해지는 가속도는 그대로고 틱 수만 늘어서, 몹이
그리는 포물선은 세계 좌표에서 완전히 같고 그 위를 20% 빨리 지날 뿐입니다. 낙하 피해는 틱이
아니라 거리로 매깁니다.

### 회차가 시작된 뒤에는 사람을 부를 수 없습니다

초대는 **게임을 시작하기 전에만** 됩니다. 시작한 뒤에 `invite` 를 치면 막히고, 팀 화면의
초대 단추도 사라집니다.

도중에 들어오면 **같은 회차를 서로 다른 조건으로 겪게 되기** 때문입니다.

- 증강은 팀 레벨 구간마다 고르는데 늦게 들어온 사람은 그 구간들을 지나지 않았습니다
- 난이도 상승은 팀원이 접속해 있던 시간만 세므로, 남이 쌓아 둔 몫을 그대로 물려받습니다
- 피해·사망 기록은 회차 단위라 중간 합류자의 몫만 비어 있습니다
- 무엇보다 **공유 인벤토리가 이미 채워져 있습니다.** 들어오는 사람의 개인 아이템은 드랍되고
  남이 모은 것을 그대로 쓰게 되는데, 그것은 「함께 시작했다」와 다른 게임입니다

사람을 바꾸려면 팀을 해체하고 다시 만듭니다.

### 팀을 해체해도 증강이 그대로 남던 것을 고쳤습니다

해체하고 새 팀을 만들면 **옛 증강이 그대로 따라왔습니다.** 두 겹이었습니다.

- **플레이어에게 붙여 둔 것을 아무도 걷어내지 않았습니다.** 속성 수정자와 상태이상은 팀이
  아니라 <b>플레이어</b>에게 적혀 있어, 팀 상태가 통째로 사라져도 그대로 남습니다. 새 팀의
  보유 목록은 비어 있는데 몸에는 옛 효과가 걸려 있는 상태였습니다.
- **화면의 보유 목록도 안 비워졌습니다.** 목록은 <b>팀원에게만</b> 보내는데 팀이 사라진
  순간 받을 사람이 없어져서, 옛 목록이 화면에 영영 남았습니다.

이제 해체·탈퇴하는 자리에서 **증강 효과와 세트 효과를 걷어내고 빈 목록을 보냅니다.** 팀
상태를 버리기 <b>전에</b> 합니다 — 버린 뒤에는 무엇이 붙어 있었는지 알 방법이 없습니다.

### 초대받아 들어온 사람에게 증강이 안 걸리던 것을 고쳤습니다

위와 정확히 뒤집힌 구멍입니다. 증강은 팀이 고르는 순간 <b>그때 접속해 있던 팀원</b>에게
붙습니다. 나중에 초대받아 들어온 사람은 그 자리를 지나지 않아 <b>목록에는 증강이 보이는데
몸에는 아무 효과도 없는</b> 상태가 됐고, 다시 접속할 때까지 그대로였습니다.

이제 팀에 들어가는 자리에서 그 팀의 증강을 함께 걸어 줍니다. 떠날 때 걷어내는 것과 짝을
맞춰 한 곳에서 합니다.

### 「현재 증강」 단추가 조용히 안 먹던 것을 고쳤습니다

열 자리가 없으면 **누른 뒤에** 그것을 알아채고 도로 닫았습니다. 화면에서는 단추가 아예
안 눌리는 것처럼 보이고, 까닭도 안 알려 주니 무엇이 잘못됐는지 알 길이 없었습니다.

이제 **미리 물어보고 못 열면 단추를 꺼 둡니다.** 회색 단추는 「지금은 안 된다」를 스스로
말합니다.

### 증강을 고르는 동안 지금 가진 증강을 볼 수 있습니다

선택 화면 왼쪽, 세트 판 아래에 **「현재 증강」** 단추가 생겼습니다. 누르면 화면 가운데에
보유 증강 목록이 덮이고, 한 번 더 누르거나 ESC 를 치면 접힙니다.

**화면을 갈아타지 않고 같은 화면 위에 덮습니다.** 선택 화면은 강제 선택 세션이 띄운
것이라, 다른 화면으로 갔다가 돌아오는 길이 하나라도 어긋나면 **카드를 영영 못 고릅니다.**
그래서 새 화면을 열지 않고 겹판으로 만들었습니다.

- **겹판 위의 클릭은 뒤로 새지 않습니다.** 목록을 보려고 누른 것이 카드 선택이 되면
  되돌릴 방법이 없습니다.
- **ESC 는 겹판만 접습니다.** 창까지 함께 닫히지 않습니다.
- 세트 판과 겹치지 않게 **단추 자리를 먼저 떼어** 세트 판이 그만큼 물러납니다.
- 화면이 좁아 왼쪽 열이 없어지면 **단추를 아예 만들지 않습니다.** 카드 위에 걸치느니
  없는 편이 낫습니다.

### 비행 부적의 쿨타임이 「비행이 끝난 뒤」부터 돕니다

예전에는 우클릭하는 순간 걸었습니다. 그러면 비행 시간이 쿨타임 **안에서** 흘러, 10초 날고
쿨 60초인 정의가 실제로는 **「50초만 기다리면 다시 난다」**가 됐습니다. 정의에 적은 숫자와
사람이 겪는 간격이 어긋난 셈입니다.

이제 한 판의 주기가 **비행 + 쿨타임**입니다. 거는 자리는 비행이 끝나는 셋 — **시간 만료 ·
죽음 · 증강 상실** — 이고 전부 겁니다. 죽은 경우를 빼 주면 「죽으면 쿨타임이 없다」가 되어
죽는 것이 이득이 됩니다.

> **접속을 끊으면 쿨타임이 풀립니다.** 바닐라의 쿨타임은 플레이어 저장 자료에 적히지 않아
> 다시 접속하면 비어 있습니다. 예전 방식에서도 마찬가지였으므로 이 판에서 생긴 것은
> 아닙니다.

### 창고 알림이 액션바에도 뜹니다

채팅은 전투 중에 위로 밀려 못 보고 지나치기 쉽습니다. 같은 일을 화면 가운데 아래에도
한 줄 띄웁니다.

```
채팅    [창고] 인벤토리가 꽉 차 다이아몬드 5개 외 2묶음을(를) 창고에 넣었습니다.
        창고에 7묶음 — /창고 로 꺼내세요.
액션바  [창고] 7묶음 — /창고
```

## 0.26.1-dev — 2026-09-15

> **통신 규약은 25 그대로입니다.** 서버 jar 만 갈면 됩니다.

### 창고에 물건이 들어가면 팀 전원에게 알립니다

```
[창고] 인벤토리가 꽉 차 다이아몬드 5개 외 2묶음을(를) 창고에 넣었습니다.
       창고에 7묶음 — /창고 로 꺼내세요.
```

**부르는 곳마다 알림을 달지 않았습니다.** 물건이 창고로 가는 길이 열 곳이 넘는데
(즉시 지급·보급·채굴 보너스·광물 교환·해시계·소집의 조각·비행 부적이 밀어낸 것·왼손
고정·커서·「유산」·잠긴 칸 비우기) **모두 같은 모양**입니다 — 대기열에 넣고 되돌려 보고,
안 들어간 것이 남습니다.

그래서 **대기열이 늘어난 것을 한 곳에서 봅니다.** 한 틱 안에 넣었다 되돌린 몫은 서로
지워지므로, 남는 증가분이 정확히 「자리가 없어 창고로 간 것」입니다. **새 경로가 생겨도
여기는 안 고쳐도 됩니다.**

창고를 열고 닫을 때의 오르내림은 기준을 다시 잡아 알림이 뜨지 않습니다.

## 0.26.0-dev — 2026-09-15

> **통신 규약은 25 그대로입니다.** 창고는 바닐라 상자 화면을 그대로 쓰므로 새 패킷이
> 없습니다. 서버 jar 만 갈면 됩니다.

### 창고가 생겼습니다 — `/창고` · `/shareteam storage`

인벤토리가 꽉 차 못 받은 물건이 **창고에 쌓이고, 사람이 열어서 꺼내 갑니다.**

예전에는 보이지 않는 대기열에 들어가 **매 틱 자동으로** 인벤토리로 돌아갔습니다. 아이템을
잃지는 않았지만 **언제 돌아오는지 아무도 몰랐습니다.** 「보급을 받았다는 채팅은 떴는데
인벤토리 어디에도 없다」가 그것이었고, 대기열에 있는 건지 사라진 건지 **구분할 수단조차
없었습니다.**

**넣을 수는 없습니다.** 마우스로도 쉬프트 클릭으로도 막힙니다. 창고는 받는 곳이지 보관하는
곳이 아닙니다 — 넣게 두면 「인벤토리가 두 개」가 되어 공유 인벤토리의 뜻이 흐려집니다.

**꺼낼 자리가 없으면 그냥 안 꺼내집니다**(바닐라 상자와 같습니다). 다만 커서에 든 채 창을
닫으면 갈 곳이 없으므로 **창고로 되돌립니다.** 바닥에 버리면 5분 뒤 사라지기 때문입니다.

화면은 여섯 줄(54칸)이고 **저장에는 상한이 없습니다.** 더 있으면 창 제목에 「뒤에 N개 더」가
뜨고, 꺼낸 만큼 다음에 열 때 올라옵니다.

> **자동 복귀를 껐습니다.** 매 틱 되돌리면 창고를 열기도 전에 물건이 빠져나가 늘 비어
> 보입니다. 「지금 바로 넣어 보고 안 되면 창고행」 하는 자리(커서 아이템·「유산」 승계)는
> 그대로라, 자리가 있는데도 굳이 창고로 가는 일은 없습니다.

### 죽은 코드를 걷어냈습니다

`SlotExpandedLockMixin` 을 지웠습니다. `Slot.mayPlace` 에 주입했는데
`ExpandedInventorySlot` 이 그 메서드를 **재정의**해서 <b>한 번도 실행된 적이 없었습니다.</b>
막고 있는 줄 알았던 자리가 비어 있던 셈이고, 실제 판정은 0.25.3-dev 부터 슬롯 자신이
들고 있습니다.

창고 쪽은 바닐라 `ChestMenu` 가 만드는 **평범한 `Slot`** 을 쓰므로 같은 방식이 제대로
듣습니다. 그 둘이 실제로 붙는지는 **클래스 파일을 뒤지는 시험**으로 못박았습니다.

## 0.25.6-dev — 2026-09-15

> **통신 규약은 25 그대로입니다.** 고친 곳이 서버 안쪽이라 서버 jar 를 갈아야 합니다.

### 짐꾼을 잃으면 그 줄을 바로 닫습니다

0.25.5-dev 에서 「빼면 칸이 닫힌다」를 고쳤는데, **그 줄에 물건이 남아 있으면 여전히 안
닫혔습니다.** 사람이 그 칸을 손으로 다 비워야 그제야 줄어들었습니다.

원인은 버그가 아니라 <b>규칙</b>이었습니다 — `PerkInventorySlots` 의 「칸은 절대 줄어들지
않는다」입니다. 줄어든 자리에 물건이 있으면 갈 곳을 잃으므로 열어 두는 것이 맞다고 보고
만든 규칙인데, 실제로 해 보니 **「증강을 잃었는데 칸은 그대로」가 더 이상하게 보였습니다.**

이제 증강이 바뀌는 자리에서 **그 칸의 물건을 넘침 대기열로 빼내고 바로 닫습니다.**

- **물건은 잃지 않습니다.** 대기열은 칸이 비는 대로 매 틱 다시 밀어 넣고, 빼낸 즉시
  한 번 되돌리므로 위에 자리가 있으면 그 자리에서 올라옵니다.
- **증강을 끈 팀은 건드리지 않습니다.** 효과를 다시 맞출 때 「껐다 켜는」 방식을 쓰는데, 그
  잠깐 꺼진 순간에 빼내면 짐꾼을 그대로 가진 팀의 물건이 이유 없이 위로 튀어 오릅니다.
- 「물건이 든 칸은 반드시 연다」 규칙 자체는 **남겨 뒀습니다.** 옛 저장처럼 이 자리를 지나지
  않고 아래 칸에 물건이 남은 경우의 마지막 그물입니다.

## 0.25.5-dev — 2026-09-15

> **통신 규약은 25 그대로입니다.** 0.25.4-dev 클라이언트로도 접속은 되지만, 고친 곳이 모두
> 서버 안쪽이라 **서버 jar 를 갈아야** 효과가 있습니다.

### 짐꾼을 빼도 셋째 줄이 안 닫히던 것을 고쳤습니다

증상이 이상했습니다 — **셋째 줄은 계속 보이는데 그 줄에는 물건이 안 들어갔습니다.** 나갔다
들어와도 그대로였습니다.

서버는 18칸으로 잠갔는데 **클라이언트 화면만 27칸으로 남은 불일치**였습니다. 클라이언트는
열린 칸 수를 스스로 셀 수 없어 서버가 밀어 주는 값 하나만 믿는데, 그 값이 어긋났을 때
되돌릴 길이 주기 재동기화 하나뿐입니다. 그런데 그 **중복 제거 키에 칸 수가 빠져 있었습니다.**

```java
// 전 — 레벨 둘만 비교한다. 증강을 빼도 레벨은 그대로다
long levels = packLevels(state.xpLevel, nextPerkLevel(state));
```

증강을 빼도 키가 안 바뀌니 **유일한 복구 경로가 영원히 안 돌았습니다.** 거기에 클라이언트는
접속을 끊어도 이 값을 버리지 않아서, 한 번 27로 굳으면 **재접속으로도 안 풀렸습니다.**

셋을 고쳤습니다.

- 비교 키에 **열린 칸 수**를 넣었습니다. 값이 달라지는 모든 길(증강 제거·환골탈태·가호 단계
  변동·물건이 든 마지막 칸 변동)이 이 한 곳을 지나므로 반 초 안에 스스로 맞습니다.
- 접속을 끊을 때 클라이언트가 칸 수를 **기본 18로 되돌립니다.** 틀리더라도 안 보이는 칸을
  그리는 쪽이 아니라 **덜 그리는 쪽**으로 틀립니다.
- `updateMenuLayout` 의 **「해금 수를 안 주면 27로 본다」 오버로드를 지웠습니다.** 상자·화로를
  새로 열 때마다 일단 세 줄로 깔고 화면이 고쳐 주기를 기대하고 있었습니다. **기본값이
  최대치인 함수는 닫는 방향에서 반드시 한 번 더 물립니다.**

### 보급·즉시지급이 안 보이는 칸으로 들어가던 것을 고쳤습니다

「보급 받았습니다」 채팅은 뜨는데 **인벤토리 어디에도 없고 재접속해야 나타나는** 일이
있었습니다.

추가 칸에 물건을 넣는 길이 넷인데 **한도가 서로 달랐습니다.**

| 길 | 한도 |
|---|---|
| 바닥에서 줍기 | 열린 칸까지 (0.25.1-dev) |
| 창에서 쉬프트 클릭 | 열린 칸까지 (0.25.3-dev) |
| **넘침 대기열 복원** | **27칸 전체** ← 고침 |
| **밀어낸 장비 보관** | **27칸 전체** ← 고침 |

보급·즉시지급·「유산」 승계가 전부 이 길로 들어옵니다. 이제 넷 다 같은 한도를 쓰고, 넣을
곳이 없으면 **대기열에 남아** 칸이 비는 대로 돌아옵니다.

덤으로 그 칸 때문에 **짐꾼도 없는 팀에 27칸이 공짜로 열리던 것**도 함께 사라집니다.

### 서버 설명의 증강 개수가 저절로 맞습니다

`server.properties` 의 MOTD 에 적힌 「증강 82개」가 실제(94개)와 달랐고, 판을 올릴 때마다
사람이 고쳐야 해서 매번 낡았습니다.

이제 서버가 뜰 때 **그 숫자만** 실제 값으로 갈아 끼웁니다. 문구·색·「세트 효과」는 한 글자도
건드리지 않고, **「증강 N개」라는 모양이 없으면 아무것도 하지 않습니다** — 남이 쓴 MOTD 를
지어내지 않기 위해서입니다.

**기본값은 꺼짐입니다**(`overrideServerMotd`). 켜면 `server.properties` 가 덮어써집니다.

### 「요새 탐지기」가 나침반을 함께 줍니다

예전 설명은 「네더에서 **나침반이** 요새를 가리킵니다」였는데, 정작 나침반을 주지 않아
직접 만들어야 했습니다. 「길잡이 나침반」과 같은 형식으로 맞췄습니다.

### `/shareteam perktest owner` 를 넣었습니다

「고른 사람만」 걸리는 증강의 **주인이 지금 누구인지** 보여 줍니다.

```
주인이 정해지는 증강
· 비행 부적 (sharedfate:flight_charm) — 플레이어1
· 열외 (sharedfate:swap_exempt) — 아직 주인 없음
```

지금까지는 주인이 **액션바 한 번으로만** 알려지고 채팅에도 로그에도 안 남아서, 놓치면 다시
볼 방법이 없었습니다. 읽기만 하므로 회차 오염 표식을 붙이지 않습니다.

## 0.25.4-dev — 2026-09-15

> **통신 규약이 24 → 25 로 올랐습니다.** 0.25.3-dev 이하 클라이언트는 접속이 막힙니다.
> 서버와 모든 클라이언트를 함께 갱신하십시오.

### 판이 다른 클라이언트가 그대로 접속되던 것을 막았습니다

`0.25.0` ~ `0.25.3` 이 **전부 규약 24** 였습니다. 그래서 **0.25.2 가 고친 클라이언트 버그를
그대로 가진 판으로도 접속할 수 있었습니다.** 낡은 클라이언트를 쓰는 사람은 화면 왼쪽 위가
안 보이는 채로 계속 플레이하게 되는데, 본인도 서버 운영자도 알아챌 방법이 없었습니다.

이 코드베이스는 원래 **「클라이언트가 조용히 덜 동작하는 판」은 패킷 형식이 안 바뀌어도 규약을
올린다**는 규칙을 써 왔습니다(규약 10·17·24가 그 경우입니다).

```
17: 「값이 안 보인다」는 「모드가 안 맞는다」보다 알아채기 어려우므로
    악수 단계에서 걸러지게 한다.
```

0.25.2 가 정확히 그 경우였는데 규약을 안 올렸습니다. **뒤늦게 여기서 올립니다.**

### 접속한 사람의 클라이언트 판이 서버 로그에 남습니다

```
[CLIENT] 플레이어1 — sharedfate 0.25.4-dev
```

지금까지는 **서버 로그만 봐서는 누가 어떤 클라이언트를 쓰는지 알 방법이 없었습니다.** 한
사람만 화면이 안 보이는 원인을 좁히려면 **그 사람의 로그 파일을 직접 받아야** 했습니다.
이제 접속 한 줄로 끝납니다.

**이 값으로는 아무 판단도 하지 않습니다.** 막는 일은 규약 번호가 합니다. 클라이언트가 보내는
문자열이라 믿을 수 없는 값이고, 판단에 쓰기 시작하면 그 순간 거짓으로 분기하게 됩니다.
로그에 심는 줄바꿈·제어문자는 걸러 냅니다.

## 0.25.3-dev — 2026-09-15

> **통신 규약은 24 그대로입니다.** 두 곳 다 서버 안쪽이라 서버 jar 를 갈아야 효과가 있습니다.

### 무작위로 받은 「고른 사람만」 증강이 아무 일도 안 하던 것을 고쳤습니다

「비행 부적」·「열외」·「호위」처럼 <b>고른 사람 한 명에게만</b> 걸리는 증강을 **무작위로
받으면 아무에게도 안 걸렸습니다.**

주인은 `PerkManager.commit` 이 **고른 사람이 있을 때만** 적습니다. 그런데 증강이 들어오는
길은 그것만이 아닙니다.

```
숨은 재능 · 하늘의 은총   PerkRarityGrant   → 주인 안 적음
요행 · 도박꾼            PerkGambler       → 주인 안 적음
환골탈태                 PerkRarityReroll  → 주인 기록을 통째로 지움
```

주인이 없으면 `perkOwners.get(id)` 가 `null` 이고, 그러면 **팀의 누구와도 같지 않아** 효과가
아무에게도 안 걸립니다.

이제 주인이 없는 「고른 사람만」 증강에는 **접속 중인 팀원 중 한 명을 무작위로 정해 주고,
누구에게 걸리는지 팀 전원의 액션바에 띄웁니다.**

```
[증강] 비행 부적: 플레이어1님에게 걸립니다.
```

한 번 정해지면 저장까지 되므로 서버를 껐다 켜도 같은 사람입니다. 「고른 사람」과 완전히 같은
취급입니다.

**증강 id 를 하나도 적지 않습니다.** `OwnerBoundEffect` 표지를 단 효과가 있으면 주인을
잡습니다 — 새 효과가 같은 규칙을 쓰게 되면 표지만 달면 됩니다.

### 화로·제작대의 결과물이 안 보이는 칸으로 빨려 들어가던 것을 고쳤습니다

인벤토리와 핫바가 꽉 찬 상태에서 결과 칸을 쉬프트 클릭하면 **물건이 화면 밖 칸으로
들어가 사라진 것처럼 보였습니다.**

0.25.1-dev 에서 **줍는** 길을 막았는데, **창에서 옮기는** 길은 그대로였습니다. 잠긴 칸을 막는
장치가 `Slot.mayPlace` 에 주입하는 믹스인이었는데, **`ExpandedInventorySlot` 이 그
`mayPlace` 를 재정의해서 믹스인이 한 번도 실행된 적이 없었습니다.** 추가 칸을 가진 슬롯은 그
클래스뿐이라 막이가 통째로 죽어 있었습니다.

바닐라 `quickMoveStack` 이 `moveItemStackTo(플레이어 시작, +36)` 으로 부르고, 그것을
`ExpandedStandardMenuMixin` 이 **추가 27칸까지 포함한 목록**으로 바꿔 넘기기 때문에 잠긴 칸이
목적지가 됐습니다.

이제 판정을 슬롯 자신이 들고 있습니다. 꺼내는 것은 그대로 됩니다 — 이미 들어간 물건이
갇히지 않게.

## 0.25.2-dev — 2026-09-15

> **통신 규약은 24 그대로입니다.** 다만 고친 곳이 **클라이언트 화면**이라, 이 판은
> **클라이언트를 반드시 갱신해야** 효과가 있습니다. 서버만 갈면 아무것도 달라지지 않습니다.

### 왼쪽 위 표시가 어떤 사람에게만 영영 안 보이던 것을 고쳤습니다

좌표·바이옴·세트 줄과 「다음 증강까지」·「팀 레벨」이 **누구에게는 보이고 누구에게는 전혀 안
보였습니다.** 핫바도 체력바도 멀쩡하고, F3 글자도 안 보이고, 본인은 아무것도 건드린 적이
없다고 했습니다.

원인은 **F3 설정에 항목 하나를 「항상 켜기」로 둔 것**이었습니다. FPS 를 화면에 박아 두는
흔한 설정입니다.

26.2 의 `showDebugScreen()` 은 이름과 달리 「F3 화면이 떠 있다」가 아닙니다.

```java
if (!entries.isOverlayVisible() && entries.getCurrentlyEnabled().isEmpty()) return false;
if (!hud.isHidden()) return true;
return gui.screen() != null;
```

그리고 `currentlyEnabled` 에 들어가는 조건이 함정입니다.

```java
if (status == ALWAYS_ON || (isOverlayVisible && status == IN_OVERLAY)) { ... }
```

**항목 하나라도 `ALWAYS_ON` 이면 F3 를 켜지 않아도 언제나 참**입니다. 그 설정은
`debug-profile.json` 에 저장돼 게임을 껐다 켜도 남습니다. 그래서 한 번 박아 둔 사람은
그 뒤로 **영영** 안 보였습니다.

이제 「F3 판이 화면을 덮고 있는가」만 묻습니다(`DebugScreenEntryList.isOverlayVisible()`).
「항상 켜기」로 박아 둔 항목과는 겹칠 수 있지만, **FPS 한 줄 때문에 모드 HUD 가 통째로
사라지는 것보다 낫습니다.**

**같은 두 줄이 세 군데에 복사돼 있어 셋이 함께 죽었습니다.** 판정을 `DebugOverlay` 한 곳으로
모았고, 아무도 `showDebugScreen` 을 다시 부르지 못하게 클래스 파일을 뒤지는 시험을 붙였습니다.

## 0.25.1-dev — 2026-09-14

> **통신 규약은 24 그대로입니다.** 고친 곳이 모두 서버 안쪽이라 **클라이언트는 다시 배포하지
> 않아도 됩니다.** 0.25.0-dev 클라이언트로 그대로 접속하십시오.

> 증강·세트 정의는 바뀌지 않았습니다. `config/` 의 두 파일을 지울 필요가 없습니다.

### 주운 물건이 안 보이는 칸으로 빨려 들어가던 것을 고쳤습니다

「짐꾼도 없는데 나갔다 오면 27칸이 되어 있다」와 「18칸에서 템을 먹었는데 안 보인다」는
**한 가지 원인**이었습니다.

잠긴 칸을 막는 자리가 `Slot.mayPlace` **한 곳뿐**이었습니다. 그건 **창에서 손으로 옮길 때만**
지나는 길입니다. 바닥의 물건을 **줍는** 길은 칸(`Slot`)을 아예 지나지 않고
`Inventory.add` 에서 곧장 추가 칸 묶음으로 들어오는데, 거기서는 **27칸 전부**를 훑고 있었습니다.

그래서 열린 18칸이 차면 주운 물건이 **화면 밖으로 치워 둔 19~27번 칸**에 들어갔고, 다음
접속에서 「물건이 든 칸은 반드시 연다」 규칙이 그 칸을 찾아내 **짐꾼도 없는 팀에 27칸을 열어
줬습니다.** 물건이 사라진 것이 아니라 안 보이는 칸에 있었던 것입니다.

이제 넣는 쪽도 **열린 칸까지만** 봅니다. 18칸이 다 차면 바닐라와 똑같이 **물건이 바닥에
남습니다.** 이미 잠긴 칸에 들어가 있던 물건은 그대로 열린 채로 두므로 잃지 않습니다.

### 음식으로 차는 자연 회복이 인원수만큼 곱해지던 것을 고쳤습니다

허기와 포만감도 공유라 팀원 전원의 값이 같습니다. 그래서 `FoodData.tick` 의 회복 조건이
**같은 틱에 네 명 모두에게** 성립했고, 넷이 각자 회복해 **공유 체력이 4인분씩 한 번에**
차올랐습니다. 공유 풀은 한 사람 몫 크기라 몇 번 만에 만피가 됐습니다.

공유 상태이상에 이미 쓰던 **「대표 한 명만 실제로 겪는다」** 방식을 자연 회복에도 붙였습니다
(`SharedNaturalRegen`). 대표도 상태이상 쪽과 **같은 사람**을 고릅니다 — 갈리면 재생과 자연
회복이 함께 도는 팀에서 셈이 어긋납니다.

막는 자리는 `heal` 이 아니라 진입 조건인 `isHurt` 입니다. 그래야 회복 갈래에 아예 들어가지
않아 **허기 소모도 함께 1인분**이 됩니다. `heal` 만 막으면 회복은 없는데 배만 고파집니다.

전원 오프라인·사망으로 대표를 못 고르면 아무도 막지 않습니다.

### 엔딩 크레딧의 「최다 피해」가 「받은 피해량」이 됐습니다

```
3회차 승리      · 팀 이름
받은 피해량     · 아이디  412.5
최다 사망       · 아이디  2회
```

**마지막 책의 표지는 「최다 피해」 그대로입니다.** 크레딧만 바꿨습니다.

## 0.25.0-dev — 2026-09-11

> **통신 규약이 23 → 24 로 올랐습니다.** 0.24.0-dev 이하 클라이언트는 악수 단계에서
> 막힙니다. 서버와 모든 클라이언트를 함께 갱신하십시오.

> **서버를 갱신할 때 `config/sharedfate-perks.json` 을 지우십시오.** 그 파일이 있으면 jar
> 안의 정의 대신 그 파일이 쓰입니다. 이번 판은 **증강 정의가 바뀌었습니다.**

### 인벤토리 칸이 반듯해졌습니다

**「칸이 생겼다 사라졌다」의 원인이 둘이었습니다.**

- 짐꾼이 주는 6칸은 18 + 6 = 24, 곧 **2⅔줄**이라 셋째 줄 오른쪽 세 칸이 배경만 남았습니다.
- 열린 칸 수를 「기본 + 증강」과 「물건이 든 마지막 칸」 중 큰 쪽으로 매번 다시 세는데,
  아래 칸의 물건을 꺼내면 그 값이 21에서 18로 떨어져 **창 높이가 한 줄 접혔다 펴졌습니다.**

이제 열린 칸은 **언제나 9의 배수**입니다. 그리고 **「짐꾼」이 6칸에서 한 줄(9칸)로** 바뀌어
짐꾼 하나면 예전과 같은 9×7 = 63칸이 됩니다. 그 대신 「가호 3」의 강화가 없어졌습니다 —
이미 마지막 줄까지 열려 더 줄 칸이 없습니다.

**「짐꾼」을 골라도 칸이 바로 안 열리던 것도 고쳤습니다.** 열린 칸 수를 클라이언트에 알리는
묶음이, 증강을 고른 뒤에는 아무도 보내지 않고 있었습니다.

### 소집의 조각 — 쿨타임을 팀이 함께 씁니다

조각은 팀 공유 인벤토리에 있어 누구나 집어 쓸 수 있습니다. 그런데 쿨타임이 사람마다 따로
돌아서, 네 명이 번갈아 누르면 4분이 사실상 1분이었습니다. 이제 한 번 쓰면 **팀 전원이 함께
기다립니다.** 쿨타임 중에 접속한 사람에게도 남은 만큼 게이지가 걸립니다.

### 폭발 교환 — 대가와 혜택을 맞바꿨습니다

예전에는 「폭발이 터진다」와 「5초 예고가 사라진다」가 **둘 다 대가**였습니다.

- **예고가 돌아왔습니다.**
- 혜택은 **다음 교환까지 남은 시간이 화면 왼쪽 위에 늘 보이는 것**입니다(세트 줄 바로 위).
- 폭발은 **반경 4 → 20** 으로 커졌고, **블록을 부수지 않습니다.**

### 엔딩

드래곤을 잡으면 팀원 자리에서 폭죽이 계속 오르고, 그 위로 다섯 장이 넘어갑니다.

```
3회차 승리      · 팀 이름
최다 피해       · 아이디  412.5
최다 사망       · 아이디  2회
고른 증강       · 합계 17개
수고하셨습니다   · 제작자 카이렌
```

가운데 셋은 **전체 회차 누적**입니다. 셀 것이 없는 장은 빠집니다. 「최다 사망」과 「고른 증강」은
이번 판부터 쌓이기 시작하므로, 지난 회차 몫은 들어 있지 않습니다.

### 마지막 책이 회차별로 바뀌었습니다

```
표지    팀 · 완주 회차 · 최다 피해 · 최다 사망 · 고른 증강 합계
N회차   팀원별 받은 피해 → 회차를 끝낸 사람 → 그때 가졌던 증강 목록
맺음말  플레이해 주셔서 감사합니다. / 제작자 카이렌
```

**하트 환산(`/ 2.0♥`)은 뺐습니다.** 숫자 둘이 나란히 있으면 어느 쪽이 무엇인지 헷갈립니다.

### 증강 카드의 아이콘이 사라지던 것을 고쳤습니다

선택 화면은 카드 아이콘 크기를 큰 것부터 대 봅니다 — **32 → 16 → 0**. 설명이 길면 카드가
세로로 길어지는데, 셋 중 **가장 긴 한 장**이 남은 높이를 넘기는 순간 아이콘 크기가 0 으로
떨어집니다. 그리고 그 크기는 **줄 전체가 함께 쓰는 값**이라, 긴 카드 한 장 때문에 **나머지 두
장의 그림까지 같이 사라졌습니다.**

설명이 가장 긴 열두 장을 줄였습니다. 수치와 뜻은 그대로 두고 군더더기만 걷었습니다.

| 증강 | 글자 수 |
|---|--:|
| 운명 공동체 | 131 → **85** |
| 비행 부적 | 126 → **79** |
| 폭발 교환 | 122 → **84** |
| 소집의 조각 | 121 → **83** |
| 정거장 | 115 → **78** |
| 시차 | 108 → **71** |
| 나무꾼의 욕심 | 105 → **85** |
| 금기의 광석 | 104 → **76** |
| 허공답보 | 92 → **81** |
| 해시계 | 90 → **69** |
| 유산 | 87 → **64** |
| 버프 돌리기 | 86 → **73** |

앞으로 다시 길어지지 않도록 **설명 85자 상한을 시험으로 박아 두었습니다.** 이 문제는 빌드도
로그도 조용하고 카드를 눈으로 볼 때만 알 수 있습니다.

### 값이 바뀐 증강

| 증강 | 바뀐 것 |
|---|---|
| **짐꾼** | 인벤토리 +6칸 → **+9칸(한 줄)**, 가호 3 강화 없음 |
| **버티는 방패** | 낙하 피해를 막으면 **막은 만큼의 10배로 방패가 닳습니다** |
| **폭발 교환** | 반경 4 → **20**, 블록 파괴 **없음**, 교환 예고 **되돌림**, 교환 시계 표시 |
| **소집의 조각** | 쿨타임 4분이 **팀 공유**가 됩니다 |
| **비행 부적** | 설명에 「고른 사람만 쓸 수 있다」를 적었습니다(동작은 그대로) |

### 문서에서 바로잡은 것

증강 값을 적어 둔 표 다섯 줄이 **예전 값 그대로 남아 있었습니다.** 실제 동작과 달라
읽는 사람을 헷갈리게 하던 자리입니다. 지금 값은 [docs/증강-목록.md](docs/증강-목록.md) 에
있습니다.

| 증강 | 문서에 적혀 있던 것 | 실제 |
|---|---|---|
| 발맞춰 걷기 | 32칸 · 신속 II·재생 I·힘 I | **15칸** · 이동 속도·공격력 +15%·재생 I |
| 엇박자 | 20초 힘 I·신속 I → 10초 나약함 I | **15초 힘 I·신속 II → 5초 아무것도 없음** |
| 제왕과 신하 | 공격력 +50% | **+100%** |
| 살기 | 초당 2 | **초당 1** |
| 완충 | 4초에 걸쳐 | **8초에 걸쳐** |

## 0.24.0-dev — 2026-09-09

> **통신 규약이 22 → 23 으로 올랐습니다.** 0.23.0-dev 이하 클라이언트는 악수 단계에서
> 막힙니다. **팀마다 인벤토리 칸 수가 달라져** 그 값을 함께 내려보냅니다. 서버와 모든
> 클라이언트를 함께 갱신하십시오.

> **서버를 갱신할 때 `config/sharedfate-perks.json` 과 `config/sharedfate-sets.json` 을
> 둘 다 지우십시오.** 두 파일이 있으면 jar 안의 정의 대신 그 파일이 쓰입니다. 이번 판은
> **증강 정의와 세트 정의가 둘 다 바뀌었습니다.**

**증강이 94개(실버 34 · 골드 36 · 프리즘 24)가 됐고, 새 유형 「가호」와 「결속」이 생겼습니다.**
세트는 **유형 13개 · 27단계**입니다. **열세 유형 모두 실버·골드·프리즘을 하나 이상씩** 갖고
있어, 어느 등급 구간이 나와도 노리던 유형의 카드가 뜰 수 있습니다. 무유형은 15개이고, 새 효과
타입이 **12종**(75 → **87종**), 새 Mixin 5개입니다.

### 새 유형 「결속」 — 뭉치면 강해집니다

무유형으로 떠 있던 「발맞춰 걷기」·「운명 공동체」가 이미 한 갈래를 이루고 있어 유형으로 묶고,
**골드 둘·프리즘 하나를 새로 만들어 다섯 장**이 됐습니다.

| 등급 | 이름 | 뭉침 거리 | 효과 | 대가 |
|---|---|---|---|---|
| 실버 | **발맞춰 걷기** | 15칸 | 이동 속도 +15% · 공격력 +15% · 재생 I | 몹 체력 ×1.3 |
| 골드 | **방패벽** | 10칸 | 날아오는 **적대적 투사체가 사라집니다** | 뭉쳐야 하는 것 |
| 골드 | **파문** | 10칸 | **10초마다 충격파**로 몹을 밀어냅니다 | 뭉쳐야 하는 것 |
| 프리즘 | **운명 공동체** | 20칸 | 공격력 +50% · 받는 피해 −15% · 최대 체력 +4 · 주변 32칸 몹 15% 감속 | 벌어지면 강제 집합 + 구속 III |
| 프리즘 | **성역** | 20칸 | 주변 10칸 몹의 **모든 행동 −40%** | 떨어지면 몹 15% 가속 |

**단계는 3 하나뿐이고, 그것이 모든 거리를 1.5배로 늘립니다.** 기동·가호와 같은 모양입니다.
이 유형의 보상이 **제약 자체를 풀어 주는 것**이라, 뭉치기 힘들어서 못 쓰던 것이 모을수록
흩어져도 되는 것으로 바뀝니다.

- **「성역」은 몹의 틱을 건너뜁니다.** 이동 속도만 늦추는 것이 아니라 **공격 간격 · 크리퍼가
  부풀어 터지는 속도 · 활 쏘는 속도**가 한꺼번에 느려집니다. 크리퍼는 부풀기를
  `super.tick()` **보다 먼저** 끝내기 때문에 `Mob.tick()` 을 잘라서는 안 되고, 서버가 엔티티에게
  틱을 주는 입구(`ServerLevel.tickNonPassenger`)를 잘라야 넷이 함께 느려집니다
- **거리 계산은 한 곳에 모았습니다.** `TeamProximity` 가 1초에 한 번 「지금 얼마나 벌어져
  있나」를 재고 다섯 장이 그 값을 나눠 씁니다. 정의에 적은 거리를 그대로 넘기면 **세트 3의
  ×1.5가 저절로 먹습니다**
- **`conditional` 에 `team_nearby` 조건이 생겼습니다.** 뭉쳐 있는 동안만 속성·배율을 거는
  길이 열렸고, 앞으로 「뭉치면 방어력 +4」 같은 증강은 코드 없이 만들 수 있습니다
- **⚠ 「운명 공동체」의 최대 체력 +4 만 조건 밖입니다.** 최대 체력을 세는 곳이 효과의 맨
  윗단만 훑어서, 조건 안에 넣으면 **조용히 아무 일도 일어나지 않습니다**

### 새 유형 「가호」 — 한 사람이 강해지고 나머지가 손해를 봅니다

무유형 중에 **「고른 사람 하나만 강해지고 나머지 팀원이 대가를 치르는」** 것들이 이미 한 갈래를
이루고 있었습니다. **새 증강을 만들어 유형을 늘리면 기존 유형이 전부 희석되지만, 무유형을
묶는 것은 총 개수가 그대로라 희석이 0입니다.** 죽어 있던 카드가 살아나기만 합니다.

| 등급 | 이름 | 고른 사람 | 나머지 |
|---|---|---|---|
| 실버 | **전속 광부** (채굴 겸업) | 채굴 속도 ×5 | 채굴 속도 −15% |
| 실버 | **짐꾼** | 인벤토리 +6칸 | — |
| 실버 | **몽둥이찜질** (무기 겸업) | 막대기로 때리면 넉백 10 (플레이어 제외) | 공격력 −10% |
| 골드 | **열외** (교환 겸업) | 위치 교환에서 빠짐 · 교환 때 10초간 이속 +15% | — |
| 골드 | **호위** | 10초에 한 번 몹 피해를 통째로 차단 | 받는 피해 ×1.15 |
| 프리즘 | **제왕과 신하** | 공격력 +50% · 받는 피해 ×0.8 | 공격력 −15% |
| 프리즘 | **비행 부적** | 우클릭하면 10초간 비행(1분마다) | — |

**단계는 3과 4 둘뿐이고, 둘이 서로 맞바꿉니다.**

| 단계 | 보상 |
|---|---|
| **가호 3** | 가호 증강의 효과가 **강화**됩니다 |
| **가호 4** | **강화가 사라지는 대신, 가호 효과를 팀원 전원이 받습니다** |

- **2단계는 두지 않습니다.** 둘만 모은 사람이 아무것도 못 받는 것은 「기동」과 같은 의도된
  모양입니다. 끝까지 모아야 값이 나오는 자리로 남깁니다
- **4단계는 「전원이 이득을 받는다」인 동시에 「나머지가 치르던 대가가 사라진다」입니다.**
  「고른 사람」이라는 구분 자체가 없어지기 때문입니다. **강화된 한 명이냐, 평범한 전원이냐**를
  고르게 되어 세기가 저절로 잡힙니다. 처음 안(강화 + 전원 + 대가 제거)은 켜지는 순간 회차가
  끝나는 수준이라 접었습니다
- **강화와 「전원에게」는 일곱 장 전부에 걸립니다.**

  | 증강 | 평소 → 가호 3 |
  |---|---|
  | 전속 광부 | 채굴 ×5 → **×6** |
  | 짐꾼 | +6칸 → **+9칸** |
  | 몽둥이찜질 | 넉백 10 → **20** |
  | 호위 | 쿨타임 10초 → **5초** |
  | 열외 | 이속 +15% → **+30%** |
  | 비행 부적 | 10초 → **15초** |
  | 제왕과 신하 | 공격력 +50% → **+80%** · 받는 피해 ×0.8 → **×0.65** |

  넉백 교체·피해 차단·교환 제외는 상태이상도 속성도 아니라 `holder` 에 담을 수 없어 저마다
  독립 효과 타입입니다. 그것들은 「누가 골랐는가」를 직접 찾아보므로, 그 판정을
  `PerkBlessingSet.appliesTo`·`isAmplified` **한 곳으로 모아** 단계가 닿게 했습니다.
- **⚠ 「열외」를 가진 팀이 4단계에 닿으면 위치 교환이 멈춥니다.** 전원이 교환 대상에서 빠지는데
  교환에는 두 명이 필요하기 때문입니다. **의도한 대가입니다**
- **「비행 부적」은 4단계와 무관하게 원래 팀 전원의 것입니다.** 부적이 팀 공유 인벤토리의
  핫바 1번 칸에 꽂히기 때문입니다
- **가호 유형이 아닌 증강은 건드리지 않습니다.** 팀 상태만 보고 정하면 무유형 「버프 돌리기」까지
  덩달아 강화되거나 전원에게 퍼집니다. 그 증강의 재미는 **1분마다 무작위로 바뀌고 맞으면
  뺏긴다**인데 그것이 통째로 사라집니다. 같은 이유로 가호에 넣지도 않았습니다

### 인벤토리 칸이 증강에 따라 달라집니다 — 규약 23의 까닭

| 상태 | 추가 칸 | 전체 |
|---|--:|--:|
| 기본 | 18 | **54 (9×6)** |
| 짐꾼 | 24 | 60 |
| 짐꾼 + 가호 3 | 27 | **63 (9×7)** — 예전과 같습니다 |

**기본이 9×7 에서 9×6 으로 줄었습니다.** 나머지 한 줄은 이제 「짐꾼」으로 사는 것입니다.

- **칸을 지우는 것이 아니라 숨깁니다.** 메뉴에는 언제나 27칸이 만들어져 있고 연 만큼만
  보입니다. 팀마다 칸 수가 다르면 바닐라의 칸 동기화가 어긋나기 때문입니다
- **물건이 있는 칸은 절대 닫히지 않습니다.** 열린 칸 수를 `max(기본 18 + 증강, 물건이 든
  마지막 칸 + 1)` 로 매번 다시 셉니다. 가호 4로 넘어가 강화가 사라지든 「환골탈태」로 짐꾼을
  잃든 서버를 껐다 켜든 마찬가지입니다. **저장하지 않는 파생 상태라 스스로 맞습니다** —
  세트 판정이 보유 목록을 매번 다시 세는 것과 같은 방식입니다. 칸이 비면 그때는 줄어도
  됩니다. 잃을 물건이 없습니다
- **아직 열리지 않은 칸으로 물건이 빨려 들어가지 않습니다.** 화면 밖으로 치운 칸도 쉬프트
  클릭이 목적지를 고를 때는 후보에 그대로 남아 있어, 그대로 두면 안 보이는 칸으로 들어가
  사라진 것처럼 보입니다. **넣는 것만 막고 꺼내는 것은 막지 않습니다** — 잠긴 칸에 물건이
  들어 있다면 꺼낼 수 있어야 합니다

**클라이언트가 이 칸 수를 알아야 화면을 그릴 수 있습니다.** `TeamSyncPayload.Options` 에
실었고 **그것이 규약을 23으로 올린 까닭입니다.** 최상위 코덱이 여덟 칸까지라 `Options`
안에 넣었습니다.

### 프리즘 「비행 부적」

**「비행 부적」을 얻어 핫바 맨 왼쪽 칸에 고정됩니다. 우클릭하면 10초간 날 수 있습니다
(1분마다). 대신 그 칸을 다른 아이템에 쓸 수 없습니다.**

- 부적을 주 손에 들고 **허공을** 우클릭합니다. 바닐라에서 팬텀 막은 허공 우클릭에 아무 동작이
  없어 가로채도 잃는 것이 없습니다
- **꺼낼 수도 옮길 수도 버릴 수도 없습니다.** 끌기·쉬프트 클릭·숫자키 교환·창 안에서의
  버리기는 칸에서 막고, **인벤토리를 열지 않고 하는 버리기는 칸을 아예 지나지 않아** 따로
  막았습니다. 버리기 키를 무엇으로 바꿔 두었든 똑같이 걸립니다 — 서버에 오는 것은 키가 아니라
  「버리는 행동」입니다. 막는 것은 그 부적 한 자루뿐이고 다른 아이템 버리기는 조금도 달라지지
  않습니다
- **켠 채로 접속이 끊겨도 영구 비행이 남지 않습니다.** 비행 허가는 플레이어 저장 자료에
  그대로 적히는 값이라, 켜기 전 값을 기억해 두었다가 반드시 되돌립니다. 크리에이티브·관전
  모드인 사람에게는 **아예 켜지지 않습니다** — 원래 날 수 있어 되돌릴 것이 없습니다
- 비행 허가·쿨타임 게이지·액션바·소리가 전부 바닐라 패킷입니다. **이 증강만으로는 규약을
  올릴 이유가 없었습니다**

### 골드 「열외」 — 두 명짜리 팀에서는 교환이 멈춥니다

위치 교환에는 **두 명이 필요합니다.** 열외로 한 명이 빠져 남은 사람이 둘 미만이 되면 교환이
일어나지 않고 남은 시간이 1초로 되돌아가며 계속 다시 시도합니다. 예고 카운트다운도 나오지
않고, 교환이 없으니 열외의 이동 속도 보너스도 붙지 않습니다. **두 명짜리 팀에서 한 명이
고르면 그 회차의 위치 교환은 사실상 꺼진 것과 같습니다.**

이동 속도는 상태이상이 아니라 **속성 수정자**입니다. 바닐라 신속은 한 단계가 +20% 로 고정이라
15% 를 표현할 수 없습니다.

### 「돌아가는 곡괭이」가 「전속 광부」가 됐습니다

보유자가 **무작위로 돌던 것**을 **고른 사람 고정**으로 바꿨습니다. 채굴 속도 ×5, 나머지
−15% 라는 값은 그대로입니다. **채굴이자 가호**로 두 유형을 겸합니다.

무작위로 도는 동안에는 지금 누가 빠른지 아무도 몰랐고, 광맥을 파던 사람이 이유 없이
느려졌습니다. 고정으로 바꾸면 팀이 **역할을 나눌 수** 있습니다.

### 「제왕과 신하」가 상태이상에서 속성으로

**힘 II · 저항 I** 이던 것이 **공격력 +50% · 받는 피해 ×0.8** 이 됐습니다. 나머지 팀원의
공격력 −15% 는 그대로이고, 이 증강은 이제 **가호** 유형입니다.

**상태이상이 팀에 공유되어 새기 때문입니다.** 이 모드의 상태이상은 팀 공유 풀을 지나므로,
한 사람에게만 걸어야 할 힘 II 가 나머지에게까지 번질 위험이 있습니다. 속성 수정자는 사람마다
따로 붙었다 떨어져 그 위험이 없습니다.

### 추첨 전제조건 — 쓸 수 없는 증강은 후보에 뜨지 않습니다

지금까지 추첨은 **등급 · 이미 가진 것 · 최소 회차** 셋만 보고 팀 설정은 보지 않았습니다.
그래서 **위치 교환을 끈 팀에게도 교환 증강이 후보로 뜨고, 골라도 아무 일이 일어나지
않았습니다.** 다섯 장이 통째로 죽은 카드였습니다.

증강 정의에 `"requires": "position_swap"` 을 적을 수 있게 했습니다. 붙은 곳은 기존 다섯
(「본진이 바뀐다」·「시차」·「폭발 교환」·「정거장」·「뿌리내린 발」)과 신규 「열외」입니다.

- **덤으로 주는 경로도 함께 막았습니다.** 후보 추첨만 막으면 「요행」과 「하늘의 은총」이
  같은 증강을 그대로 건네줍니다
- **「소집의 조각」에는 붙이지 않았습니다.** 자동 교환에 기대지 않고 직접 부르는 것이라
  위치 교환을 끈 팀에서도 그대로 성립합니다

### 방어 프리즘 「완충」

방어에 프리즘이 하나도 없었습니다.

**받는 피해가 즉시 들어오지 않고 4초에 걸쳐 나뉘어 들어옵니다. 대신 나뉘어 들어오는 동안에는
체력이 회복되지 않습니다.**

- 크리퍼 한 방, 높은 낙하, 드래곤 몸통처럼 **한 번에 끝나던 것들이 4초짜리가 됩니다.**
  그 사이에 도망치거나 죽이면 삽니다
- 나뉘어 들어오는 동안 또 맞으면 **남은 몫과 합쳐 남은 시간에 다시 나뉩니다.** 시간이
  늘어나지는 않습니다

> **넉백이 사라집니다.** 피해가 그 순간에는 0 이라 때린 몹이 밀어내지 못합니다.
> 그리고 몫마다 방어 공식을 다시 지나므로 방어구가 조금 더 닳습니다.

### 사냥 프리즘 「살기」

사냥에도 프리즘이 없었습니다.

**팀원 주변 10칸 안의 적대적 몹이 초당 2의 피해를 받습니다. 팀원이 겹쳐 있으면 그만큼
곱해집니다. 대신 네더와 엔드에서는 아무 효과도 없습니다.**

- 넷이 뭉치면 초당 8입니다. **뭉칠수록 강해집니다**
- 전리품과 경험치가 그대로 나오고, **사냥 세트의 약탈도 함께 걸립니다**
- 소·양·닭·마을 주민·길들인 동물은 때리지 않습니다. **엔더 드래곤도 제외입니다**
- 맞은 몹은 가까이 있던 팀원을 노리고 달려옵니다

### 「도박 3」이 다시 뽑기 횟수도 줍니다

프리즘만 나오게 하는 것에 더해 **다시 뽑기 횟수를 3회 더** 받습니다. 도박 2(5회)까지 켜져
있으면 한 회차에 **11회**까지 다시 뽑을 수 있습니다.

한 회차에 가질 수 있는 **총 횟수 상한이 10 에서 15 로** 올랐습니다. 10 이던 때는 두 단계를
다 켜도 몫이 잘려 「3회 더」가 실제로는 2회밖에 늘지 않았습니다. 팀을 만들 때 정하는 다시
뽑기 횟수도 이제 **15까지** 고를 수 있습니다.

### 실버 「아가미」

회복 유형에 실버가 하나도 없어서, 회복 세트를 노리면 가장 잦은 실버 구간을 버려야 했습니다.
바다·물 갈래도 그동안 아무 증강이 쓰지 않았습니다.

**물에 들어가 있는 동안 재생 I 을 얻고 물에서 15% 빠르게 움직입니다. 땅에서는 아무 효과도
없습니다.**

- 물에 몸을 담그면 켜지고 나오면 꺼집니다. **비를 맞는 것은 해당하지 않고**, 머리까지 잠길
  필요도 없습니다
- 켜지고 꺼지는 것은 반 초마다 다시 봅니다

> **팀 안에서 물에 있는 사람만** 재생을 받습니다. 지금까지 조건부 증강은 팀 공유 체력·허기만
> 봐서 팀원이 언제나 같은 판정을 받았는데, 이 증강이 처음으로 사람마다 갈립니다.

### 프리즘 「소집의 조각」

교환 유형에 프리즘이 하나도 없어서, 교환 세트를 노리면 프리즘 구간을 통째로 버려야
했습니다.

**위치 교환이 저절로 일어나지 않는 대신, 언제 교환할지를 팀이 정합니다.**

- 「소집의 조각」을 하나 받습니다. **우클릭하면 나를 뺀 팀원 전원이 3초간 굳었다가 내
  자리로 끌려옵니다.** 쿨타임은 4분입니다
- **도착 지점은 누른 자리가 아니라 그 순간의 내 자리입니다.** 부르고 나서 걸어가면
  팀원은 그 뒤를 따라 도착합니다
- 끌려올 때 **위치 교환 효과가 그대로 발동합니다.** 교환 세트 3단계까지 켜져 있으면
  4초 무적인 채로 도착합니다
- **「시차」를 가지면 한 명씩** 굳고 끌려오기를 되풀이합니다. **「폭발 교환」을 가지면**
  끌려온 사람이 떠난 자리가 터집니다
- 굳어 있는 3초 동안은 걸을 수 없습니다. 시선은 자유롭습니다

> **도착 지점이 안전한지 검사하지 않습니다.** 용암 위에서 부르면 팀이 그리로 옵니다.
> 체력이 공유라 전멸로 이어질 수 있습니다.

새 아이템을 등록하지 않았습니다 — 바닐라 메아리 조각에 표식을 붙인 것이라 **모드를 깔지
않은 클라이언트도 그대로 보입니다.** 모루에서 이름만 바꿔서는 가짜가 만들어지지 않고,
쿨타임이 평범한 메아리 조각까지 잠그지도 않습니다.

### 「뿌리내린 발」은 그대로입니다

교환을 막는 방식이 두 가지가 됐습니다. 「뿌리내린 발」은 **교환 시점이 계속 찾아오고**
그때마다 실명과 구속을 뭅니다. 「소집의 조각」은 **교환 시점 자체가 없어집니다.**

둘 다 가지면 조각 쪽이 이깁니다. 교환 시점이 오지 않으므로 「뿌리내린 발」의 대가도 함께
멈춥니다.

### 새 효과 타입 9종

| 타입 | 무엇을 하나 |
|---|---|
| `rally_shard` | 조각을 쓰면 팀원 전원이 내 자리로 끌려옴 |
| `aura_damage` | 주변 적대적 몹이 초당 피해를 받음 |
| `spread_damage` | 받는 피해를 여러 초에 나눠 넣음 |
| `damage_ward` | 일정 시간마다 몹 피해를 한 번 통째로 차단 |
| `weapon_knockback` | 정해진 아이템으로 때릴 때의 넉백 |
| `swap_exempt` | 위치 교환에서 빠지고, 교환이 일어날 때 이동 속도 |
| `flight_charm` | 부적을 쓰면 잠깐 비행 |
| `inventory_slots` | 팀 공유 인벤토리 칸 |
| `proximity_range` | 뭉침 판정 거리 배율. **아직 쓰는 증강이 없습니다** |

### 고친 버그 — 「손에 쥔 목숨」이 엉뚱한 칸을 잠갔습니다

왼손 칸을 잠그는 자리에서 **`Slot.index`(화면 순번)와 `getContainerSlot()`(컨테이너 칸)을
혼동**했습니다. 컨테이너 칸 번호로는 왼손이 40 인데, 26.2 의 인벤토리 메뉴는 왼손 칸을 맨
마지막에 추가하므로 그 화면에서의 순번은 45 입니다. 그래서 **생존 인벤토리에서는 핫바 5번
칸이 잠겨 있었습니다.** 상자를 열면 또 다른 칸이 잠겼습니다.

`index` 는 화면마다 달라지고 `getContainerSlot()` 은 어느 화면에서도 같습니다. 바이트코드로
확인하고 뒤쪽으로 바꿨습니다. 이제 어느 화면에서도 왼손 칸만 잠깁니다.

### 쿨타임이 있는 증강 아이템의 남은 시간이 보입니다

**해시계 · 비행 부적 · 소집의 조각**을 손에 들고 있는 동안 액션바에 남은 시간이 뜹니다.
바닐라도 아이템 칸에 게이지를 그리지만 「얼마나 남았는지」는 알 수 없고, 4분짜리 「소집의
조각」처럼 긴 것은 게이지가 거의 안 움직여 더 그렇습니다.

- **들고 있을 때만** 보냅니다. 다른 액션바 알림(해시계의 광물 개수 · 보급 알림)을 덮지 않습니다
- 10초 미만은 **소수 첫째 자리까지** 적습니다. 마지막 몇 초가 가장 궁금한 자리입니다
- 총 쿨타임은 **아이템에 적힌 값**(`minecraft:use_cooldown`)을 읽습니다. 증강 정의를 고쳐도
  이미 나가 있는 아이템의 표시가 그 아이템의 실제 쿨타임과 어긋나지 않습니다
- 액션바는 바닐라 패킷이라 **통신 규약을 올리지 않습니다**

### 「짐꾼」의 칸이 바로 안 열리던 것을 고쳤습니다

증강을 골라도 **재접속하기 전까지 늘어난 칸을 쓸 수 없었습니다.** 칸 자리를 다시 잡는 일을
부르는 곳이 **메뉴를 만드는 자리뿐**이었는데, 인벤토리 메뉴는 접속할 때 한 번 만들어져 계속
살아 있습니다. 그래서 클라이언트는 새 칸 수를 받아 판을 크게 그리는데 서버 슬롯은 여전히 화면
밖에 있었습니다.

팀 동기화를 보내는 자리에서 함께 맞추도록 했습니다. **칸 수를 내려보내는 곳과 서버 메뉴를
고치는 곳이 같은 자리**여야 둘이 어긋나지 않습니다 — 증강을 고를 때든 「가호 3」이 켜질 때든
「환골탈태」로 잃을 때든 반드시 그 길을 지납니다.

### 「성역」과 「방패벽」이 켜진 것이 보입니다

범위 경계에 **파티클 고리**를 둘렀습니다. 조건이 맞아야 도는 증강인데 켜졌는지 알 방법이
없었습니다 — 성역은 몹이 원래 느린 건지 구분이 안 되고, 방패벽은 화살이 안 오는 것이 막힌
건지 안 쏜 건지 알 수 없었습니다.

- **성역** 푸른 영혼불 · **방패벽** 전기 불꽃. 어디까지가 안인지도 함께 보입니다
- 고리를 놓는 자리가 **판정 구역을 만드는 바로 그 자리**라 보이는 원과 실제 범위가 어긋날 수
  없습니다
- 발밑이 아니라 눈높이 아래에 띄웁니다. 바닥에 그리면 지형에 파묻힙니다
- 파티클은 바닐라 패킷이라 **통신 규약은 그대로 23** 입니다

### 값 조정

- **「비행 부적」은 이제 고른 사람만 쓸 수 있습니다.** 부적이 팀 공유 칸에 꽂혀 있어 누구나
  집을 수는 있지만, 우클릭이 듣는 것은 고른 사람뿐입니다 — **「가호 4」가 켜지면 전원**이
  쓸 수 있습니다. 가호는 「고른 사람 하나가 강해지는」 유형인데 이것만 예외였습니다
- **「버티는 방패」가 실버에서 골드가 됐습니다**
- **「완충」이 4초에서 8초에 걸쳐 들어옵니다.** 그리고 **몫마다 나던 피격 소리와 붉은 화면이
  사라졌습니다** — 한 번 맞은 것이 여러 몫으로 나뉘는데 몫마다 이펙트가 나면 여덟 번 맞은
  것처럼 보였습니다. 처음 맞은 그 한 번은 그대로 소리가 납니다
- **「제왕과 신하」의 공격력이 +50% 에서 +100%** 가 됐습니다(「가호 3」이면 **+130%**).
- **「살기」의 피해가 초당 2 에서 1** 로 줄었습니다
- **「엇박자」가 15초 힘 I·신속 II ↔ 5초 아무 효과 없음**으로 바뀌었습니다. 예전에는 20초
  힘 I·신속 I 뒤에 10초 나약함 I 이었습니다. **쉬는 구간에서 나약함이 사라진 것**이 큽니다
- **「파문」이 5초마다 돌고 밀어내는 힘이 두 배**가 됐습니다(10초·1.2 → 5초·2.4)
- **「버프 돌리기」가 결속 유형이 됐습니다.** 다만 이 증강에는 뭉침 조건이 없어 **세트
  「결속 3」의 거리 증가는 받지 못합니다** — 유형 개수만 채웁니다
- **「사냥꾼의 식사」의 몹 피해 ×1.1 에 엔더 드래곤 제외가 빠져 있었습니다.** 같은 성격의 다른
  열두 개에는 전부 있던 것이라 빠뜨린 것입니다. **드래곤이 주는 피해는 몸통 10 · 밀쳐내기 5
  인데 공유 최대 체력이 20** 이라 이 배율이 걸리면 안 됩니다
- **「제왕과 신하」가 무시되는 값 셋을 들고 있었습니다.** `fixed_to_owner` 로 바뀐 뒤
  `rotate_ticks`·`min_hold_ticks`·`pass_on_hurt` 가 죽은 값이 되어 **서버가 뜰 때마다 경고**를
  남겼습니다. 정의에서 걷어냈습니다
  받는 피해 ×0.8 과 나머지 팀원의 −15% 는 그대로입니다
- **위치 교환 주기의 상한이 120분에서 30분**으로 줄었습니다. 한 회차가 그렇게 길지 않아
  그 위는 「회차 내내 한 번도 안 바뀜」과 다르지 않았습니다. 굴림 단추 자리도
  `끔·1·5·10·15·20·30` 일곱으로 줄었습니다. **예전에 120분으로 만든 팀은 죽지 않고
  30분으로 접힙니다**
- **혼자인 팀이 위치 교환을 켜면 알려 줍니다.** 교환에는 두 명이 필요한데 팀을 만드는
  순간에는 언제나 리더 혼자라, 막지 않고 「지금은 안 돈다」는 사실만 알립니다. 초대해서
  둘이 되면 저절로 돕니다

### 그 밖

- **「요새 탐지기」가 골드에서 실버가 됐습니다**
- `/shareteam status` 가 「소집의 조각」을 가진 팀에게 **멈춰 있는 남은 시간 대신**
  「자동 교환이 꺼져 있습니다」를 보여 줍니다
- `/shareteam` 도움말이 다시 뽑기 범위를 아직 `<0~10>` 으로 적고 있었습니다. 실제 상한은
  「도박 3」과 함께 15로 올랐으므로 **`<0~15>`** 로 고쳤습니다

## 0.23.0-dev — 2026-09-07

> **통신 규약은 22 그대로입니다.** 패킷 형식이 바뀌지 않았습니다. 다만 서버와 클라이언트는
> 같은 버전을 쓰는 편이 안전합니다.

### 세트 진행도가 없는 단계를 가리켰습니다

방어는 단계가 2·3 둘뿐인데 넷을 모으면 `방어 4/4` 가 떴습니다. 채굴은 열을 모으면
`채굴 10/10` 이 떠서 10단계가 있는 것처럼 읽혔습니다. **유형 열한 개 전부**에서 나던
증상입니다.

분모를 **최고 단계로 고정**했습니다 — `방어 4/3`. 분자가 분모보다 큰 것이 그대로
「더 모았지만 더 켤 것은 없다」는 뜻이 됩니다. 마우스를 올렸을 때 뜨는 툴팁도 같았습니다.

기동은 단계가 3 하나뿐인데 `기동 1/2` 가 떴습니다. 2를 채워도 아무것도 안 켜지고 곧바로
`2/3` 이 되던 것도 함께 고쳤습니다.

### 요새 탐지기와 길잡이 나침반을 같이 가지면 마을을 못 가리켰습니다

나침반 증강을 여럿 가져도 **하나만 고르고 차원을 보지 않았습니다.** 요새 탐지기를 먼저 얻은
팀은 오버월드에 서 있어도 요새 정의가 골라지고, 네더에 아무도 없으니 나침반이 아무 데도
못 가리켰습니다.

이제 **지금 서 있는 차원의 정의만** 후보가 됩니다. 오버월드에서는 마을, 네더에서는 요새를
가리킵니다.

> 인벤토리가 공유라 나침반은 팀에 하나뿐입니다. 한 명이 네더·한 명이 오버월드일 때 각자
> 다른 곳을 가리키게 할 수는 없어, **나침반을 손에 든 사람의 차원이 이깁니다.**

### 인벤토리 능력치가 세트 줄과 겹쳤습니다

인벤토리를 열어도 화면 왼쪽 위의 세트 줄은 계속 그려져 글자가 서로 뭉갰습니다. 능력치
덩어리를 세트 줄 아래로 내렸습니다. 1080p 기본 배율에서 일곱 줄쯤 내려갑니다.

> 화면이 아주 짧으면(720p 배율 3 등) 능력치가 「단추만 남기고 감춤」으로 물러납니다.
> 자리가 물리적으로 모자랍니다. 값은 단추가 여는 팀 화면 「능력치」 탭에 그대로 있습니다.

### 숨죽인 걸음

**설명이 처음부터 사실이 아니었습니다.** 웅크림 속도 속성은 상한이 걷기 속도라, 아무리
올려도 달리기를 넘을 수 없습니다. 「달리기보다 10% 빠르게」라고 적혀 있었지만 실제로는
**달리기의 69%** 였습니다.

이제 **웅크린 채로 달리기보다 35% 빠릅니다.** 달리다 웅크려도 배수가 겹치지 않습니다.
1블록 틈을 기어갈 때도 같은 배수가 붙습니다.

### 비옥한 땅

「작물을 수확하면 3배」에서 **「밀을 수확하면 밀 대신 황금 당근을 1~3개」**로 바뀌었습니다.
대가는 허기 회복량 20% → **15%** 감소입니다.

- 씨앗은 그대로 나옵니다. 농사를 이어갈 수 있습니다
- 덜 자란 밀은 아무 일도 일어나지 않습니다
- 행운은 씨앗만 늘리고 황금 당근 개수에는 영향이 없습니다
- 「메아리 채굴」이나 채굴 세트로 함께 캐지는 밀에도 똑같이 걸립니다

### 해시계

- **쿨타임 20초 → 30초**
- 개수만 알려 주던 것에 **가장 가까운 다이아몬드 하나의 좌표**를 더했습니다 —
  `[증강] 가장 가까운 다이아몬드: X 128  Y 12  Z -302 (18칸, 근처 3개)`
- 파티클은 그대로 32칸 안의 광석을 전부 표시합니다
- 거리가 같은 후보가 여럿일 때 **같은 자리에서 두 번 써도 같은 좌표**가 나옵니다

### 팀 화면

**보유 증강 줄에서 세트 설명 상자가 뜨지 않습니다.** 증강 설명은 줄 아래에 펼쳐져 있는데
그 상자가 바로 그 설명을 덮고 있었습니다. 위쪽 세트 줄의 툴팁은 그대로입니다.

## 0.22.0-dev — 2026-09-06

> **통신 규약이 21 → 22 로 올랐습니다.** 세트 줄에 「켜진 시점」 칸이 하나 늘었습니다.
> 0.21.0-dev 는 배포하지 않았으므로, 0.20.0-dev 에서 오는 경우 **한 번만** 갱신하면 됩니다.

**실제로 플레이해 보고 나온 것들을 고쳤습니다.**

### 증강 고를 때 세트가 안 보이던 것

곁판을 그리는 코드는 있었는데 **기본 GUI 배율에서 통째로 사라지고 있었습니다.** 카드 세 장이
가로 364px 를 고정으로 쓰고 화면 한가운데 놓이는데, 1080p 의 자동 배율은 4(=480px 폭)라
왼쪽에 남는 자리가 40px 뿐이었습니다. 곁판은 그보다 좁으면 스스로 물러납니다.

이제 **카드를 놓기 전에 곁판 자리를 먼저 뗍니다.** 카드 폭은 줄지 않고 오른쪽으로 밀립니다.
640px 이상에서는 아무것도 달라지지 않습니다. 아주 작은 창에서는 여전히 곁판을 포기합니다 —
자리를 떼면 카드가 좁아져 증강 설명이 잘리기 때문입니다.

선택창이 뜨면 **HUD 의 세트 줄은 접힙니다.** 같은 내용이 흐린 것과 또렷한 것으로 두 벌
보이지 않게 하기 위해서입니다.

### 보급

- **시계가 진행도 뒤로 갔습니다** — `◆ 보급 4/4 04:12`
- **10분을 「보급 2/3 이 켜진 그 순간」부터 셉니다.** 전에는 게임 시간의 배수가 경계라,
  세트를 켠 자리에 따라 첫 보급이 몇 초 만에 오거나 꼬박 10분을 기다렸습니다
- 단계가 올라 주기가 10분에서 5분으로 줄어도 **남은 시간이 늘어나지 않습니다.** 이미 지난
  시간은 그대로 지난 것으로 칩니다
- 켜진 시점을 **월드에 저장합니다.** 저장하지 않으면 주기보다 자주 재시작하는 서버에서
  기준이 매번 되감겨 보급이 한 번도 오지 않습니다. 서버가 꺼져 있던 동안 지나간 회차를
  몰아서 주지는 않습니다

### 해시계

- **쿨타임 20초 → 30초**
- 개수만 알려 주던 것을 **가장 가까운 다이아몬드 하나의 좌표**까지 알려 주도록 바꿨습니다
  — `[증강] 가장 가까운 다이아몬드: X 128  Y 12  Z -302 (18칸, 근처 3개)`
- 파티클은 그대로 32칸 안의 광석을 전부 표시합니다
- 거리가 같은 후보가 여럿일 때 **같은 자리에서 두 번 써도 같은 좌표**가 나오도록 규칙을
  못박았습니다(거리 → y → x → z). 전에는 훑는 차례에 딸려 갔습니다

### 팀 화면

**보유 증강 줄에서 세트 툴팁이 안 뜹니다.** 증강 설명은 툴팁이 아니라 줄 아래에 펼쳐져
있는데, 세트 툴팁 상자가 바로 그 설명을 덮고 있었습니다. 위쪽 세트 줄의 툴팁은 그대로입니다.

## 0.21.0-dev — 2026-09-06

> **통신 규약이 20 → 21 으로 올랐습니다.** 0.20.0-dev 이하 클라이언트는 악수 단계에서
> 막힙니다. 세트 줄에 「주기」 칸이 하나 늘어 형식이 바뀌었습니다.

> **서버를 갱신할 때 `config/sharedfate-perks.json` 과 `config/sharedfate-sets.json` 을
> 둘 다 지우십시오.** 두 파일이 있으면 jar 안의 정의 대신 그 파일이 쓰입니다. 이번 판은
> **증강 정의와 세트 정의가 둘 다 바뀌었습니다.**

**드러나 있던 버그 다섯 개를 잡고, 세트 효과와 확장 인벤토리를 손봤습니다.**
시험이 **1708 → 1844개**입니다.

### 고친 버그

- **「유산」이 도구를 하나도 가져오지 못했습니다.** 갑옷만 넘어왔습니다. 스냅샷을 뜨는
  자리가 바닐라가 인벤토리를 쏟은 **뒤**라, 그 시점에는 공유 인벤토리 36칸이 이미 비어
  있었습니다. 곡괭이·도끼·삽뿐 아니라 주 손에 든 검·활도 사라진 뒤였습니다. 이제 쏟기
  직전에 뜹니다
- **유산 장비의 인챈트가 회차를 넘길 때 사라졌습니다.** 팀 명단 파일이 레지스트리 없는
  연산자로 아이템을 적어 인챈트 코덱이 오류를 냈습니다. 인챈트만 빠지는 것이 아니라
  **그 장비가 통째로 버려졌습니다**
- **「나무꾼의 욕심」과 보급 알림이 아이템 이름을 `Air` 로 띄웠습니다.** 인벤토리에 넣은
  뒤에 이름을 읽어서 그때는 묶음이 비어 있었습니다. 아이템 이름이 서버 언어로 굳던 것도
  함께 고쳐, 이제 각자의 언어로 뜹니다
- **신호기·양조대에서 확장 칸을 쉬프트 클릭하면 아이템이 두 배가 됐습니다.** 목적지 목록에
  출발 칸이 섞여 자기 자신과 합쳐졌습니다
- **「금기의 광석」이 왼손을 보지 않았습니다.** 다이아 검을 왼손에 걸면 제한을 통째로
  비켜 갈 수 있었습니다

### 확장 인벤토리 — 아래 27칸이 이제 진짜 인벤토리입니다

- **쉬프트 클릭으로 장비가 곧바로 장착됩니다.** 아래 칸의 갑옷이 갑옷 칸으로 갑니다
- **주민 거래가 아래 칸의 재료를 알아봅니다**
- **핫바가 비어 있으면 핫바로 먼저 갑니다.** 위 세 줄로 새지 않습니다
- **모루·인챈트대·연마석·대장장이·양조대·직조기·지도 제작대·석재 절단기·말·신호기·호퍼·
  발사기·셜커 상자·통에서도 됩니다.** 화면마다 코드를 두지 않고 모든 화면이 지나는 자리
  하나를 잡아, 앞으로 늘어나는 화면도 따로 손댈 필요가 없습니다

> 상자가 가득 찬 상태에서 아래 칸을 쉬프트 클릭하면 이제 물건이 핫바로 올라갑니다.
> 바닐라는 아무 일도 하지 않던 상황입니다.

### 세트 효과

- **무기 3단계는 「공격력이 깎이지 않습니다」입니다.** 지금 가진 증강뿐 아니라
  **앞으로 얻는 증강의 공격력 감소도 전부** 사라집니다. 나약함 같은 상태이상, 공격력
  증가를 막는 효과, 몹에게 걸리는 값은 대상이 아닙니다. 2단계(공격 속도 +15%)는 그대로입니다
- **기동은 3단계 하나로 갑니다.** 2단계는 만들지 않습니다
- **보급 줄에 다음 보급까지 남은 시간이 뜹니다** — `◆ 04:12 보급 4/4`. 보급 줄에만
  붙습니다
- **보급이 오면 무엇이 왔는지 채팅으로 알립니다.** 빈손일 때도 알립니다

### 증강

- **「나무꾼의 욕심」** — 철 15% → 13%. 줄인 2%로 **엄청난 보상**이 나옵니다
- **「금기의 광석」** — 다이아 장비를 곧바로 버리지 않고 **인벤토리로 밀어냅니다.**
  인벤토리마저 꽉 차 있을 때만 버립니다. 그리고 **네더라이트 장비의 내구도가 3배**가 됩니다
- **「경장」** — 착용할 수 없는 것이 다리 방어구에서 **신발**로 바뀌었습니다
- **「메아리 채굴」** — 주변 아무 블록이나 캐던 것을 **같은 블록만** 캐도록 바꿨습니다.
  돌 사이의 다이아를 캐면 돌은 캐지지 않습니다
- **「흡혈귀」** — 흡수 III 이 다시 채워지는 주기가 20초 → **2분**

### 그 밖

- 「금기의 광석」의 밀어내기·버리기 알림에 **1초 쿨다운**을 넣었습니다. 알림만 줄어들고
  치우는 동작 자체는 그대로입니다

## 0.20.0-dev — 2026-09-05

> **통신 규약이 19 → 20 으로 올랐습니다.** 0.19.0-dev 이하 클라이언트는 악수 단계에서
> 막힙니다. 서버와 모든 클라이언트를 함께 갱신하십시오.

> **서버를 갱신할 때 `config/sharedfate-perks.json` 과 `config/sharedfate-sets.json` 을
> 둘 다 지우십시오.** 두 파일이 있으면 jar 안의 정의 대신 그 파일이 쓰입니다.
> **옛 증강 파일이 남으면 유형이 하나도 안 붙어 세트가 통째로 안 켜집니다.**
> 세트 파일은 **이번 판에서 처음 생기는 것**이라 특히 놓치기 쉽습니다 — 서버가 처음 켜질 때
> jar 안의 정의를 그 자리에 꺼내 놓고, 그 다음부터는 꺼내 놓은 쪽만 읽습니다. 지우면
> 새 정의로 다시 만들어집니다.

**세트 효과를 넣었습니다.** 증강 82개에 유형 11개를 붙였고, 같은 유형을 임계값만큼 모으면
보상이 켜집니다. 보상 22단계를 전부 만들었습니다. 새 효과 타입이 **12종**(59 → **71종**),
새 Mixin 3개, 시험이 **1292 → 1613개**입니다. 덤으로 **대장장이 형판 없이 네더라이트를
만들 수 있습니다.**

### 유형 열한 개

| 유형 | 증강 | 실버 | 골드 | 프리즘 | 임계값 | 노리면 | 무심 |
|---|--:|--:|--:|--:|:--:|--:|--:|
| **채굴** | 10 | 6 | 3 | 1 | 3 | 47% | 5% |
| **화력** | 8 | 1 | 6 | 1 | 3 | 32% | 3% |
| **기동** | 7 | 2 | 3 | 2 | 2 | 59% | 14% |
| **방어** | 7 | 5 | 2 | 0 | 2 | 58% | 14% |
| **보급** | 7 | 3 | 2 | 2 | 2 | 59% | 14% |
| **무기** | 6 | 1 | 3 | 2 | 2 | 49% | 10% |
| **도박** | 5 | 2 | 1 | 2 | 2 | 38% | 7% |
| **생존** | 5 | 2 | 1 | 2 | 2 | 38% | 7% |
| **사냥** | 5 | 3 | 2 | 0 | 2 | 40% | 8% |
| **교환** | 4 | 2 | 2 | 0 | 2 | 29% | 5% |
| **회복** | 4 | 0 | 2 | 2 | 2 | 26% | 4% |
| 무유형 | 16 | | | | — | | |

「노리면」은 그 유형 카드가 보일 때마다 집었을 때, 「무심」은 세트를 신경 쓰지 않았을 때
켜질 확률입니다. 실제 추첨 규칙(`PerkDraft`)으로 20만 회를 굴려 낸 값입니다.

- **한 증강이 유형을 둘 가질 수 있고, 그때는 양쪽에 다 세어집니다.** 「원정 준비물」은
  보급이자 도박, 「피의 대가」는 화력이자 회복입니다. 어느 한쪽으로만 세면 그 증강을 집은
  사람이 손해를 봅니다
- **단계는 누적입니다.** 채굴을 4개 모으면 2·3·4단계가 전부 켜집니다. 가장 높은 것 하나만
  켜지는 것이 아닙니다
- **방어·사냥·교환에는 프리즘이, 회복에는 실버가 하나도 없습니다.** 그 등급 구간에는 그
  유형 카드가 뜰 수 없다는 뜻입니다. 82개 안에서 옮겨 올 후보가 없어 남겨 두었습니다
- **무기 6개는 보상이 아직 없습니다.** 화면에는 「무기 2/2」로 진행도만 뜹니다. 정의 파일의
  자리는 비운 채로 남겨 두었으니 값만 넣으면 켜집니다
- **기동은 3단계 하나뿐입니다.** 59% 구간이 통째로 비어 있어, 둘만 모은 사람은 화면에
  `기동 2/3` 만 보고 아무것도 받지 못합니다

### 보상 22단계

| 단계 | 무엇을 하나 |
|---|---|
| **채굴 2** | 광물에서 나오는 경험치 ×1.5 |
| **채굴 3** | 다이아몬드 광석을 캘 때 +2개 |
| **채굴 4** | 캘 때 바로 옆의 **같은 종류** 블록 2개가 함께 캐짐 |
| **보급 2** | 10분마다 무작위 보급. **30% 는 꽝** |
| **보급 3** | 꽝 12%, 두 번 뽑기, 표 전체가 좋아짐 |
| **보급 4** | 주기가 5분 |
| **화력 2** | 공격력 +1 |
| **화력 3** | 준 피해의 5% 회복 |
| **화력 4** | 흡혈 15%, 공격력 +0.5. 대신 몹 스폰율 +35% |
| **기동 3** | 낙하 피해 −80% |
| **방어 2** | 방패로 막는 동안 받는 피해 ×0.5 |
| **방어 3** | **방어 증강들의 대가가 사라짐** |
| **도박 2** | 다시 뽑기 5회 |
| **도박 3** | 다시 뽑으면 프리즘만 나옴 |
| **사냥 2** | 약탈 I, 몹 경험치 ×1.5 |
| **사냥 3** | 약탈 III |
| **생존 2** | 팀 최대 체력 +2 |
| **생존 3** | 피해 1당 허기 0.4 |
| **교환 2** | 교환 뒤 4초간 신속·재생 II·흡수 II·저항 II |
| **교환 3** | 교환 뒤 4초간 저항 V |
| **회복 2** | 음식을 먹을 때마다 체력 +2 |
| **회복 3** | 흡혈로 되돌리는 양 ×1.3 |

**단계에는 이름을 붙이지 않습니다.** 「보급 2」·「보급 3」처럼 **유형과 열리는 개수**만
보여 줍니다. 그 숫자가 곧 「그 유형 증강을 몇 개 모으면 열리는가」입니다.

**세트에는 대가가 없습니다.** 고르는 대상이 아니라 결과라 판단할 기회가 없기 때문입니다.
대가는 이미 두 겹으로 치러집니다 — 여덟 번 중 셋을 한 방향에 쓴 기회비용과, 그 유형
증강들의 대가가 곱해지는 것입니다. **화력 4단계의 「몹 스폰율 +35%」만이 예외**입니다.

### 규약을 20으로 올린 까닭 둘

둘 다 클라이언트가 **증강 풀을 아예 안 읽는다**는 한 가지 사실에서 나옵니다.
`PerkRegistry.load` 를 부르는 곳은 서버 경로 하나뿐이고, 보유 증강조차 id 없이
`(이름, 설명, 등급)` 문자열로만 내려갑니다.

1. **툴팁에 「아직 안 가진 증강」을 띄우려면** 새 S2C 패킷이 필요했습니다. 클라이언트는
   「채굴을 몇 개 가졌나」도 「채굴에 무엇이 더 있나」도 스스로 셀 수 없습니다
2. **선택 카드에 유형을 넣으면서** `PerkOfferPayload.PerkOption` 에 칸이 하나 늘어
   **형식 자체가 바뀌었습니다.** 옛 클라이언트는 선택창 패킷을 읽지 못합니다

새 패킷은 진행도(`유형·가진 개수·다음 임계값·켜진 단계`) 열한 줄과, 유형에 속한 증강
목록(`유형·이름·등급·가졌는가`)을 함께 싣습니다. **가진 것도 함께 싣습니다** — 빼 버리면
「전부 모았다」와 「목록이 잘렸다」를 구별할 수 없습니다.

- **임계값과 켜진 단계는 서버가 계산해 보냅니다.** `PerkSetType.threshold()` 는 시뮬레이션
  결과라 앞으로도 자주 바뀝니다. 양쪽에 적어 두면 버전이 다른 클라이언트가 다른 숫자를
  그립니다
- **보내는 쪽은 사건 훅이 아니라 0.5초마다 훑어 값이 달라진 사람에게만 보냅니다.** 보유
  목록이 바뀌는 자리가 넷이라(`PerkManager.commit`·`setPerksEnabled`·`TeamState.applyPerkSection`·
  `PerkTestCommand.reapply`) 하나만 빠뜨리면 그 경우에만 화면이 옛 값을 듭니다
- **목록 상한을 코덱에 맡기지 않고 먼저 잘랐습니다.** `ByteBufCodecs.list(max)` 는 상한을
  넘으면 예외를 던지는데, 그 예외는 패킷 한 장이 아니라 **접속 자체를 끊습니다.** 상한은
  11줄·160행이고 지금 실제 값은 11줄·68행입니다
- 접속을 끊으면 서버는 마지막으로 보낸 값을, 클라이언트는 들고 있던 값을 각각 버립니다.
  한쪽만 버리면 재접속한 사람에게 서버가 「이미 보냈다」고 여겨 그 회차 내내 세트 줄이
  안 뜹니다

### 세트를 증강으로 등록하지 않았습니다

원래 설계는 「세트를 증강처럼 만들고 추첨 풀 네 곳에서 뺀다」였습니다. 그 길을 접었습니다.

**「환골탈태」가 보유 목록의 크기를 그대로 세고 목록을 통째로 비운 뒤 그 개수만큼 골드로
채우기 때문입니다.** 세트를 목록에 넣으면 (1) 개수가 부풀어 골드를 더 받고 (2) 갈아엎을 때
세트가 지워지고 (3) 그 자리를 골드가 차지합니다.

**세트는 보유 목록을 읽어 매번 다시 세는 파생 상태입니다.** 그러자 추첨 풀 네 곳
(`PerkDraft`·`PerkGambler`·`PerkRarityGrant`·`PerkRarityReroll`)을 막을 필요조차 없어졌습니다 —
애초에 풀에 없습니다. 이미 이 저장소의 관례이기도 합니다(「원정 준비물」의 실버 차단도,
몹 배율도 전부 매번 다시 셉니다).

**저장하지도 않습니다.** 저장하면 환골탈태가 목록을 비울 때 저장된 세트만 살아남아 실제와
어긋납니다.

- **그래서 재평가 훅을 어디에도 걸지 않았습니다.** 보유 목록이 바뀌는 모든 경로가 결국
  증강 효과를 다시 붙이는 자리를 지나므로, 그 바로 뒤에서 세트를 다시 맞추면 끝입니다.
  「세트가 켜지는 순간」을 감시하는 코드가 따로 없습니다
- **판정은 반드시 보유 목록(`ownedPerks`) 기준입니다.** 주인 기록(`perkOwners`) 으로 세면
  「요행」·「숨은 재능」·「하늘의 은총」이 덤으로 준 증강이 세트에서 빠집니다 — 그것들은
  주인이 없기 때문입니다(설계 의도)
- **딱 하나 저장하는 것이 있습니다 — 도박 2단계의 다시 뽑기 5회.** 이것만은 쓰면 줄어드는
  값이라 재계산형으로 만들면 접속할 때마다 5씩 불어납니다. 그래서 저장 형식에
  「세트로 얻은 몫」 칸을 따로 두고, **직접 고름과 시간초과 자동선택이 합류하는 유일한
  지점**에서 한 번만 맞춥니다. 하는 일이 「더한다」가 아니라 「지금 값에 맞춘다」라
  두 번 불려도 안전하고, 연쇄 도중 환골탈태로 도박 증강을 잃으면 그 자리에서 0으로
  내려갑니다. 저장 칸은 있어도 그만 없어도 그만이라 **옛 저장 파일도 그대로 읽힙니다**
- **남은 횟수만 5 올리면 안 됐습니다.** 저장을 읽고 쓸 때 남은 횟수를 회차당 허용치로 자르는
  자리가 둘 있어서, 그대로 두면 **서버를 다시 켤 때마다 3으로 되돌아갑니다.** 그 두 자리의
  상한을 「허용치 + 세트 몫」으로 바꿨습니다
- 회차당 허용치 3에 5를 얹어 8회입니다. 총 상한 10은 세트 몫을 접는 한 곳에서만 봅니다 —
  접는 규칙이 두 군데 있으면 반드시 어긋납니다. **회차당 10회로 정한 팀은 세트를 모아도
  더 받지 못합니다**

### 「대가 무효화」를 감싸는 방식으로 만들지 않았습니다

방어 3단계는 가지고 있는 **방어 유형 증강의 대가만** 없앱니다. 값이 가장 비싼 보상이었습니다.

효과 목록은 평면이고 「이건 대가다」라는 표시가 없었습니다. 자연스러운 답은 대가를
`DrawbackEffect` 로 감싸는 것인데, **그 길은 막혀 있습니다.**

**이 저장소에서 효과를 소비하는 자리는 예외 없이 `instanceof` 로 형을 가립니다.** 감싸면
그 `instanceof` 가 전부 빗나가므로 **세트가 꺼져 있을 때조차 대가가 사라집니다.** 「표시가
없으면 지금과 똑같이」를 지키려면 효과를 훑는 자리를 하나도 빠짐없이 고쳐야 하고, 한 곳이라도
빠뜨리면 빌드도 통과하고 로그도 없이 증강이 조용히 반쪽이 됩니다.

**대신 효과 객체 → 증강 id 표를 옆에 두었습니다.** 정의에 `"drawback": true` 를 적으면 그
효과가 표에 등록됩니다. 효과 객체는 그대로이므로 표를 모르는 자리는 예전과 완전히 같은 길을
지나고, 표를 보는 자리만 골라서 건너뜁니다. 덤으로 **효과 객체가 스스로는 알 수 없는
「나를 가진 증강이 누구인가」를 함께 적어 둘 수 있습니다** — 「방어 유형의 대가만」을 지키려면
그 정보가 반드시 필요하고, `on_team_hurt` 의 하위 효과처럼 소비하는 자리에 부모 증강이
넘어오지 않는 경로도 있습니다.

지금 표시가 붙은 것은 다섯입니다.

| 증강 | 사라지는 대가 |
|---|---|
| 바람 빠진 방패 | 넉백 2배 |
| 동병상련 | 발동 중 공격력 감소 |
| 화살막이 | 폭발 피해 ×1.2 |
| 맨살의 각오 | 채굴 속도 −25% |
| 불굴 | 체력 75% 초과 시 받는 피해 ×1.1 |

- **「철벽」이 목록에 없는 것은 그 대가를 아예 없앴기 때문입니다**(아래 값 조정 참고).
  「버티는 방패」와 「철벽」에 남은 몹 체력 배율은 표시하지 않았습니다 — 그것은 나에게 붙는
  것이 아니라 세상이 바뀌는 것이라 붙였다 뗄 자리가 다릅니다
- **표시는 아무 데나 붙일 수 없습니다.** 읽어서 실제로 건너뛰는 자리가 있는 곳
  — 증강의 최상위 효과와 `on_team_hurt`·`on_critical` 의 하위 효과 — 뿐입니다.
  조건부의 `when_true`/`when_false` 안에 적으면 경고를 남기고 무시합니다
- **필드가 없어 하나를 돌려쓰는 효과에는 붙이면 안 됩니다.** 표의 열쇠가 객체의 신원이라
  같은 형을 쓰는 다른 증강까지 대가로 보입니다. 그런 일이 생기면 경고가 남습니다
- **건너뛰기만 해서는 부족했습니다.** 세트가 켜지기 전에 이미 붙어 있던 속성 수정자를
  걷어내야 하고, 조건부는 자기 하위 효과를 스스로 붙였다 떼므로 그쪽에도 같은 필터를
  넣어야 했습니다 — 안 그러면 걷어낸 것을 반 초 뒤에 도로 붙입니다.
  걷어내는 쪽은 가리지 않습니다. 걸려 있지도 않은 것을 걷어내는 일은 언제나 안전합니다
- 면제 필터가 들어간 자리는 **다섯**입니다 — 속성을 붙이는 곳, 피해 배율을 합성하는 곳,
  조건부의 주기 재평가, 피해원별 배율, 그리고 `on_team_hurt` 가 얹는 임시 효과.
  **대가를 하나도 가지고 있지 않은 팀은 세트를 보지도 않습니다** — 그런 정의가 없는
  서버의 피해 계산은 예전과 한 톨도 다르지 않습니다
- 「동병상련」처럼 **발동 자체는 이득이고 하위 하나만 대가인** 정의가 있어서, 표시는
  부모가 아니라 그 하위에 붙습니다

### 대장장이 형판은 데이터만으로 끝났습니다

**네더라이트 업그레이드에 형판이 필요 없어집니다.** 다이아몬드 장비와 네더라이트 주괴만
있으면 됩니다.

Mixin 0개, 규약 영향 없음, 서버만 바꾸면 됩니다. 26.2 의 `SmithingTransformRecipe` 는
`template` 이 이미 **선택 항목**이고, `Ingredient.testOptionalIngredient` 가 `Optional.empty` 를
**「그 칸이 비어 있어야 통과」**로 해석합니다. 그래서 형판 줄을 뺀 레시피를 새 id 로 한 벌 더
얹으면 끝입니다.

- **바닐라를 덮어쓰지 않았습니다.** 덮으면 형판을 끼운 사람이 조용히 실패하고 형판 아이템이
  쓸모없어집니다. 지금까지 형판을 쓰던 방식이 그대로 됩니다
- **두 레시피는 동시에 맞을 수 없습니다.** 형판이 차 있으면 새 것이 탈락하고, 비어 있으면
  옛 것이 탈락합니다. 그래서 둘 다 살아 있어도 충돌하지 않습니다
- **12벌입니다.** 26.2 에는 창(`spear`)과 앵무조개 갑옷(`nautilus_armor`)이 있습니다.
  검·도끼·곡괭이·삽·괭이·창·투구·갑옷·바지·부츠·말 갑옷·앵무조개 갑옷
- 시험이 열두 벌을 실제 코덱으로 해석해 **형판 칸이 비어야만 걸리는지**와 **바닐라 원본은
  형판이 비면 탈락하는지**를 양방향으로 확인하고, `base`·`addition`·`result` 가 바닐라와
  글자 하나까지 같은지도 봅니다

### 화면 세 곳

**HUD** — 좌표·바이옴 아래에 구분선을 긋고 세트를 쌓습니다.

```
X 128  Y 64  Z -302
어두운 숲
────────────────
◆ 채굴 3/3
◇ 방어 1/2
```

- **켜진 것뿐 아니라 진행도까지 띄웁니다.** 「방어 1/2」를 알아야 다음 카드에서 무엇을
  집을지 정하는데, 그 판단은 선택창이 뜬 몇 초 안에 내려야 해 팀 화면을 열 겨를이 없습니다
- **가진 것이 0인 유형은 뺍니다.** 열한 개를 다 그리면 「기동 0/2」 같은 줄 여덟 개가 화면
  왼쪽 위를 덮습니다. 그 유형에 무엇이 있는지는 팀 화면에서 봅니다
- **뜻은 마름모가 지고 색은 거듭니다** — 켜짐 `◆`, 진행 중 `◇`. 색만으로 나타내면 색약자에게
  아무것도 전해지지 않습니다
- 켜진 것이 위로 오고, 그 안에서는 높은 단계가 먼저입니다. 여섯 줄이 넘으면 **가장 덜 모은
  유형부터** 사라집니다. 줄이 하나도 없으면 구분선도 긋지 않습니다. F3·F1 에서는 통째로
  사라집니다

**선택 카드** — 이름과 설명 사이에 유형 줄이 들어갑니다(`무기·화력`). 무유형이면 그 줄도,
위의 틈도 없습니다. 유형을 여럿 가진 증강은 서버가 한 줄로 이어 보냅니다 — 나누고 다시
잇는 규칙을 양쪽에 두지 않으려는 것입니다.

> **카드 높이 계산을 함께 고쳐야 했습니다.** 카드 안쪽은 `enableScissor` 로 잘려서,
> **덜 세면 아무 오류 없이 설명 마지막 줄이 소리 없이 사라집니다.** 카드가 짧아졌다는 것도
> 무언가 잘렸다는 것도 화면에 아무 표시가 없습니다. 그래서 계산을 공용 소스셋의
> `PerkCardMetrics` 로 빼내고 「줄이 늘면 높이도 는다」를 시험으로 못 박았습니다 —
> 선택창은 클라이언트 소스셋이라 시험이 보지 못합니다.

**팀 화면** — 「증강」 탭 머리글과 목록 사이에 세트를 넣고, **세트 줄에 마우스를 올리면
그 유형에서 아직 안 가진 증강을 이름과 등급색으로** 보여 줍니다.

- **여섯 줄까지만 보이고 나머지는 「… 외 N개」로 접습니다.** 채굴은 유형 증강이 열 개라
  툴팁이 화면 절반을 덮고, GUI 배율이 큰 화면에서는 아래가 잘려 **무엇이 잘렸는지조차
  안 보입니다.** 말없이 자르면 「이게 전부구나」로 읽히므로 접은 줄은 반드시 붙입니다
- **화면 갱신 판정에 세트 상태를 넣었습니다.** 팀 화면은 만들어 둔 문자열이 바뀔 때만
  자리를 다시 잡는데, 「채굴 2/3」 → 「채굴 3/3」은 줄 수가 그대로라 줄 수만 보면 갱신이
  되지 않습니다. 반대로 툴팁 내용은 일부러 뺐습니다 — 위젯 구성을 바꾸지 않으면서 백여 줄
  문자열을 매 틱 잇게 되기 때문이고, 툴팁은 어차피 매 프레임 새로 만듭니다
- 증강 목록의 시작 y 를 한 곳에서만 계산합니다. 그리는 쪽에서 직접 더하면 스크롤이 옛
  자리를 기준으로 삼아 **맨 아래 증강이 창 밖에 남아 영영 안 보입니다**

### 세트가 조용히 아무 일도 안 하는 함정

`PerkEffectType` 을 `switch` 로 소비하는 곳이 한 군데도 없습니다. **새 효과 타입을 넣어도
컴파일이 절대 안 깨집니다.** 마찬가지로 보유 증강의 효과를 훑는 자리에 세트 순회를 잇지
않으면 **그 자리가 소비하는 효과 형은 세트에서 아무 일도 하지 않습니다** — 빌드도 통과하고
로그도 없습니다.

그래서 효과를 훑는 자리 **29곳**에 한 줄씩 이어 붙였습니다. 특히 놓치기 쉬웠던 셋을 적어
둡니다.

- **상태이상을 모으는 자리** — 여기서 세지 않으면 세트가 거는 상시 상태이상이 팀 공유 풀로
  새어 나가 **세트가 꺼진 뒤에도 되살아납니다**
- **최대 체력을 세는 자리** — 여기서 세지 않으면 `max_health_bonus` 를 붙여도 상한 덮어쓰기가
  정확히 상쇄해 아무 일도 일어나지 않습니다
- **팀 피격 트리거** — 여기서 세지 않으면 세트의 `on_team_hurt` 가 무동작이 됩니다

**세트가 거는 속성 수정자의 이름을 증강과 갈라 두었습니다**(`set/<유형>/<단계>`). 같은
이름을 쓰면 세트가 꺼질 때 **증강이 걸어 둔 수정자를 지웁니다** — 증강이 조용히 무력해지고
원인을 찾기가 대단히 어렵습니다. 겹치지 않는지 보는 시험을 넣었습니다.

**붙였다 떼는 효과는 「빠진 단계만」 걷어냅니다.** 세트는 저장하지 않는 파생 상태라 「방금
전까지 무엇이 켜져 있었는가」를 아는 곳이 없어, 사람마다 마지막으로 붙여 준 단계를 기억해
둡니다. 「지금 안 켜진 단계는 전부 걷어낸다」로 하면 `on_swap` 처럼 걷어내기가 실제로
무언가를 지우는 효과가 있어 **포션으로 얻은 신속까지 사라집니다.**

### 새 효과 타입 12종

| 타입 | 무엇을 하나 | 만들면서 알게 된 것 |
|---|---|---|
| `supply_drop` | 주기마다 무작위 아이템 | 아래에 따로 적었습니다 |
| `experience_bonus` | 출처(광물·몹)별 경험치 배율 | 아래에 따로 적었습니다 |
| `mob_spawn_rate` | 적대 몹 자연 스폰 배율 | 아래에 따로 적었습니다 |
| `same_kind_mining` | 옆의 **같은 종류** 블록도 캐짐 | 아래에 따로 적었습니다 |
| `always_looting` | 처치 시 약탈 N | 아래에 따로 적었습니다 |
| `food_heal` | 먹을 때 팀 체력 회복 | 개인을 회복시키면 안 됩니다 — 개인 체력의 변화량을 관측해 공유 풀에 합산하는 구조라 **한 번 더** 들어갑니다. 「즉시 회복 I」로도 안 됩니다. 그쪽 회복량은 `4 × 2^증폭` 이라 **정확히 2를 만들 수 없습니다** |
| `hunger_on_damage` | 피해를 받으면 팀 허기가 참 | 허기는 정수 칸이라 0.4씩은 그대로 넣을 수 없습니다. 한 칸이 찰 때까지 소수를 들고 있다가 넘길 때만 올립니다. **그 나머지를 사람이 아니라 팀 단위로 들고 있습니다** — 사람 단위면 인원수만큼 찹니다. 세는 것은 방어구·저항·흡수를 모두 지난 **실제 피해**입니다 |
| `damage_taken_blocking` | 방패로 막는 동안 받는 피해 배율 | 기존 `damage_taken_from` 은 피해원만 보고 자세를 모릅니다. 그렇다고 `damage_taken` 을 넓히면 **자세를 모르는 옛 경로가 「막는 중 전용」 배율을 언제나 곱합니다**. 새 Mixin 없이 기존 피해 계산에 곱을 하나 더했고, 능력치 화면에는 안 적습니다 — 방패를 내린 순간 거짓말이 됩니다 |
| `lifesteal_efficiency` | 흡혈량 배율 | 흡혈 계산이 덧셈만 하고 있어 곱셈 단계를 새로 넣었습니다. 흡혈 자체가 0이면 효율만 있어도 0입니다 |
| `extra_rerolls` | 다시 뽑기 횟수 | 위의 「저장하는 것 하나」 |
| `prism_reroll` | 다시 뽑으면 프리즘만 | 아래에 따로 적었습니다 |
| `no_defense_drawbacks` | 방어 증강의 대가 면제 | 위의 「대가 무효화」 |

「효과를 붙인다」가 아니라 「규칙을 바꾼다」인 보상 둘(`prism_reroll`·`no_defense_drawbacks`)은
값이 없는 표시일 뿐이라 붙였다 뗄 것이 없습니다. 그런데도 **보유 증강 쪽까지 함께 훑습니다** —
나중에 이것을 증강에 붙였을 때 아무 표시 없이 무동작이 되지 않게 하려는 것입니다.

**「어차피 프리즘」이 카드를 세 장 못 채울 수 있습니다.** 등급이 모자랄 때 아래 등급으로
내려가는 것이 원래 규칙인데, 「프리즘 등급만 나옵니다」라고 적어 놓고 골드를 보여 주면
설명이 거짓이 됩니다. **모자라면 「적게 준다」를 골랐습니다** — 남은 프리즘이 두 장이면
카드도 두 장입니다. 일반 추첨도 후보가 모자라면 가능한 만큼만 줍니다. 한 장도 못 채우면
다시 뽑기가 실패하고 **횟수도 깎이지 않습니다.** 처음 뜨는 후보는 그대로 구간 확률표를
따릅니다 — 이 판정을 보는 곳은 다시 뽑기 한 곳뿐입니다.

**약탈은 세 갈래를 서로 다르게 셉니다.** `loot_bonus`(「수확자」)끼리는 더하고,
`always_looting`(사냥 세트)끼리는 **가장 높은 하나만** 남기고, 그 둘 사이는 **더합니다.**
사냥 2·3단계가 함께 켜지면 약탈 III 이지 IV 가 아닙니다 — 3단계 설명이 「약탈이 III 으로
오릅니다」인 이상 더하면 화면과 실제가 어긋납니다. **그래서 보유 증강 몫과 세트 몫을 한
목록으로 합쳐 한 번만 훑어야 합니다.** 따로 계산해 더하면 「가장 높은 하나만」이 각각 따로
뽑혀 I + III = IV 가 됩니다.

**「곡괭이가 알아서」와 「메아리 채굴」은 서로를 억누르지 않습니다.** 둘 다 가지면 둘 다
발동해 최대 네 칸이 함께 캐지고 내구도도 각각 닳습니다. 골드 증강으로 산 것이 채굴 증강
넷을 모았다는 이유로 사라지면 설명할 방법이 없습니다. 같은 칸을 두 번 캐지는 않습니다 —
메아리가 먼저 지우고, 그 뒤 후보를 새로 훑을 때 그 자리는 이미 공기입니다.

- **「같은 종류」는 블록 종류만 봅니다.** 상태까지 맞추라고 하면 **레드스톤 광석의 `lit`**
  때문에 광맥 한복판에서 효과가 조용히 사라집니다 — 밟거나 치기만 해도 켜집니다. 원목의
  축, 계단의 방향도 마찬가지입니다. **딥슬레이트 변종은 다른 종류입니다** — 묶으려면
  동치표를 손으로 유지해야 하고 그 표는 광석 여덟에서 끝나지 않습니다(돌·화강암, 구리
  산화 네 단계, 다른 모드의 블록). 「표가 없는 규칙 하나」를 골랐습니다
- 훑는 범위와 **팀원 발밑 보호**·미로드 청크·도구 등급은 「메아리 채굴」의 규칙을 그대로
  씁니다. 두 효과는 「종류를 보는가」 하나로만 갈립니다
- **연쇄를 막는 장치를 하나 더 넣었습니다.** 같은 종류일수록 잘 걸리는 효과라 그대로 두면
  광맥 하나가 아니라 **돌밭 전체가 도미노로 무너져 서버가 멈춥니다.** 스레드마다 따로 두는
  표시로 막습니다 — 전역 표시로 하면 다른 스레드가 켜 놓은 것 때문에 서버 스레드의 증강이
  통째로 사라지는 조용한 사고가 납니다

### 보급은 바닥에 떨어지지 않습니다

아이템 엔티티를 만들지 않고 **팀 공유 인벤토리로 곧장 밀어 넣습니다.** 꽉 차 있으면 대기열에
남았다가 칸이 비는 대로 들어가므로 **분실이 없습니다.** 남은 것이 있으면 채팅으로 알립니다.

- **인원수만큼 곱하지 않습니다.** 접속하면 개인 인벤토리가 팀 공유 목록으로 통째로 갈리는
  구조라, **한 벌이 곧 전원의 인벤토리**입니다. 인원수만큼 넣으면 그대로 배수로 불어납니다
- **주기는 오버월드 게임 시간으로 잽니다.** 서버 켠 뒤 지난 시간으로 재면 **껐다 켜는 것만으로
  보급을 앞당길 수 있습니다.** 게임 시간은 월드와 함께 저장되어 이어집니다. 「다음 보급 시각」
  자체는 저장하지 않습니다 — 순수 계산으로 다시 나오므로 저장 형식을 건드릴 이유가 없습니다
- **재시작 직후 한 회는 건너뜁니다.** 그 팀을 처음 본 틱에는 기준만 잡습니다. 안 그러면
  재시작할 때마다 한 벌씩 더 받습니다. 4단계가 켜져 주기가 10분에서 5분으로 바뀔 때도
  같습니다 — 주기 번호의 뜻 자체가 달라지기 때문입니다
- **아무도 접속해 있지 않으면 그 회는 건너뜁니다.** 놓친 회차는 쌓이지 않습니다. 빈 서버가
  밤새 돌아 대기열이 수십 벌 부풀면 다음 접속이 정리 작업으로 시작됩니다
- **단계가 여럿 켜져도 보급은 한 번만 옵니다.** 단계는 누적이라 보급을 4개 모으면
  `supply_drop` 이 셋 나오고, 그대로 두면 한 주기에 세 번 옵니다. 우선순위가 높은 하나만
  고릅니다. **그래서 상위 단계 정의는 하위 내용을 전부 흡수한 완결형이어야 합니다**
- **꽝은 뽑기표와 따로 굴립니다.** 표의 가중치를 손봐도 꽝 확률이 흔들리지 않습니다
- `periodic` 안에 `item_grant` 를 넣는 조합으로는 **절대** 만들 수 없습니다. 파싱도 통과하고
  로그도 없는데 영원히 아무 일도 안 일어납니다. 그래서 타입을 따로 만들었습니다

### 경험치를 출처별로 가르려고 Mixin 을 둘 더 넣었습니다

26.2 에서 경험치 오브가 생기는 길은 한 곳으로 모입니다. 전역 배율(0.19.0-dev 의 1.2배)이
거기 한 자리로 끝난 이유입니다. **그런데 그 자리에는 출처도 사람도 남아 있지 않습니다** —
인자가 월드·좌표·양뿐입니다. 그래서 **「누가·무엇에서」를 아직 알고 있는 위쪽 두 자리**를
각각 잡았습니다.

- **몹** — 처치 보상을 계산하는 그 한 번의 호출만 골라 잡았습니다. **한 단계 위에서 잡으면
  안 됩니다** — 26.2 의 스컬크 촉매가 같은 값으로 번식량을 정해서, **사냥 세트를 낀 팀 근처에서만
  스컬크가 더 번지는, 아무도 설명해 주지 않는 부작용**이 생깁니다
- **광물** — 26.2 에는 `getExpDrop` 이 없고 그 자리를 다른 메서드가 대신합니다. 구버전
  글을 그대로 따라 하면 없는 메서드를 잡게 됩니다. 블록이 경험치를 뿜는 자리에 걸었고,
  **거기 닿는 경험치는 전부 블록에서 나온 것**이라 화로·낚시 쪽으로 번질 수 없습니다.
  그 위에 좌표까지 일치해야 곱하도록 한 겹 더 두었습니다
- **전역 배율과는 대체가 아니라 곱셈입니다.** 설정 1.2배 × 세트 1.5배 = 1.8배입니다.
  자리가 둘이라 반올림이 두 번 일어나 **한 번에 곱한 값과 1점까지 다를 수 있습니다.**
  정수 경험치에서 1점 차는 보이지 않고, 대신 전역 배율 지점을 하나로 유지할 수 있어
  이쪽을 골랐습니다
- **화로·낚시·번식·주민 거래는 어느 쪽도 아닙니다.** 전역 배율만 받습니다
- 「광물」의 정의는 새로 적지 않고 「욕심 많은 곡괭이」의 목록을 그대로 가리킵니다. 여기
  다시 적으면 한쪽만 고쳤을 때 **곡괭이는 터지는데 경험치는 안 붙는** 상태가 조용히 생깁니다
- `source` 를 안 적거나 모르는 값이면 정의를 **버립니다.** 오타 하나가 「광물만」이어야 할
  보상을 모든 경험치에 거는 사고가 되기 때문입니다

### 몹 스폰율

적대 몹의 자연 스폰이 도는 자리에 걸어 **시도 횟수만** 늘립니다.

- **몹 총량 천장은 바닐라 그대로입니다.** 상한 검사를 이미 지난 뒤라, 상한에 닿으면 배율이
  아무 일도 안 하는 것이 정상입니다
- **주민·소 같은 생물과 수중 몹은 건드리지 않습니다.** 갈래를 첫 인자로 받아 한 번에 거릅니다
- **1.35회를 돌 수는 없으므로** 정수부만큼 돌고 소수부는 확률로 한 번 더 돌립니다. 기댓값이
  정확히 배율입니다. 추가 패스는 같은 자리를 다시 부르는 것이라 그대로 두면 무한히 겹칩니다 —
  깃발로 두 번째부터 즉시 빠져나옵니다
- **팀 단위가 아니라 월드 전역입니다.** 몹은 어느 팀에도 속하지 않습니다. 한 팀 안에서는
  곱하고, 팀이 여럿이면 1.0 에서 가장 먼 하나만 씁니다 — 체력·공격력과 같은 규칙입니다
- **`mobSpawnRatePerks` 설정으로 끌 수 있습니다**(기본 켜짐). 이 효과만은 몹 하나에 수정자를
  붙이는 것이 아니라 **매 틱 도는 스폰 경로를 배율만큼 더 돌립니다.** 사양이 낮은 서버에서
  틱이 밀리면 다른 것을 건드리지 않고 이 한 줄로 끌 수 있어야 합니다

### 증강 값 조정 넷

| 증강 | 전 | 후 | 왜 |
|---|---|---|---|
| **철벽**(골드) | 방어력 +8 · 넉백 면역 / **이속 −25%** · 몹 체력 ×1.5 | 방어력 +8 · 넉백 면역 / 몹 체력 ×1.5 | 방어 3단계가 지워 줄 대가 목록에 있던 것입니다. 표시를 붙이는 대신 아예 없앴습니다 |
| **해시계**(골드) | **20칸** / **채굴 속도 ×0.65** · 받는 피해 ×1.15 | **32칸** / 받는 피해 ×1.15 | 채굴 유형에 든 증강이 채굴을 느리게 하는 것은 세트가 가리키는 방향과 정면으로 어긋납니다. 대가를 없앤 만큼 찾는 범위도 넓혔습니다 |
| **벼랑 끝**(프리즘) | 체력 **80%** 이하 힘 II | 체력 **75%** 이하 힘 II·**신속 I·저항 I** | 발동선을 내리고 대신 켜졌을 때 확실히 세집니다. 화력 유형의 프리즘 한 장이 이것뿐입니다 |
| **도박꾼** → **「요행」**(프리즘) | — | 이름만 바뀜 | 유형 「도박」과 글자가 겹쳐 「도박꾼을 가지면 도박 세트」로 읽힙니다. 효과와 id(`sharedfate:gambler`)는 그대로입니다 |

### 눈으로 확인해야 하는 것

**이번 판은 실제 플레이로 본 것이 하나도 없습니다.** 값과 규칙은 시험 1613개가 받치고 있지만,
화면과 손맛은 시험이 보지 못합니다. 처음 켤 때 아래를 순서대로 보시는 것을 권합니다.

1. **선택 카드의 유형 줄이 설명을 밀어내지 않는지** — 잘리면 아무 표시 없이 마지막 줄이
   사라집니다. 유형을 둘 가진 「원정 준비물」·「피의 대가」가 가장 위험합니다
2. **HUD 가 좌표를 가리지 않는지**, 여섯 줄이 넘칠 때 모양
3. **팀 화면 툴팁** — 세트 줄 위에서만 뜨는지, 「… 외 N개」가 붙는지, 창 밖으로 나가지 않는지
4. **보급이 실제로 들어오는지** — 10분을 기다려야 합니다. 인벤토리가 꽉 찼을 때의 안내도
5. **방어 3단계** — 「맨살의 각오」를 낀 채로 세트를 켜고 껐을 때 채굴 속도가 오갔다 돌아오는지.
   속성 수정자를 걷어내는 자리가 가장 미덥지 못합니다
6. **형판 없는 네더라이트** — 형판을 낀 옛 방식도 여전히 되는지 함께
7. **환골탈태로 갈아엎은 직후** 세트가 즉시 다시 계산되는지

### 값을 고치는 자리

- **세트 보상은 전부 `sharedfate-sets-default.json` 한 파일에 있습니다.** 효과의 형식이
  증강 정의와 완전히 같아서, 이미 있는 효과 타입이면 코드를 한 줄도 안 고치고 값만 바꾸면
  됩니다. 서버에서는 `config/sharedfate-sets.json` 을 고치고 다시 켜면 회차 도중에도
  들어갑니다
- **임계값은 `PerkSetType.threshold()` 한 곳입니다.** 판정하는 쪽도 화면도 숫자를 직접 적지
  않고 이 메서드를 부릅니다. 시뮬레이션으로 정한 값이라 앞으로도 자주 바뀝니다
- **어느 증강이 어느 유형인가는 증강 정의의 `set_types` 입니다.** 모르는 유형 이름은 그것만
  건너뛰고 경고를 남깁니다. 유형을 안 적으면 무유형이고, 그것이 정상이라 경고하지 않습니다
- **효과가 비어 있는 단계는 「아직 안 만들어진 자리」입니다.** 무기 여섯이 그렇습니다.
  이름과 설명만 정해 두었으니 값만 넣으면 켜집니다. 항목을 지우지 마십시오

> **같은 유형의 단계들은 서로를 덮어쓰지 않고 쌓입니다.** 값을 고칠 때 가장 헷갈리는
> 자리입니다. 화력 4단계의 설명은 「흡혈이 15% 로 오른다」이지만 정의 파일에 적힌 값은
> **`0.10` 입니다** — 3단계의 `0.05` 위에 얹어 0.15 가 됩니다. `0.15` 를 적으면 20% 가 됩니다.
> 반대로 보급은 우선순위가 높은 한 단계만 도므로 **상위 단계가 하위 내용을 전부 들고 있어야**
> 합니다. 같은 파일 안에서 규칙이 정반대인 두 자리입니다.

## 0.19.0-dev — 2026-09-02

> **통신 규약이 18 → 19 로 올랐습니다.** 0.18.0-dev 이하 클라이언트는 악수 단계에서 막힙니다.
> 서버와 모든 클라이언트를 함께 갱신하십시오. 어차피 좌표·바이옴 표시가 클라이언트 코드라
> 새 판이 필요했으므로, 규약 인상으로 늘어난 비용은 없습니다.

증강 57개의 값을 조정하고, 새 증강 4개와 새 효과 타입 7종을 넣었습니다. 풀이 **78 → 82개**
(실버 31 · 골드 32 · 프리즘 19), 효과 타입이 **52 → 59종**, 시험이 **1156 → 1292개**입니다.

### 프리즘이 정해진 자리에서만 나오지 않습니다

예전에는 15·30 두 구간이 **언제나** 프리즘이었고 나머지 여섯은 실버/골드 반반이었습니다.
회차마다 프리즘이 정확히 두 번, 그것도 같은 자리에서 나왔습니다.

이제 **15 구간만 고정**이고, 나머지 일곱 구간은 확률표를 따릅니다.

| 이번 회차에 추가로 뜬 프리즘 | 실버 | 골드 | 프리즘 |
|---|---|---|---|
| 0개 | 45% | 45% | 10% |
| 1개 | 46% | 51% | 3% |
| 2개 | 50% | 50% | 0% |

프리즘이 한 번 뜨면 다음 확률이 크게 줄어 **회차당 최대 3번(고정 1 + 추가 2)** 에서 멈춥니다.
기대값은 추가 0.57회 — 「웬만하면 두 번, 운 좋으면 세 번」입니다.

계산 순서는 **한도 접기 → 프리즘 보너스 → 실버 차단**입니다. 보너스가 한도를 뚫지 못하게
하려는 것입니다.

### 실버 「원정 준비물」이 실버를 막습니다

이 증강을 고르면 **그 뒤로 실버 후보가 한 장도 나오지 않고**, 그 몫이 전부 골드로 갑니다.
덤으로 프리즘 확률이 3%p 오릅니다. 실버 카드 한 장으로 남은 회차를 통째로 거는 셈입니다.

### 새 증강 넷

- **버티는 방패**(실버) — 방패를 들어 막는 동안 낙하 피해를 받지 않습니다. 대신 몹 체력 ×1.15
- **비술 공방**(골드) — 인챈트 탁자와 서고 30개를 얻고, 인챈트 값이 다이아몬드 1개가 됩니다.
  대신 몹 체력 ×1.2. **규약을 19로 올린 것이 이 증강 때문입니다** — 팀마다 값이 달라져서
  클라이언트가 그 숫자를 알아야 툴팁과 단추가 맞고, 그러려면 인챈트 화면의 데이터 칸이
  10개에서 11개로 늘어야 했습니다.
- **해시계**(골드) — 시계를 얻습니다. 우클릭하면 20칸 안의 다이아몬드 광석 자리에 파티클이
  뜹니다(20초 쿨). 대신 채굴 속도 ×0.65, 받는 피해 ×1.15. 없애 주는 것이 **캐는 시간이 아니라
  찾는 시간**이라 대가를 캐는 쪽에 물렸습니다.
  - 청크 섹션의 팔레트를 먼저 훑어 다이아몬드가 없는 4096칸 덩어리를 통째로 버립니다.
    근처에 없으면 블록을 **한 칸도 읽지 않습니다.**
  - **돌에 완전히 묻힌 광석은 파티클이 보이지 않습니다.** 파티클은 지형에 가려지고, 26.2 에
    블록을 발광시키는 방법이 없습니다. 굴·갱도로 노출된 것만 보이고 나머지는 액션바의
    개수로만 압니다.
  - 파티클은 `end_rod` 입니다. `SimpleAnimatedParticle` 이라 밝기가 최대로 고정되어 칠흑
    같은 굴에서도 보입니다. `happy_villager` 는 그 처리가 없어 어두운 곳에서 안 보입니다.
- **종결곡**(프리즘) — 효율 VI · 내구성 X · 행운 III 네더라이트 곡괭이. 대신 몹 이동 속도 ×1.15.
  바닐라 최대 레벨은 인챈트 탁자와 모루가 지키는 약속일 뿐이고, 컴포넌트 자체는 1~255 를
  받습니다(`ItemEnchantments.LEVEL_CODEC`).

### 화면 왼쪽 위에 좌표와 바이옴이 뜹니다

`X 128  Y 64  Z -302` 와 한글 바이옴 이름. **클라이언트가 이미 아는 값이라 서버 통신이 없습니다.**
바이옴 66개의 한글 이름은 26.2 공식 한국어 번역에서 그대로 옮겼습니다 — `Component.translatable`
을 쓰면 게임을 영어로 켠 사람에게 영어가 나오기 때문입니다. F3·F1 에서는 사라집니다.

### 난이도 상승이 이제 말을 합니다

30분마다 몹이 세질 때 **빨간 글씨**로 알립니다: `몹이 강해졌습니다 — 체력과 공격력 +8% (2단계)`.
상한(25단계·+100%)에 닿으면 그 사실을 한 번만 알리고 조용해집니다. 30분에 한 번이라 채팅을
골랐습니다 — 액션바는 곧 덮여서 「몰랐다」가 나옵니다.

### 경험치가 1.2배가 됩니다

몹·광석·화로·낚시 어느 쪽이든 떨어지는 경험치가 1.2배입니다. `SharedFateConfig` 의
`experienceMultiplier` 로 조정할 수 있습니다. `/xp` 명령은 오브를 만들지 않아 해당하지 않습니다.

### 증강 값 조정 57건

전부 적기에는 길어서 갈래만 적습니다.

- **대가가 사라진 것 여덟** — 작아진 몸 · 길잡이 나침반 · 공명 · 숨은 재능 · 요새 탐지기 ·
  거인 · 달 위를 걷다 · 하늘의 은총. 특히 「숨은 재능」·「하늘의 은총」은 **덤으로 한 장 더
  받는 것 자체가 이미 그 구간의 카드 한 장을 쓴 것**이라, 대가까지 무는 것은 이중 지불이었습니다
- **성격이 바뀐 것** — 「광맥 감각」은 캘 때마다 발동하던 것을 상시 성급함으로, 「운수 좋은 날」은
  행운 속성에서 **광물 드롭 0~3개 + 액션바 알림**으로, 「바람 빠진 방패」는 폭발 넉백에서
  **폭발 피해 −50%** 로, 「곡괭이 전사」는 다른 무기의 피해를 1로 만들던 것에서
  **곡괭이가 아닌 도구를 들면 느려지는** 것으로 바뀌었습니다
- **「제왕과 신하」는 이제 고른 사람이 계속 제왕입니다.** 그 사람이 나가면 아무도 제왕이
  아닌 채로 팀만 대가를 뭅니다 — 의도한 동작입니다
- **「메아리 채굴」이 두 개를 더 캐고, 팀원 발밑은 절대 캐지 않습니다.** 밟은 칸이 사라지면
  떨어져 죽고 체력이 공유라 팀 전체가 죽는데, 그 피해자는 대개 캔 사람이 아니었습니다
- **「흡혈귀」에 20초마다 다시 차는 흡수 III 이 붙었습니다.** 주기 끝의 1틱 동안 효과가
  떨어졌다 다시 붙으면서 보호막이 가득 찹니다
- **「환골탈태」의 30렙 제한을 없앴습니다.** 이제 프리즘이 뜨는 어느 구간에서나 나옵니다

### 그 밖

- 「나무꾼의 욕심」이 나무 30개만 쓰고 허기·독을 걸지 않습니다(60개 + 허기 V·독 I 이었습니다)
- 「공명」의 16칸 제한과 「혼자 있으면 느려짐」 대가가 사라지고 성급함이 III 이 됐습니다
- `DoubleJumpEffect.DEFAULT_POWER` 를 0.714 → 0.63 으로 맞췄습니다. 「허공답보」가 1.5배로
  바뀌었는데 생략했을 때의 기본값만 1.7배로 남으면 다음에 헷갈립니다

## 0.18.0-dev — 2026-09-02

> 통신 규약은 **18 그대로**입니다. 고행자 고침은 **서버 쪽 코드**라 서버 jar 를 바꿔야 하지만,
> 클라이언트는 그대로 두어도 됩니다.

### 「고행자」를 받으면 팀이 그 자리에서 전멸했습니다

프리즘 「고행자」는 최대 체력을 10 으로 고정합니다. 그런데 **그 순간 팀이 즉사했습니다.**
체력이 많을수록 확실하게 죽었습니다 — 3인 팀은 체력 15 이상, 2인 팀은 만피에서 그랬습니다.

상한이 20 → 10 이 되면 `MaxHealthAttribute` 가 **팀원 각자의** 체력을 10 으로 자릅니다.
`StatMirror` 는 팀원별 체력 감소를 **사람 수만큼 합산**하는데(팀원 둘이 각각 맞았으면 팀은
진짜로 두 번 맞은 것이므로 맞는 규칙입니다), **상한이 줄어 잘린 몫까지 함께 세고 있었습니다.**
한 번의 자름이 인원수만큼 곱해진 것입니다.

```
3인 · 체력 18 → 8 이 세 번 빠져 18 − 24 = 0  → 전멸
```

이제 `StatMirror.healthDelta` 가 **상한이 줄어 잘린 몫을 빼고** 관측합니다. 결과는
「가득 찬 10」입니다. 같은 틱에 진짜로 맞은 몫은 그대로 남습니다 — 상한이 반토막 난 틱에
좀비에게 3 을 맞았다면 공유 풀에서 3 이 빠집니다.

- **흡수 보호막은 원래부터 이렇게 되어 있었습니다.** `consumedAbsorption` 이 효과가 만료돼
  최대치가 사라진 몫을 피해에서 뺍니다. 체력 쪽에만 그 처리가 없었습니다.
- 피해 기록(`DamageLedger`)에도 같은 몫이 들어가고 있었습니다. 「누가 제일 많이 맞았나」가
  상한이 줄어든 회차에서 부풀어 있었을 수 있습니다. 함께 고쳤습니다.
- `/shareteam health` 로 최대 체력을 낮출 때도 같은 일이 일어났습니다. 원인이 한 곳이라
  그쪽도 함께 고쳐집니다.

`PerkHealthRules` 주석은 "상한만 옮기면 관측과 자르기가 같은 결론에 이른다"고 적고
있었는데, 그 계산은 **혼자일 때만** 맞았습니다. 주석도 함께 고쳤습니다.

### 굴착기가 실제로는 아무 이득도 주지 않고 있었습니다

> 이 고침은 **증강 정의 파일만 바뀐 것**이라, 서버의 `config/sharedfate-perks.json` 을 고치고
> 재시작하면 **회차 도중에도** 적용됩니다. 속성 수정자는 transient 라 저장되지 않고 접속할
> 때마다 다시 붙습니다(`PerkManager.refreshPlayer`).

「모든 블록 채굴 ×3」이 **한 번도 작동한 적이 없습니다.** 광석 페널티만 걸려서, 가지면
손해만 보는 증강이었습니다.

`mining_speed` 효과는 **서버에서만** 계산됩니다. 그런데 26.2 에서 블록이 언제 부서지는지를
정하는 것은 **클라이언트가 보내는 `STOP_DESTROY_BLOCK`** 입니다 —
`ServerPlayerGameMode.tick` 은 진행도를 세어 금 가는 모습만 보내고 그 결과를 버립니다
(바이트코드에서 `pop`). 서버가 스스로 부수는 자리는 시작 시점의 즉시 파괴, 클라이언트의
`STOP` 을 받았을 때, `hasDelayedDestroy` 셋뿐입니다.

그래서 방향에 따라 결과가 갈립니다.

| 배율 | 결과 |
|---|---|
| **1 미만(느려짐)** | 작동합니다. 클라가 먼저 끝냈다고 해도 서버가 `hasDelayedDestroy` 로 붙잡습니다 |
| **1 초과(빨라짐)** | **아무 일도 하지 않습니다.** 서버가 빨라도 클라가 말하기 전엔 안 부서집니다 |

굴착기가 `mining_speed` 배율이 1보다 큰 **유일한** 증강이라 지금까지 드러나지 않았습니다.

**고친 방법은 바닐라 속성 `minecraft:block_break_speed` 입니다.** 이 속성은
`setSyncable(true)` 라 클라이언트까지 자동으로 내려가고, `Player.getDestroySpeed` 안에서
바닐라가 직접 곱합니다. 즉 양쪽이 같은 값을 봅니다. 같은 방식을 쓰는 「익숙한 손목」과
「맨살의 각오」는 원래부터 잘 작동하고 있었습니다.

| 증강 | 전 | 후 |
|---|---|---|
| **굴착기**(골드) | 모든 블록 ×3 *(무효)* / 광석 ×0.5 | **모든 블록 ×3** / **공격력 −15%** |

광석 예외는 뺐습니다. 속성으로 빨라진 만큼 광석에서 클라·서버가 더 크게 어긋나
금이 찼다 되돌아가는 것이 심해지기 때문입니다. 대가는 공격력으로 옮겼습니다.

- **`mining_speed` 에 1보다 큰 배율을 적으면 이제 경고가 남습니다.** 정의는 살려 두되
  서버 로그에 이유와 대안을 적습니다. 기본 풀에 그런 값이 없는지 보는 시험도 넣었습니다.
- `PlayerMiningSpeedMixin` 과 `MiningSpeedEffect` 의 주석에 근거를 적어 뒀습니다.

## 0.17.0-dev — 2026-09-01

> 통신 규약은 **18 그대로**입니다. 패킷 형식도 저장 형식도 바뀌지 않았습니다.

### 다시 뽑으면 방금 본 카드가 돌아오지 않습니다

「다시 뽑기」는 보유 증강만 빼고 새로 뽑았습니다. 그래서 **방금 본 3장이 그대로 다시
나올 수 있었고**, 남은 후보가 28개인 실버 라운드라면 **세 번에 한 번꼴(약 30%)** 로 최소 한
장이 돌아왔습니다. 다시 뽑기를 쓰고도 안 쓴 것처럼 보이는 상황입니다.

이제 **직전에 보여 준 3장을 후보에서 뺀 뒤** 뽑습니다. 실버·골드는 풀이 30개씩이라 사실상
언제나 완전히 새로운 3장이 나옵니다.

- **등급이 회피보다 우선입니다.** 남은 후보가 회피 대상뿐이라면 등급을 내려가는 대신
  회피 대상을 그대로 씁니다. 「실버 라운드에서 다시 뽑았더니 골드가 나왔다」가 되는 것이
  겹쳐 보이는 것보다 훨씬 나쁩니다.
- **카드가 3장 미만으로 뜨는 일은 없습니다.** 빼고 나서 모자라면 뺐던 것에서 마저 채웁니다.
- **직전 한 번만** 피합니다. 두 번 넘게 다시 뽑으면 그보다 앞서 본 후보는 돌아올 수 있습니다.
- 구간 간 중복은 그대로입니다 — 안 고른 증강은 다음 구간에 다시 후보가 됩니다. 「아까 안
  고른 그거, 이번엔 가져가자」가 가능해야 선택에 의미가 생기기 때문입니다.

`PerkDraft.draw` 에 회피 목록을 받는 오버로드가 하나 늘었습니다. 보유 목록(`owned`, **절대**
안 나옴)과 회피 목록(`avoid`, **되도록** 안 나옴)은 성격이 다릅니다.

### 휩쓸기의 대가가 절반이 됐습니다

| 증강 | 전 | 후 |
|---|---|---|
| **휩쓸기**(실버) | 휩쓸기 피해 비율 +1.0 / 공격력 −20% | 휩쓸기 피해 비율 +1.0 / **공격력 −10%** |

## 0.16.0-dev — 2026-09-01

> 통신 규약은 **18 그대로**입니다. 패킷 형식이 하나도 바뀌지 않았습니다.

### 팀을 만들어도 창이 닫히지 않습니다

`/shareteam` 화면에서 「팀 만들기」를 누르면 창이 사라졌습니다. 만든 직후가 곧 「게임 시작」을
누를 자리인데 창을 다시 열어야 했습니다. 이제 **창이 그대로 열려 있고**, 서버에서 팀이
생겼다는 소식이 도착하는 순간 만들기 양식이 있던 자리에 **팀원 목록과 「게임 시작」 단추**가
그려집니다. 「팀」 탭에 그대로 머물기 때문에 눈이 옮겨 갈 곳도 없습니다.

- **이름이 겹쳐 실패해도 창은 열려 있습니다.** 서버가 채팅으로 사유를 알려 주고, **적던
  이름은 지워지지 않습니다.** 한 글자만 고쳐 다시 누르면 됩니다.
- 결과를 기다리는 사이에 다른 탭을 눌러 두었다면 팀이 생길 때 「팀」 탭으로 되돌립니다.

닫히던 까닭은 팀에 들어갈 때 서버가 부르던 `closeContainer()` 였습니다. 그 닫기 패킷을 받은
클라이언트는 **지금 떠 있는 화면이 무엇이든** 없앱니다. 상자·화로를 연 채로 인벤토리가
바뀌는 것을 막으려던 장치가 컨테이너와 아무 상관 없는 팀 화면까지 함께 닫고 있었습니다.
이제 팀을 **만들 때**는 따로 연 창이 있을 때만 닫습니다. 커서에 쥔 것과 2×2 조합칸을
정리하는 일은 그대로 합니다.

### 증강 둘의 대가가 바뀌었습니다

| 증강 | 전 | 후 |
|---|---|---|
| **숨은 재능**(실버) | 골드 증강 +1 / 최대 체력 −4 | 골드 증강 +1 / **받는 피해 ×1.15** |
| **하늘의 은총**(골드) | 프리즘 증강 +1 / 최대 체력 −8, 받는 피해 ×1.25 | 프리즘 증강 +1 / **받는 피해 ×1.25** |

최대 체력은 팀이 한 칸을 나눠 쓰는 값이라, 한 사람이 둘을 고르면 팀 전체가 −12 를 안고
회차 내내 되돌릴 방법이 없었습니다. 받는 피해 배율은 같은 팀 전체 부담이라도 **맞았을
때만** 드러나므로 조심하면 줄일 수 있습니다. 둘을 함께 가지면 배율이 곱해져 ×1.4375 입니다.

## 0.15.0-dev — 2026-09-01

> **통신 규약 17 → 18.** 서버와 클라이언트를 함께 올려야 합니다.

### 인벤토리(E) 왼쪽에 능력치가 늘 보입니다

예전에는 팀 화면을 열고 「능력치」 탭까지 들어가야 볼 수 있었습니다. 이제 인벤토리를 여는
순간 단추 아래에 여덟 줄이 세로로 늘어섭니다. 창을 닫지 않아도 무기를 바꾸면 숫자가
따라옵니다.

| 묶음 | 줄 |
|---|---|
| 내 능력치 | 최대 체력 · 공격력 · **공격 속도** · 방어력 · 이동 속도 · **받는 피해** |
| 이 판의 몹 | **몹 체력** · **몹 공격력** |

- **공격력과 공격 속도를 붙여 두었습니다.** 무기 하나가 둘을 함께 바꾸는데(검을 들면
  공격력이 오르고 공격 속도가 내려갑니다) 떨어져 있으면 그 거래가 안 보입니다.
- **받는 피해·몹 두 줄은 색이 반대입니다.** 오르면 빨강, 내리면 초록입니다. 몹이 두 배가 된
  것을 초록으로 알릴 수는 없습니다. 팀 화면에도 그렇게 적혀 있습니다.
- **공격 속도는 초당 공격 횟수 그대로 적습니다**(기본 4). 이동 속도의 0.1 과 달리 숫자
  자체가 이미 말이 되므로 백분율로 바꾸지 않았습니다.
- **몹 두 줄은 배율**입니다 — 아무것도 안 걸렸으면 `100% → 100%`. 증강 배율과 「난이도
  상승」 배율을 곱한 값이라, 난이도가 30분마다 오르면 이 줄도 따라 오릅니다.

#### 자리가 모자라면 차례로 물러납니다

인벤토리 창은 화면 한가운데에 있고 **왼쪽에 남는 자리는 GUI 배율과 화면 크기에 따라 몇 배씩
달라집니다.** 그래서 남은 폭에 들어가는 가장 자세한 모양을 고릅니다.

| 남는 자리 | 모양 | 예 |
|---|---|---|
| 172px 이상 | 온전한 이름 | `최대 체력  20 → 26  (+6)  (하트 13개)` |
| 138px 이상 | 이름을 줄임 | `체력  20 → 26  (+6)` |
| 94px 이상 | 증감 괄호를 뗌 | `체력 20 → 26` |
| 64px 이상 | 두 줄로 접음 | `체력` / `20 → 26` |
| 그 아래 | **감춤** | 단추만 남습니다 |

1920×1080 기준으로 배율 1·2·3 은 온전한 이름, 배율 4 는 줄인 이름이 들어갑니다.
감추는 것은 화면이 아주 좁은데 조합법 책까지 펼친 경우뿐이고, 그때는 팀 화면의 「능력치」
탭에서 온전한 모습으로 볼 수 있습니다. **어떤 배율에서도 창을 덮거나 화면 밖으로 나가지
않습니다.**

#### 팀 화면 「능력치」 탭은 남깁니다

인벤토리 쪽은 남는 자리만큼만 그리므로, 어떤 배율에서도 온전한 이름과 설명까지 볼 수 있는
자리가 하나는 있어야 합니다. 대신 **두 곳이 같은 코드로 줄을 만듭니다** — 형식이 갈라질
자리가 없습니다.

### 인벤토리의 단추 이름이 「SharedFate」 가 되었습니다

「팀」 한 글자로는 무엇이 열리는지 알 수 없었습니다. 그 단추가 여는 것은 팀만이 아니라
현황·팀·설정·증강·능력치를 담은 **이 모드의 창 하나**이고, 그 창의 제목이 이미
「SharedFate 팀」이라 단추와 창이 같은 것으로 읽힙니다. 「모드 설정」이라고 적지 않은 이유도
같습니다 — 그 창에서 설정은 다섯 탭 중 하나일 뿐입니다.

단추 폭은 못 박지 않고 **글자를 실제로 재서** 정합니다. 자리가 모자라면 남은 만큼만
넓어지고 글자는 바닐라가 잘라 보여 줍니다. 조합법 책을 펼쳐도 그 판에 깔리지 않습니다.

### 위치 교환의 기본값이 「켬 · 5분」이 되었습니다

`/shareteam create` 에 `swap` 을 적지 않으면 이제 **5분 주기로 켜진 팀**이 만들어집니다.
체력과 경험치만 나누면 팀원이 각자 제 갈 길을 가도 아무 일이 없습니다. 자리가 주기적으로
섞여야 서로가 어디서 무엇을 하는지 신경 쓰게 됩니다.

5분인 까닭은 **한 가지 일을 끝낼 만한 시간**이기 때문입니다. 1분이면 광질 한 번, 이동 한 번을
못 끝내고 끌려다니기만 하고, 10분이 넘으면 교환이 있다는 사실을 잊습니다. 팀 화면의 팀
만들기 탭도 이 값에서 시작합니다. 끄고 싶으면 `swap off` 를 적거나 단추를 「끔」까지 굴리면
됩니다.

### 팀 이름을 적기 전에는 「팀 만들기」가 눌리지 않습니다

예전에는 이름이 비어 있어도 단추가 눌렸고, **눌러도 아무 일이 일어나지 않았습니다.**
화면이 명령을 만들다 말고 조용히 돌아섰기 때문입니다. 눌리는데 아무 일도 없는 단추는
「모드가 고장 났다」로 읽힙니다.

이제 이름 칸이 비어 있으면 단추가 회색이고, **글자를 치는 즉시** 켜집니다. 공백만 넣은 것도
비어 있는 것으로 봅니다. 단추 아래에는 왜 못 누르는지가 한 줄로 적힙니다 — 툴팁이 아니라
늘 보이는 글자인 이유는, 마우스를 올려 보기 전에는 툴팁이 있는지조차 알 수 없기 때문입니다.

### 통신 규약 17 → 18

`AttackDamagePayload` 가 **`StatSnapshotPayload`** 로 바뀌었습니다. 공격력 하나만 싣던
패킷에 **받는 피해 배율·몹 최대 체력 배율·몹 공격력 배율** 셋이 더 실리면서 이름이 내용보다
좁아졌기 때문입니다. 셋 다 서버만 아는 값입니다 — 증강이 만드는 배율은 바닐라 속성이 아니고,
몹 배율은 사람이 아니라 몹에게 붙어 클라이언트에 흔적이 없습니다.

**공격 속도는 이 패킷에 없습니다.** `minecraft:attack_speed` 는 공격력과 달리
`setSyncable(true)` 로 등록되어 무기가 거는 수정자까지 그대로 클라이언트에 옵니다. 이미 있는
값을 또 보내면 두 값이 어긋날 자리만 생깁니다.

### 증강을 고른 뒤 무엇이 정해졌는지 볼 시간이 생겼습니다

**결과 화면이 3초에서 5초로 늘었습니다.** 예전에는 3초 안에 안 고른 카드가 내려가고, 고른
카드가 가운데로 옮겨 오고, 이름과 설명을 읽고, 「N초 뒤 다시 시작합니다」까지 봐야 했습니다.
앞머리 움직임을 빼면 읽을 시간이 2.5초도 남지 않았고, **고르지 않은 팀원에게는 그 순간이
증강 설명을 처음 보는 순간**입니다. 두세 줄짜리 설명을 읽는 데만 2초가 듭니다.

- **5초로 잡은 이유는 위아래가 모두 막혀 있어서입니다.** 4초면 카드가 자리를 잡는 0.75초를
  빼고 3초대라 여전히 빠듯하고, 6초부터는 연출이 아니라 대기가 됩니다. 한 회차에 여덟 번
  겪는 연출이라 5초여도 회차당 시간 정지가 16초 늘어납니다.
- 카운트다운은 **5 → 4 → 3 → 2 → 1** 로 떨어집니다. 화면에 숫자를 박아 두지 않고 서버가
  보낸 길이를 그대로 세므로, 서버 값과 화면이 어긋날 수 없습니다.

**고른 카드가 가운데로 미끄러져 옵니다.** 예전에는 한 프레임 만에 순간이동했습니다. 왼쪽이나
오른쪽 카드가 골라졌을 때 옆 두 장이 사라지는 것과 겹쳐 「화면이 통째로 다른 것으로 바뀌었다」로
읽혔고, 무엇이 정해졌는지 다시 찾아 읽어야 했습니다. 이제 0.3초에 걸쳐 감속하며 자리를 잡고,
둘레의 등급색 빛도 그동안 함께 짙어집니다.

**안 고른 카드가 조금 더 천천히, 더 벌어져서 내려갑니다.** 한 장에 0.38초·시차 0.07초였던 것을
**0.52초·0.11초**로 바꿨습니다. 세 장이 0.52초 만에 한 덩어리로 없어지던 것이, 이제 왼쪽부터
차례로 떠나는 것이 눈에 보입니다. 다 정리되는 데 0.74초이므로 남은 4초 넘는 시간은 전부 고른
카드를 읽는 데 쓰입니다.

### 선택자 추첨이 끝나는 순간이 보이고 들립니다

**추첨 길이(3.5초)는 1틱도 바뀌지 않았습니다.** 그 안에서 고장 둘을 고쳤습니다.

- **멈추는 순간의 소리가 이제 납니다.** 이름을 넘기지 않는 틱이면 그 아래의 확정 소리까지
  함께 건너뛰던 탓에, **70틱 일정에서는 확정 소리가 한 번도 나지 않았습니다.** 끝나는 틱이
  이름 교체 틱과 겹칠 때만 우연히 나던 것을, 겹치든 말든 **반드시 한 번** 나게 바꿨습니다.
- **뽑힌 이름을 0.6초 동안 보여 줍니다.** 예전에는 굴림이 3.5초를 다 써서, 「OOO님이
  고릅니다」가 뜨는 바로 그 틱에 서버의 선택창이 도착했습니다. 3.5초를 굴려 놓고 결말이
  한두 프레임이었습니다. 이제 굴림은 2.9초에 끝나고 남은 0.6초를 결과에 씁니다.
  0.25초로는 글자 크기·색·소리가 한꺼번에 바뀌는 것을 하나도 받기 전에 사라지고, 0.5초가
  「봤다」의 최소선입니다.
- **마지막 이름이 0.45초 머뭅니다.** 다음 이름이 제 간격을 채우지 못하고 결과에 잘릴
  상황이면 아예 넘기지 않습니다. 그러지 않으면 애써 늦춰 온 마지막 한 칸이 2틱 만에
  지나가 감속이 헛수고가 됩니다.

**서버가 시간을 멈추는 길이는 그대로입니다.** 총 3.5초를 굴림과 결과 표시로 나눈 것뿐이라
선택창이 오는 시점도 바뀌지 않았습니다.

### 「허공답보」의 두 번째 점프가 즉시 뛰고, 더 높이, 앞으로 나아갑니다

**누른 순간 바로 뜹니다.** 예전에는 키를 누르면 서버에 물어보고 답이 돌아올 때까지 몸이
계속 떨어졌습니다. 빨라야 두 틱, 핑이 붙으면 더 걸리는 그 틈 때문에 **두 번째 점프에서
살짝 멈추는** 느낌이 났습니다. 이제 클라이언트가 누른 그 틱에 스스로 띄웁니다 — 바닐라가
땅 점프를 다루는 방식과 같습니다.

- **두 번 뛰지 않습니다.** 서버는 요청을 받아들여도 속도를 되돌려 보내지 않습니다. 되돌려
  보내면 몇 틱 뒤 도착한 그 값이 이미 줄어든 속도를 처음 세기로 되살려 한 번의 점프가 두
  번 튑니다.
- **판단은 여전히 서버가 합니다.** 증강이 없는데 뛰면 서버가 속도를 되돌려 끌어내리고,
  증강이 있어도 한 번 뜬 동안 두 번째는 세어 주지 않습니다. 세기와 미는 방향도 서버가
  증강 정의에서 읽습니다.
- **효과음이 몸과 같이 옵니다.** 뛴 사람에게는 클라이언트가, 주변 사람에게는 서버가 냅니다.

**두 번째 점프가 세졌습니다 — 기본 점프의 1.7배(0.714).** 첫 점프의 점프력 +50% 는
그대로입니다. 두 번째 점프는 **3.24칸**을 올라 첫 점프(2.59칸)보다 높습니다.

**가던 방향으로 밀어 줍니다.** 달려가며 뛰면 그쪽으로 약 두 칸을 더 나아갑니다. 보는
방향이 아니라 **움직이던 방향**이라, 제자리에서 뛰면 제자리 그대로 떠오릅니다. 미는 크기는
바닐라의 달리기 점프가 얹는 값과 같습니다.

**낙하 피해가 2배에서 1.5배로 낮아졌습니다.** 점프가 세진 만큼 배율을 낮췄습니다. 최고점
(5.83칸)에서 떨어졌을 때 들어오는 피해는 **4** 로 예전과 같습니다.

## 0.14.0-dev — 2026-09-01

### 「시작 대기」 안내가 무엇을 하면 되는지 알려 줍니다

예전에는 보스바·`/shareteam status`·팀 화면이 모두 **「N회차 시작 대기」**라고만 적었습니다.
상태만 적혀 있어 무엇을 기다리는 것인지, 무엇을 눌러야 하는지 알 수 없었습니다. 이제 세 곳
모두 **다음에 할 일**을 적습니다. **회차 번호는 그대로 남습니다** — 지금 몇 회차인지는 상태가
아니라 정보입니다.

자리마다 들어가는 길이가 다르므로 문장도 다르게 적었습니다.

| 자리 | 문구 |
|---|---|
| 보스바 | `SharedFate · 1회차 — 「게임 시작」을 눌러 주세요` |
| 팀 화면 「현황」 탭 (리더) | `1회차 — 「팀」 탭에서 「게임 시작」을 눌러 주세요.` |
| 팀 화면 「현황」 탭 (팀원) | `1회차 — 리더가 게임을 시작할 때까지 기다려 주세요.` |
| `/shareteam status` (리더) | `1회차 — 아직 시작하지 않았습니다. /shareteam start confirm 을 입력하거나 팀 화면(/st)의 「게임 시작」을 눌러 주세요.` |
| `/shareteam status` (팀원) | `1회차 — 아직 시작하지 않았습니다. 리더가 게임을 시작할 때까지 기다려 주세요.` |

- **보스바가 가장 짧습니다.** 화면 맨 위를 가로지르는 한 줄이고, 서버가 만들어 모두에게 같은
  것을 뿌리므로 **보는 사람이 리더인지 알 수 없습니다.** 그래서 눌러야 할 것 하나만 적습니다.
- **리더가 아닌 사람에게는 그 사람이 할 수 있는 것만 적습니다.** 누를 수 없는 사람에게
  `/shareteam start` 를 알려 줘 봐야 눌러도 거부만 당합니다.
- 회차 번호를 1로 적을 수 있는 것은 **「시작 대기」가 1회차에만 존재**하기 때문입니다.
  2회차부터는 회차가 저절로 시작됩니다.

### 인벤토리 화면(E) 왼쪽에 팀 화면 단추가 생겼습니다

`/shareteam` 을 치지 않고도 팀 화면을 엽니다. **팀이 없을 때도 보입니다** — 팀을 만드는 것도
그 화면에서 하는 일이기 때문입니다.

- **창 바깥 왼쪽 위**에 붙습니다. 창 안에는 빈자리가 없고(방어구 칸·미리보기·조합칸·인벤토리·
  추가 27칸), 오른쪽은 상태이상 목록이 씁니다.
- **조합법 책과 겹치지 않습니다.** 조합법 책 단추는 창 안 오른쪽 가운데(`leftPos + 104`)에
  있어 가로세로 모두 떨어져 있고, **책을 펼치면 단추가 그 판보다 더 왼쪽으로 물러납니다.**
  바닐라는 책을 펼칠 때 창만 밀고 화면을 다시 만들지 않으므로, 그림을 뽑기 직전에 자리를
  다시 잽니다.
- **이 모드가 창을 아래로 54px 늘려 놓은 배치와 부딪히지 않습니다.** 세로 기준을 창 위 끝으로
  잡아 두어, 추가 27칸이 붙든 안 붙든 같은 자리입니다.
- 글자 단추(「팀」)입니다. 이 모드에는 GUI 그림이 없어 아이콘 하나만 튀기 때문입니다. 크기는
  조합법 책 단추와 거의 같게 두어 두 단추가 형제로 읽힙니다. 무엇을 여는지는 툴팁이 적습니다.

### 팀 화면에 「능력치」 탭이 생겼습니다

**증강이 무엇을 얼마나 바꿨는지**를 바닐라 기본값과 나란히 보여 줍니다.

```
최대 체력  20 → 26  (+6)  (하트 13개)
공격력     1 → 9   (+8)
방어력     0 → 8   (+8)
이동 속도  100% → 115%  (+15%)
```

- 오르면 초록, 내리면 빨강입니다. 대가로 무언가를 깎는 증강이 있어 **내려간 값이 반드시 눈에
  띄어야** 합니다.
- **차례는 최대 체력 · 공격력 · 방어력 · 이동 속도입니다.** 앞의 셋이 전투의 세 축이라
  (*얼마나 버티는가 → 얼마나 때리는가 → 얼마나 덜 맞는가*) 붙여 두고, 성격이 다른 이동 속도를
  맨 뒤에 둡니다.
- **이동 속도만 백분율입니다.** 바닐라 기본값이 `0.1` 이라 그대로 적으면 빠른지 느린지 알 수
  없습니다. 방어력·최대 체력은 방어구 칸 수와 하트 수로 이미 익은 단위라 그대로 둡니다.
- **「현황」 탭이 아니라 새 탭입니다.** 「현황」은 이미 아홉 줄이라 창이 낮으면 아래 단추와
  겹칩니다. 「증강」 탭 바로 뒤에 두었습니다 — 저쪽이 *무엇을 가졌는지*라면 이쪽은 *그래서
  얼마나 세졌는지*라 이어 읽힙니다.
- **「장님 거인」의 대가는 그대로입니다.** 그 증강이 감추는 것은 *지금* 체력과 허기이고 여기
  적는 것은 *상한*입니다. 상한은 「현황」·「설정」 탭에서도 그냥 보이므로 여기서만 가리면 세
  탭이 서로 다른 말을 하게 됩니다. 다만 **방어력은 HUD 의 방어구 칸이 곧 이 숫자라**, 그것을
  가리는 증강이 생기면 여기서도 `???` 로 가립니다.

#### 공격력은 서버가 따로 보내 줍니다 — 통신 규약이 **17** 로 올라갔습니다

바닐라는 `minecraft:attack_damage` **하나만** 클라이언트에 동기화하지 않습니다. `Attributes` 의
등록부에서 이 속성만 `setSyncable(true)` 없이 등록되고, 장비의 속성 수정자를 실제로 붙이는
`LivingEntity.detectEquipmentUpdates` 도 서버에서만 돕니다. 그래서 클라이언트가 스스로 읽는
공격력은 **맨손 기본값 1** 뿐이라 무기도 증강도 들어 있지 않습니다. 공격력을 건드리는 증강이
열둘인데 그 중 무엇도 보이지 않았습니다.

- **`AttackDamagePayload` 를 새로 두었습니다.** 이미 도는 `TeamSyncPayload` 에 얹지 않은 이유는
  넷입니다. 저 묶음은 **팀마다 하나를 만들어 전원에게 같은 것을 보내는데** 공격력은 받는
  사람마다 다르고, **팀에 속하지 않아도 이 줄은 보여야** 하는데 팀이 없을 때는 빈 묶음만
  나가고, **보내는 때가 명단·레벨이 바뀔 때**라 무기를 바꿔도 나가지 않으며, 무엇보다 저
  묶음의 `StreamCodec.composite` 는 **항목 8개가 상한이고 이미 다 차 있습니다.**
- **0.25초(5틱)마다 값을 견주어 달라졌을 때만 보냅니다.** 매 틱 보내면 사람 수만큼의 패킷이
  아무 뜻 없이 초당 20번 나갑니다. 반대로 「공격력을 바꾸는 사건」을 하나하나 잡는 길은 택하지
  않았습니다 — 무기 교체·증강 획득·증강 상실·장비 교체·팀 해체·회차 리셋을 전부 걸어야 하고
  하나만 빠뜨려도 그 경우에만 옛 값이 남습니다. **결과값 하나를 견주면 무엇이 바꿨든 걸립니다.**
- **무기를 바꾸면 따라옵니다.** 바닐라가 매 틱 손에 든 것의 수정자를 갈아 끼우므로, 늦어야
  0.25초 뒤에는 화면이 새 숫자를 적습니다.
- **기준값과 지금 값은 나머지 세 줄과 같은 방법으로 뽑습니다** — `getBaseValue()` 가 바닐라
  기본값(플레이어는 1), `getValue()` 가 증강과 지금 든 무기까지 얹힌 값입니다. 증강이 거는
  수정자는 임시 수정자라 왼쪽 값을 건드리지 않습니다.
- **마법부여와 치명타는 빠져 있습니다.** 날카로움 같은 마법부여는 이 속성 밖에서 더해집니다.
  화면에도 그 사실을 한 줄로 적어 둡니다.
- 서버가 아직 알려 주지 않은 동안에는 **줄 자체를 그리지 않습니다.** 맨손 기본값 1 을 대신
  적으면 없는 사실을 지어내는 셈이고, 그것이 바로 이 패킷을 만든 이유입니다.

**통신 규약을 16에서 17로 올렸습니다.** 기존 페이로드의 형식은 한 바이트도 바뀌지 않았지만,
이 패킷을 모르는 클라이언트는 능력치 탭에서 **공격력 줄만 조용히 빠진** 화면을 보게 됩니다.
「값이 안 보인다」는 「모드가 안 맞는다」보다 알아채기 어려우므로 악수 단계에서 걸러지게 했습니다.

**나머지 세 줄은 여전히 규약을 쓰지 않습니다.** 최대 체력·방어력·이동 속도는 클라이언트가
이미 갖고 있는 값입니다 — 서버가 `ServerEntity` 에서 속성 갱신을 **본인에게도**
보내고(`sendToTrackingPlayersAndSelf`), 증강이 거는 수정자도 그 묶음에 함께 실립니다.

## 0.13.1-dev — 2026-09-01

### 5회차인데 「시작 대기」로 보이던 문제를 고쳤습니다

**이미 굴러가던 월드에서 서버만 다시 켜면 회차가 「시작 대기」에 갇히던** 문제를 고쳤습니다.
5회차를 진행 중이던 팀이 서버 재시작 뒤 「시작 대기」로 보였고, 그 상태에서는 증강도 위치
교환도 난이도 상승도 돌지 않았습니다. **더 나빴던 것은 그 화면에 「게임 시작」 단추가 다시
떴다는 점입니다** — 눌렀다면 5회차 동안 모은 것이 전부 사라졌을 것입니다.

- **이제 회차가 시작되었는지는 회차 번호만 보고 정합니다.** **1회차이고 아직 시작한 적이
  없을 때만** 「시작 대기」이고, **2회차 이상이면 언제나 진행 중**입니다. 서버가 새 월드로
  떴든 이미 굴러가던 월드로 떴든, 저장에 무엇이 적혀 있든 상관없습니다.
- 예전에는 **명단을 복원하는 순간**(= 전멸로 월드가 새로 열렸을 때)에만 회차를 켰습니다.
  월드에 팀이 살아 있으면 그 길을 지나지 않아 영영 대기로 남았습니다.
- **2회차 이상에서 팀을 새로 만들면 그 팀도 바로 진행 중입니다.**

**자동 시작은 아이템을 지우지 않습니다.** 사람을 스폰으로 옮기지도, 시각을 되돌리지도,
경험치와 증강 구간을 되감지도 않습니다. 그 다섯은 리더가 「게임 시작」을 누를 때만 하는
일입니다. 자동 시작이 하는 일은 셋뿐입니다 — 상태를 「진행 중」으로 올리고, **「유산」이 넘긴
장비를 인벤토리에 넣고**, 위치 교환의 남은 시간을 채웁니다.

새 월드와 이미 굴러가던 월드는 **위치 교환의 남은 시간 한 가지에서만** 다르게 처리합니다.
새 월드는 주기 그대로 채우고(첫 교환은 한 주기 뒤), 이미 굴러가던 월드는 **세다 만 값이
있으면 그대로 둡니다.** 0 일 때만 채우는데, 대기 중에는 주기를 세지 않으므로 그 0 은
「곧 교환할 때」가 아니라 「한 번도 세지 않았다」는 뜻이기 때문입니다.

**이 변경은 통신 규약을 올리지 않았습니다(16 그대로).**

## 0.13.0-dev — 2026-09-01

### 「게임 시작」은 이제 첫 회차 전에 한 번만 누릅니다

직전 판에서 **매 회차마다** 「게임 시작」을 눌러야 하게 만든 것을 되돌립니다.

- **처음 한 번만 누릅니다.** 팀을 만들면 「시작 대기」이고, 리더가
  `/shareteam start confirm` 이나 팀 화면의 단추로 1회차를 시작합니다. 여기까지는 그대로입니다.
- **그 뒤로는 전멸해서 새 월드로 넘어가면 저절로 「진행 중」입니다.** 다시 누르지 않습니다.
- **월드 초기화를 끈 서버에서도 회차가 이어집니다.** 예전에는 전멸 처리가 회차를 대기로
  되돌렸는데, 같은 월드에서 그대로 이어 가는 서버에서는 그 단추가 방금 살아남은 것들까지
  지웠습니다.

자동으로 열린 회차에서는 **「유산」이 넘긴 장비가 인벤토리에 들어가고**, **위치 교환의 남은
시간이 주기 그대로 채워집니다**(새 월드에 들어서자마자 첫 교환이 터지지 않도록). 그 밖에
「게임 시작」이 하던 일은 다시 하지 않습니다 — 새 월드는 이미 1일차 아침이고 팀 상태도
통째로 새로 만들어지기 때문입니다.

`/shareteam start` 와 `/shareteam help` 의 안내 문구도 「첫 회차 전에 한 번만」으로 고쳤습니다.
**이 변경은 통신 규약을 올리지 않았습니다.**

### 게임 오버 연출 — 화면 한가운데 5초 카운트다운

팀이 전멸하면 화면 한가운데에 크게 **「게임 오버 · N회차」**가 뜨고, 그 바로 아래에서
**빨간 숫자가 5·4·3·2·1** 로 내려갑니다. 숫자가 다 내려가면 **서버가 종료됩니다.**

- **종료 절차는 예전과 똑같습니다.** 이 모드는 월드를 직접 지우지 않고 **표식 파일만 남기고
  정상 종료**하며, 월드를 지우고 서버를 다시 여는 일은 재시작 루프 스크립트가 합니다.
  카운트다운은 그 앞에 붙은 연출일 뿐입니다.
- **전멸부터 종료까지가 8초에서 5초로 줄었습니다.** `worldResetDelayTicks` 의 기본값이
  160에서 **100(5초)** 이 되었고, 이 값이 곧 화면에 뜨는 카운트다운의 길이입니다. 설정
  파일에 이 항목을 적어 둔 서버는 적어 둔 값이 그대로 쓰입니다.
- 사망 알림을 켠 팀에서는 숫자 아래에 누구의 죽음으로 끝났는지가 함께 적힙니다.
- **모드를 깔지 않은 클라이언트에게는 숫자가 보이지 않습니다**(`requireClientMod` 가 기본으로
  켜져 있어 실제로는 문제가 되지 않습니다).

**이 연출도 통신 규약을 올리지 않았습니다.**

**5초 동안 멈추는 것.**

- **피해** — 사람이든 몹이든 전부 버립니다.
- **위치 교환·시차·정거장** — 돌지 않습니다. 월드를 지우지 않는 서버에서 종료 직전에 자리가
  뒤바뀌면 **벽 속에 박힌 채 저장됩니다.**
- **난이도 상승 시간** — 세지 않습니다. 회차는 이미 끝났습니다.
- **이동은 막지 않습니다.** 전멸한 팀원은 이미 사망 화면에 갇혀 움직일 수 없습니다.

### 발전과제 달성이 채팅에 뜨지 않습니다

바닐라 게임 규칙 `show_advancement_messages`(26.2 이전 이름 `announceAdvancements`)를
**서버가 뜰 때마다** 끕니다. 달성 자체는 그대로이고 채팅 줄만 사라집니다.

게임 규칙은 월드(`level.dat`)에 새겨지므로 `/gamerule` 로 한 번 꺼 두어도 **다음 회차의 새
월드에서는 되돌아갑니다.** 서버가 뜰 때마다 맞춰야 새로 만든 월드든 이어 하는 월드든 언제나
꺼져 있습니다.

알림을 그대로 보고 싶은 서버를 위해 `config/sharedfate.json` 의
`silenceAdvancementMessages` 로 되돌릴 수 있습니다. 기본값은 **켬(알림을 없앤다)** 입니다.

### 증강을 손봤습니다

#### 프리즘 「금기의 광석」 — 무력화가 아니라 자동 폐기

지금까지는 다이아몬드 도구·방어구를 들고만 있어도 되고 쓸모만 없어지는 쪽이었습니다. 이제는
**핫바에 두거나 장착하면 계속 자동으로 버려집니다.** **인벤토리 깊숙이(핫바 밖) 보관하는 것은
여전히 됩니다** — 소지 자체를 막는 증강이 아니라 손에 쥐고 쓰는 것을 막는 증강이기
때문입니다.

증강 정의의 `item_ban` 에 새 필드 `discard`(기본 거짓)가 생겼습니다. 다른
`item_ban`·`equip_ban` 증강은 이 필드를 적지 않았으므로 예전과 동작이 같습니다.

#### 무작위 증강 지급이 연쇄로 이어집니다

「숨은 재능」·「하늘의 은총」·「도박꾼」·「환골탈태」처럼 "이 증강을 고르면 다른 증강을 더
준다" 는 효과가 뽑은 증강이 또 같은 종류의 효과를 가지면, 이제 그 효과도 실제로 발동합니다.
같은 증강을 두 번 처리하지 않고 연쇄 길이에 상한이 있어 끝없이 이어지지는 않습니다.

#### 골드 「굴착기」 — 모든 블록 ×3, 광석만 절반

"돌만" 빨라지던 것을 **모든 블록**으로 넓혔습니다. 대신 광석은 절반으로 느려집니다.

#### 프리즘 「유산」 — 승계 기준이 고르는 시점에서 전멸 시점으로

고른 순간 몰수하는 것은 그대로지만, **다음 회차로 넘어가는 것은 몰수한 목록이 아니라
전멸하는 시점에 팀이 실제로 가진 도구·무기·방어구**입니다.

#### 실버 「길잡이 나침반」 — 나침반을 함께 지급합니다

나침반이 없으면 무용지물이었던 것을 고쳐, 고르면 나침반 1개를 함께 줍니다.

#### 프리즘 「도박꾼」 — 대가를 없앴습니다

15·20·25렙 중 20·25렙을 실버로 고정하던 대가를 없앴습니다. 이제 대가 없이 무작위 증강
2개를 그냥 받습니다.

#### 프리즘 「허공답보」 — 점프력 50%, 공중 점프도 그만큼 세게

점프력 보너스를 30%에서 **50%** 로 올렸고, 두 번째 점프가 첫 점프보다 약하던 것을 맞췄습니다.
최고점에서 떨어지면 낙하 피해가 이제 실제로 들어옵니다(정점에서 약 5.1칸 낙하, 피해 4) —
「낙하 피해 2배」라는 대가가 이름뿐이던 것이 고쳐졌습니다.

### 통신 규약이 15에서 16으로 올랐습니다 — 클라이언트를 함께 갱신해야 합니다

인챈트 탁자에 **다이아몬드 칸이 하나 늘었고**, 확장 27칸이 창 오른쪽 바깥에서 **플레이어
인벤토리 아래로** 옮겨졌습니다. 서버와 클라이언트가 아는 슬롯 수가 다르면 클라이언트가
죽으므로 번호를 올렸습니다. **0.12.0-dev 이하 클라이언트는 악수 단계에서 거부됩니다.**

### 인챈트에 다이아몬드를 넣는 칸이 생겼습니다

직전 판에서는 다이아몬드 5개를 인벤토리에서 몰래 걷었습니다. 어디에 얼마를 넣어야 하는지
화면에 단서가 없고, 단추를 누르는 순간 인벤토리 어딘가에서 다이아몬드가 조용히 사라졌습니다.

- **청금석 칸 바로 아래에 진짜 칸이 생겼습니다.** 다이아몬드만 들어갑니다. 비어 있을 때는
  다이아몬드 아이콘이 흐리게 보입니다 — 청금석 칸과 같은 방식입니다.
- **이제 그 칸에 든 것만 셉니다.** 인벤토리에 다이아몬드가 아무리 많아도 칸이 비어 있으면
  단추가 눌리지 않습니다. 단추의 숫자·회색 판정·툴팁이 모두 이 칸을 기준으로 바뀝니다.
- **세 칸 모두 5개**로 그대로입니다.
- **창을 닫으면 남은 다이아몬드를 돌려줍니다.** 바닐라가 인챈트할 물건과 청금석을 돌려주는
  것과 같은 자리에서 함께 비웁니다.
- **다이아몬드를 쉬프트 클릭하면 그 칸으로 들어갑니다.** 예전에는 인챈트할 물건 자리에
  다이아몬드가 놓였습니다.
- 「인벤토리」 글자는 새 칸과 겹치지 않도록 조합법 단추 아래로 옮겼습니다.

### 추가 27칸이 인벤토리 **아래**로 내려왔고, 눌러도 아이템이 버려지지 않습니다

**1. 자리 — 오른쪽 바깥에서 인벤토리 바로 아래로.** 상자·화로·제작대·인챈트·주민 등
플레이어 인벤토리를 쓰는 **모든 창**에서 추가 27칸이 바닐라 세 줄 바로 아래에 붙고 핫바가
그만큼 내려갑니다. 창도 54px 커집니다. 보기에는 **바닐라 인벤토리가 여섯 줄로 늘어난 것**과
같습니다. 조합법 책 단추도 창을 따라 내려갑니다.

**2. 눌렀을 때 아이템이 버려지던 것 — 고쳤습니다.** 추가 칸이 창 바깥에 있어서 그 자리를
누르면 「창 밖을 눌렀다」로 판정되어 아이템이 버려졌습니다. 칸이 창 안으로 들어오면서
사라진 문제입니다.

**3. 쉬프트 클릭이 위아래를 따로 다루던 것 — 하나로 이었습니다.** 바닐라 27칸과 추가 27칸이
이제 **하나의 연속된 공간**입니다.

- 상자에서 쉬프트 클릭하면 예전처럼 **핫바 끝부터** 채웁니다. 예전에는 물건이 추가 칸부터
  채워져 아래 칸으로 날아갔습니다.
- 같은 아이템은 **위아래를 가리지 않고 먼저 합칩니다.** 예전에는 같은 아이템이 한 칸에
  모이지 않고 흩어졌습니다.
- **화로·인챈트의 추가 칸에서 쉬프트 클릭이 먹습니다.** 예전에는 아무 일도 일어나지
  않거나(화로), 인챈트할 물건 자리에 밀려 들어갔습니다(인챈트).

팀에 속하지 않은 동안에는 추가 칸을 치우고 창 높이와 핫바를 바닐라 그대로 되돌립니다.

## 0.12.0-dev — 2026-09-01

**통신 규약이 14에서 15로 올랐습니다. 서버와 모든 클라이언트를 함께 갱신해야 합니다.**
팀 동기화 묶음에 「회차가 시작되었는가」가 들어갔기 때문입니다 — 묶음의 형식 자체가 바뀌어
예전 클라이언트는 팀 정보를 아예 읽지 못합니다.

### 회차는 이제 「게임 시작」을 누를 때 시작됩니다

예전에는 회차가 **월드에 붙어** 있었습니다. 서버가 뜨면 팀이 있는지도 보지 않고 곧바로
「1회차 진행 중」이었고, 그래서 **팀을 만들기 전에 돌아다닌 시간까지 회차에 들어갔습니다.**
회차의 시작점을 사람이 정하는 한 순간으로 옮겼습니다.

- **`/shareteam start` 와 팀 화면의 「게임 시작」 단추로 시작합니다.** 리더만 누를 수
  있습니다. 누르면 이렇게 됩니다.
  - 회차가 「시작 대기」에서 「진행 중」으로 바뀝니다
  - **월드 시각이 1일차 아침(누적 0틱)으로** 돌아가고, 난이도 상승이 세는 회차 경과
    시간도 0에서 다시 시작합니다
  - **접속 중인 팀원 전원이 월드 스폰으로 모입니다**
  - **모든 아이템이 사라집니다** — 공유 인벤토리 36칸, 추가 27칸, 방어구·오프핸드,
    공유 엔더상자까지 전부입니다. **드랍이 아니라 삭제**입니다. 발밑에서 다시 주울 수
    있으면 맨몸으로 시작한다는 뜻이 없습니다
  - 공유 경험치가 0이 되고 증강 구간을 처음부터 다시 셉니다. 아이템을 지우면서 그
    아이템으로 올린 레벨만 남겨 두면 **시작하자마자 증강 선택창이 몇 개나 터집니다**
  - 체력·허기·상태이상이 회차 처음 값으로 돌아가고, 다시 뽑기 횟수가 다시 찹니다
- **되돌릴 수 없으므로 두 단계입니다.** `/shareteam start` 는 무엇이 사라지는지
  보여 주기만 하고, `/shareteam start confirm` 을 쳐야 실제로 시작합니다
  (`disband confirm` 과 같은 방식). 팀 화면의 단추도 **한 번 누르면 붉은 경고로 바뀌고
  한 번 더 눌러야** 나가며, 그 단추가 보내는 것도 결국 `start confirm` 하나입니다.
  탭을 옮기면 확인 단계는 풀립니다.
- **이미 시작한 팀에게는 단추가 아예 보이지 않고**, 명령으로 쳐도 거부됩니다.
- **매 회차 눌러야 합니다.** 전멸해서 새 월드로 넘어간 팀도 「시작 대기」로 되살아납니다.
  월드 초기화를 끈 서버에서도 전멸하면 대기로 돌아갑니다.

### 시작 전에는 회차가 돌지 않습니다

- **증강 구간을 세지 않습니다.** 팀원을 기다리며 올린 레벨로 증강이 나오면 회차가
  시작되기도 전에 판이 정해집니다.
- **위치 교환이 돌지 않습니다.** 시작하는 순간 남은 시간이 주기 그대로 채워져,
  첫 교환은 시작으로부터 한 주기 뒤입니다.
- **난이도 상승 시간을 세지 않습니다.** `/shareteam status` 도 「켬 (게임을 시작하면
  그때부터 셉니다)」로 적습니다.
- **팀원이 죽지 않습니다.** 시작 전에는 피해를 통째로 버립니다. 체력이 공유라 한 명만
  죽어도 팀이 전멸하는데, 시작도 안 한 회차 때문에 월드가 지워지면 사람이 하려던 일과
  정반대이고, 전멸만 막으면 **하드코어에서 관전자로 영원히 갇힙니다.** 그래서 죽는 일
  자체를 없앴습니다. 넉백이나 산소 게이지처럼 피해가 아닌 것은 그대로 돌지만, 시작하는
  순간 체력·허기가 전부 처음 값으로 돌아가므로 남는 것이 없습니다.
- **드래곤을 잡아도 승리가 아닙니다.** 시작하지 않은 팀의 처치는 회차 승리로 세지
  않습니다.

### 화면 표시

- **보스바가 「N회차 · 시작 대기」로 나옵니다.** 시작하면 「N회차 진행 중」으로 바뀌고
  색도 노랑에서 파랑이 됩니다. 예전에는 팀이 하나도 없어도 「N회차」라고만 적혀 이미
  회차가 굴러가는 것처럼 읽혔습니다.
- **`/shareteam status` 맨 위에 회차 줄이 생겼습니다** — 「3회차 진행 중」 또는
  「3회차 시작 대기 (리더가 /shareteam start 로 시작합니다)」.
- **팀 화면 「현황」 탭에 시작 대기 안내가 나옵니다.** 단추는 일부러 여기 두지
  않았습니다 — 창을 열자마자 보이는 자리에 되돌릴 수 없는 단추가 있으면 잘못 누릅니다.
  단추는 「팀」 탭입니다.
- **팀을 만들면 「아직 회차는 시작되지 않았습니다」를 함께 알립니다.**
- **증강이 정해지면 안 고른 카드가 아래로 미끄러져 내려갑니다.** 예전에는 그 자리에서
  한 프레임 만에 사라져서, 남은 한 장이 가운데로 옮겨 가는 것과 겹쳐 화면이 갑자기
  바뀐 것처럼만 보였습니다. 이제 왼쪽 카드부터 조금씩 시차를 두고 떨어지는 것처럼
  가속하며 내려가고, 내려가는 동안 어둠에 가라앉습니다. **0.5초 남짓이면 다 끝나므로**
  나머지 2.5초는 그대로 고른 카드를 보는 시간입니다. 시간이 멈춰 있는 3초는 그대로입니다.

### 인챈트 값이 경험치 레벨에서 다이아몬드로 바뀌었습니다

인챈트 탁자가 **경험치 레벨을 아예 보지 않습니다.** 요구 레벨도, 레벨 소모도 없습니다.
**레벨 0으로도 세 칸 모두 인챈트할 수 있고**, 대신 **다이아몬드 5개**를 냅니다. 밸런스가
크게 바뀌는 변경이며 일부러 그렇게 한 것입니다 — 경험치는 이 모드에서 팀이 함께 쓰는
증강 자원이라, 인챈트에 쓰면 증강 구간이 늦어져 두 체계가 서로를 갉아먹었습니다.

- **대가는 세 칸 모두 다이아몬드 5개입니다.** 칸에 따라 다르지 않습니다.
- **청금석은 바닐라 그대로 1·2·3 개**입니다.
- **다이아몬드는 공유 인벤토리 36칸과 확장 패널 27칸에서 함께 셉니다.** 인벤토리를 먼저
  비우고 모자라면 확장 패널에서 채웁니다. **새 슬롯을 만들지 않았으므로 통신 규약은
  그대로 15** 입니다.
- **크리에이티브는 다이아몬드도 청금석도 없이 됩니다.** 바닐라의 예외를 그대로 따릅니다.
- **인챈트의 세기는 그대로입니다.** 서고 개수로 정해지는 숫자(바닐라의 요구 레벨)는 여전히
  같은 방식으로 계산되어 인챈트의 위력을 정합니다. 화면에 그 숫자를 보여 주지 않을 뿐입니다.

**화면.** 단추에 찍히던 숫자가 요구 레벨에서 **필요한 다이아몬드 개수**로 바뀝니다.
단추가 회색으로 죽는 조건도 **가진 다이아몬드** 기준이고, 툴팁의 마지막 줄은
「인챈트 레벨 N」 대신 **「다이아몬드 5개」**, 모자랄 때는 빨간 **「다이아몬드 5개가
필요합니다」**로 나옵니다. 필요한 값은 클라이언트가 이미 아는 것들이라 새로 주고받는 것은
없습니다.

### 「허공답보」의 더블 점프가 실제로 두 번 뛰어집니다

예전에는 **스페이스를 한 번 누르면 땅 점프와 공중 점프가 같은 틱에 겹쳐 버렸습니다.**
바닐라는 땅에서 뛴 그 틱 안에 이미 땅을 떠나므로, 「방금 눌렀다 + 지금 공중이다」만 보던
예전 판정에는 **한 번의 누름이 두 조건을 동시에 만족**했습니다. 게다가 겹친 공중 점프가
땅 점프의 속도를 **더 낮은 값으로 덮어써서**, 증강을 가진 쪽이 오히려 낮게 뛰었습니다.
더블 점프를 한 느낌이 없던 것이 아니라 **더블 점프가 일어나지 않았습니다.**

- **한 번 뜬 뒤 착지하기 전까지 언제든 한 번 더 뛸 수 있습니다.** 높이도, 올라가는
  중인지 떨어지는 중인지도 따지지 않습니다. 절벽에서 그냥 걸어 나간 경우에도 첫 누름이
  공중 점프가 됩니다.
- **공중에서 스페이스를 뗐다가 다시 눌러야 합니다.** 누르고 있는 동안 저절로 떠오르는
  것은 점프가 아니라 비행이라, 붙잡고 통통 뛰는 사람의 공중 점프를 몰래 쓰지 않습니다.
- **더 빠르게 올라가던 중이라면 속도를 깎지 않습니다.** 폭발에 떠밀렸다거나 점프력
  증강이 겹쳤을 때 공중 점프가 오히려 발목을 잡던 자리입니다.
- **공중 점프에 소리가 납니다** — 브리즈가 바람을 밟고 뛰는 `entity.breeze.jump`.
  근처 사람에게도 들리고, 서버가 요청을 실제로 받아들였을 때만 납니다.

### 그 밖에

- **「유산」이 넘긴 장비를 돌려주는 시점이 바뀌었습니다.** 예전에는 새 월드를 열 때
  바로 인벤토리에 꽂았는데, 그 뒤에 오는 「게임 시작」이 인벤토리를 비우므로 그대로
  두면 **유산이 시작과 동시에 사라집니다.** 이제 회차 경계를 넘어온 장비는 그대로
  들려 있다가 **아이템 청소가 끝난 뒤에** 시작 인벤토리로 들어갑니다.
- **이 기능이 생기기 전의 월드는 「이미 시작한 팀」으로 읽습니다.** 저장에 항목이 없으면
  참으로 봅니다. 거짓으로 읽으면 이미 몇 시간을 플레이하던 팀이 갑자기 「시작 대기」가
  되고, 그 상태에서 「게임 시작」을 누르면 가진 것이 전부 사라집니다.
- **골드 「얼음 발」이 2배 더 미끄러워졌습니다.** 이전 값은 체감상 덜 미끄러웠습니다.
- **프리즘 「고행자」가 이제 자연 회복 중에도 배가 고파지지 않습니다.** 예전에는 달리기·
  채굴 같은 행동으로만 허기가 안 줄고, 체력이 자연 회복될 때는 예외로 배가 줄었습니다.
  지금은 어떤 상황에서도 허기가 줄지 않습니다.

## 0.11.0-dev — 2026-09-01

**통신 규약이 13에서 14로 올랐습니다. 서버와 모든 클라이언트를 함께 갱신해야 합니다.**
증강 후보 다시 뽑기 때문입니다 — 새 C2S 패킷이 생겼고 증강 선택창 패킷의 형식 자체가
바뀌었습니다. 예전 클라이언트는 악수 단계에서 접속이 거부됩니다.

### 증강 후보 다시 뽑기

- **증강 선택창의 카드 아래에 「다시 뽑기」 단추가 생겼습니다.** 후보 셋이 마음에 안 들면
  **같은 등급 안에서** 셋을 다시 뽑습니다. 이미 가진 증강은 재추첨에서도 빠집니다.
  단추에 **이번 회차에 남은 횟수**가 늘 보이고, 0이면 잠긴 채로 남습니다 — 사라지면
  원래 없는 기능인지 다 쓴 것인지 알 수 없기 때문입니다.
- **회차당 기본 3회이고, 팀을 만들 때만 정합니다.**
  `/shareteam create ... reroll <0~10> <이름>` 으로 정하고, 적지 않으면 3회입니다.
  0으로 만든 팀은 다시 뽑기를 아예 쓰지 않습니다. 증강·최대 체력·위치 교환·난이도
  상승과 같은 자리에 들어간 **다섯 번째 「팀 만들 때만 정하는 설정」**입니다.
- **회차가 넘어가면 횟수가 다시 찹니다.** 「회차당 몇 번」이라는 결정은
  `sharedfate-team-roster.json`(형식 6)으로 회차를 넘어 이어지고, 이번 회차에 쓰고 남은
  횟수는 월드 저장에 들어가 전멸과 함께 사라집니다.
- **다시 뽑을 때마다 선택 제한시간이 60초로 다시 찹니다.** 남은 시간이 5초일 때 다시
  뽑고서 그대로 시간이 끝나면, 새 후보를 읽지도 못한 채 무작위로 정해집니다. 예고
  ("30초 남았습니다")도 함께 처음부터 다시 셉니다. 다시 뽑는 동안에도 **시간 정지와
  무적은 그대로 유지**됩니다.
- **판단은 전부 서버가 합니다.** 클라이언트는 "눌렀다"와 "어느 창에서 눌렀다"만 보내고,
  남은 횟수 검사도 재추첨도 서버가 처음부터 다시 합니다. 진행 중인 강제 선택 세션이
  아니거나, 보낸 사람이 선택자가 아니거나, 남은 횟수가 0이면 **조용히 무시**합니다.
  창을 조작해도 정해진 횟수보다 더 뽑을 수 없습니다.

### 팀 화면 — 만들 때 정하는 것이 일곱 가지가 됐습니다

- **설정 탭에서 단추 셋을 걷어냈습니다.** 최대 체력 ±, 교환 주기 ±1분 / 위치 교환 끄고
  켜기, 증강 끄고 켜기 — 셋 다 이미 서버가 거부만 하던 단추입니다. 이제 값을 글자로만
  보여 주고 「이 설정들은 팀을 만들 때 정한 값이라 바꿀 수 없습니다.」를 함께 적습니다.
  **보는 것은 예전처럼 리더만** 입니다.
- **팀 만들기 탭에서 일곱 가지를 모두 정합니다** — 증강·피격 알림·사망 알림·난이도
  상승·최대 체력(20~40)·위치 교환(끔 / 1~120분)·다시 뽑기 횟수. 숫자 셋은 −/+ 두
  단추 대신 **누를 때마다 다음 값으로 굴러가는** 단추 하나입니다. 일곱 가지에 −/+ 를
  다 붙이면 창 높이를 넘칩니다.
- **증강 토글의 기본값이 「사용」이 됐습니다.** 서버가 아무것도 안 적었을 때 쓰는 값과
  같아졌습니다. 예전에는 화면만 「사용 안 함」이라 눈에 보이는 것과 실제가 어긋났습니다.
- **「증강은 나중에 설정 탭에서 바꿀 수 있습니다」를 지웠습니다.** 증강이 팀 생성 시
  고정 설정이 된 뒤로 거짓이 된 문구입니다.

### 회차가 갈수록 어려워지는 선택지

- **팀을 만들 때 「난이도 상승」을 켤 수 있습니다.** 켜면 **30분마다 적대적인 몹의
  최대 체력과 공격력이 4%씩 오릅니다.** 1시간이면 8%, 5시간이면 40%입니다. 곱이 아니라
  덧셈이라 얼마나 세졌는지 암산할 수 있고, **상한은 25단계(두 배)** 입니다.
- **엔더 드래곤은 오르지 않습니다.** 회차를 끝내는 조건이라 강화하면 오래 끈 회차가
  잠깁니다. 위더·워든·엘더 가디언은 반드시 잡아야 하는 상대가 아니라 **스스로 고르는
  도전**이므로 함께 오릅니다.
- **시간은 팀원이 한 명이라도 접속해 있을 때만 흐릅니다.** 서버가 꺼져 있던 시간도,
  아무도 없던 시간도 세지 않습니다. 지금 몇 %인지는 `/shareteam status` 에 나옵니다.
- 기본값은 **꺼짐**이고, 다른 설정과 마찬가지로 **팀을 만들 때만** 정합니다.
- 증강이 주는 몹 배율과는 **곱해집니다.** 서로 덮어쓰지 않습니다.

### 화면

- **`/shareteam` 증강 탭에 스크롤이 생겼습니다.** 예전에는 목록이 화면 밖으로 나가면
  「…」 한 줄로 잘려 아래를 볼 수 없었습니다. **GUI 배율 4에서는 5개째, 3에서는
  7개째부터** 잘렸는데 한 회차에 여덟 개를 모으므로 반드시 겪는 문제였습니다.
- **다음 증강까지 남은 게이지가 생겼습니다.** 화면 아래 가운데, 경험치 레벨 숫자 바로
  위에 경험치 바와 같은 모양으로 그립니다. 레벨을 써 버리면 다음 구간까지 얼마나
  남았는지 알기 어렵던 것을 눈으로 보게 했습니다. 만렙을 넘기면 사라집니다.
- **고른 증강을 강조하고 3초를 세어 줍니다.** 화면을 어둡게 깔고 정해진 카드를 가운데로
  옮겨 등급색으로 빛나게 한 뒤, 「3초 뒤 다시 시작합니다」를 셉니다. 멈춰 있는 시간
  자체는 예전과 같은 3초입니다 — 무엇이 정해졌는지 보여 주지 않고 갑자기 게임으로
  돌아가던 것을 고친 것입니다.
- **팀 화면의 공유 체력이 최대 체력만 보여 줍니다.**

### 그 밖에

- **`/st` 로도 쓸 수 있습니다.** `/shareteam` 의 모든 하위 명령이 그대로 통하고,
  인자 없이 `/st` 만 쳐도 팀 화면이 열립니다.
- **스폰 근처에서 블록을 놓거나 캘 수 없던 문제.** 서버의 `server.properties` 에서
  `spawn-protection` 을 `0` 으로 두면 됩니다.
- **운영자용 증강 시험 명령이 생겼습니다** — `/st perktest give|remove|clear|list`.
  **기본은 꺼져 있습니다.** `config/sharedfate.json` 의 `perkTestCommands` 를 켜고
  **운영자 권한까지 있어야** 동작합니다. 켜져 있으면 서버 시작 로그·접속 시 채팅·
  `status` 세 곳에서 경고하고, 한 번이라도 쓴 팀에는 회차가 끝날 때까지 표식이
  남습니다. 실제로 노는 서버에서는 켜지 마십시오.
- **프리즘 구간이 15렙 하나에서 15렙·30렙 둘로 늘었습니다.** 한 회차에 프리즘 카드를
  두 번 받습니다. 「도박꾼」의 20·25렙 실버 고정과는 겹치지 않고, 15렙에서 고른
  프리즘은 30렙 후보에 다시 나오지 않습니다.
- **증강 구간 만렙이 35렙에서 40렙으로 늘었습니다.** 한 회차에 여덟 번(5·10·15·20·
  25·30·35·40) 고릅니다. 40렙은 실버·골드 무작위 구간이고, 프리즘 고정 구간(15·30)과
  도박꾼의 20·25렙 실버 고정은 그대로입니다.
- **선택자를 뽑는 연출이 5초에서 3.5초로 줄었습니다.** 회차마다 여덟 번 겪는 연출이라
  5초는 길었습니다. 굴림 간격도 같은 비율로 함께 줄어 속도감은 그대로입니다. 시간이
  멈춰 있는 길이도 그만큼 짧아집니다.
- **강제 증강 선택 중 물속에 있으면 산소가 계속 줄어들던 버그를 고쳤습니다.** 무적은
  익사 피해만 막고 공기 게이지 자체는 얼어 있는 동안에도 줄고 있어서, 선택이 끝나는
  순간 곧바로 익사 피해를 받는 일이 있었습니다. 이제 선택이 시작되는 시점의 공기량을
  기억해 뒀다가 세션이 끝날 때까지 그 값으로 묶어 둡니다.

### 증강 값 조정

밀어내기(공격력 −25%→−10%) · 수확자(공격 속도 −35%→−20%) · 작아진 몸(공격력
−15%→−5%) · 제왕과 신하(공격력 −30%→−15%, 리더가 1분마다 순환) · 비상식량(황금
사과 10→20개) · 유리 세계(받는 피해 ×3→×2.5) · 불굴(체력 30%→50% 경계) · 비옥한
땅(50% 확률 +1 → 확정 3배, 허기 회복 −20%) · 포식(허기 4→10, 체력 2→8) · 광전사
(최대 체력 +10 추가) · 허공답보(점프력 +30% 추가) · 손에 쥔 목숨(토템 1→3개) · 얼음
발(미끄러움 부호 반전 — 원래 부호가 반대라 오히려 덜 미끄러웠습니다) · 버프 돌리기
(대가에서 실명 제거, 남은 대가 5초→2초).

### 증강 효과 재설계 — 3종

- **해독제 → 타지 않는 살갗** — 독·시듦 면역에서 화염 면역으로 바뀌어 id·이름·
  아이콘을 모두 새로 붙였습니다.
- **삽질의 대가** — 대가가 "다른 무기 공격력 1 고정"에서 "공격력을 늘리는 효과를
  전혀 못 받는다"(원래 공격력과 감소 효과만 적용)로 바뀌었습니다. 새 효과 타입
  `no_damage_boost`가 이 사람의 공격력 속성에서 이 증강 자신과 무기 자체가 주는
  값을 뺀 나머지 중 양수(증가분)만 상쇄합니다.
- **메아리 채굴** — "가장 가까운 팀원 발밑"에서 "내가 캔 블록 근처의 블록 하나"로
  바뀌어 팀원과 무관해졌습니다.
- **폭발 교환** — 위치 교환과 같은 틱에 즉시 터지고 "떠난 사람만" 면역이라 도착한
  사람이 못 피하던 문제를 고쳤습니다. 이제 0.5초 뒤에 터지고, 그 교환에 참여한 사람
  전원이 1초간 면역입니다.
- **나무꾼의 욕심** — "원목 채굴 시 25% 확률로 +1"에서 "나무 도끼로 우클릭하면 나무
  60개를 소모해 무작위 광물 하나(다이아몬드 10% · 금 15% · 철 15% · 구리 30% · 석탄
  30%)를 얻고, 허기 V·독 I 을 10초간 얻는다"로 완전히 바뀌었습니다.

### 증강 추가 — 75개에서 78개로

실버 29 → **30**, 골드 29 → **30**, 프리즘 17 → **18** 입니다.

- **숨은 재능(실버)** — 무작위 골드 증강 1개 즉시 지급 / 최대 체력 −4
- **하늘의 은총(골드)** — 무작위 프리즘 증강 1개 즉시 지급 / 최대 체력 −8, 받는
  피해 ×1.25
- **환골탈태(프리즘, 30렙 전용)** — 가진 증강 전부(자기 자신 포함)가 무작위 골드
  증강으로 같은 수만큼 바뀝니다. 자기 자신은 효과는 사라지고 보유 목록에 기록으로만
  남습니다. 이미 가진 골드도 다시 뽑힐 수 있고, 결과가 지금보다 나빠질 수도 있는
  진짜 도박입니다.

### 새 효과 타입 4종 — 48종에서 52종으로

`rarity_grant`(지정 등급 무작위 지급, 「도박꾼」의 등급 지정판), `no_damage_boost`
(공격력 증가 효과 차단), `ore_exchange`(나무를 광물로 교환), `rarity_reroll`(지정
등급으로 보유 증강 전체를 다시 채움 — `rarity_grant` 의 반대 방향으로, 더하는 대신
지우고 다시 뽑습니다).

### 특정 구간부터만 나오는 증강 — 새 필드 `min_level`

증강 정의에 `min_level`(기본 0, 제한 없음)이 생겼습니다. 그 레벨 구간에 닿기 전에는
후보로 나오지 않습니다. 「환골탈태」를 30렙 전용으로 만들 때 쓴 필드이고, 증강 후보
다시 뽑기도 같은 검사를 지나므로 이른 구간에서 다시 뽑아도 튀어나오지 않습니다.

## 0.10.0-dev — 2026-08-31

증강이 9개 늘고 효과 타입이 7종 늘었습니다. **통신 규약은 13 그대로**라 0.9.0-dev
클라이언트와 악수는 통합니다. 다만 새 증강은 서버와 클라이언트를 이 jar 로 바꿔야
나옵니다.

> 서버에 `config/sharedfate-perks.json` 이 있으면 jar 안의 기본 풀 대신 그 파일이
> 쓰입니다. 예전 파일이 남아 있으면 새 증강이 하나도 나오지 않습니다.

### 증강 추가 — 66개에서 75개로

실버 25 → **29**, 골드 26 → **29**, 프리즘 15 → **17** 입니다.

- **실버**
  - **돌아가는 곡괭이** — 1분마다 무작위 팀원 한 명이 채굴 속도 +20% / 나머지는 −10%
  - **길잡이 나침반** — 오버월드에서 나침반이 가장 가까운 마을을 가리킴 / 몹에게 받는
    피해 ×1.3. 네더의 「요새 탐지기」와는 차원이 달라 서로 덮어쓰지 않습니다
  - **시차** — 위치 교환이 한 명씩 5~10초 간격으로 순서대로 일어남 / 그동안 팀이 옛
    자리와 새 자리에 나뉘어 있고, 실제 교환 간격도 그만큼 길어짐
  - **공명** — 16칸 안 팀원과 같은 종류 블록을 캐면 둘 다 성급함 I 5초 / 16칸 안에
    아무도 없으면 채굴 속도 −15%
- **골드**
  - **폭발 교환** — 위치가 바뀔 때 떠난 자리가 폭발(반경 4, 블록 파괴, 불은 안 붙음) /
    5초 예고와 효과음이 사라짐. 떠난 사람만 면역이라 도착한 사람은 맞습니다
  - **정거장** — 매 교환마다 무작위 팀원 한 명에게 전원이 모이고 15초 뒤 각자 원래
    자리로 복귀 / 그 15초 동안 나약함 I, 복귀 지점이 어떻게 변했든 그대로 돌아감
  - **메아리 채굴** — 캔 블록이 가장 가까운 팀원 발밑에서 한 번 더 캐짐 / 도구
    내구도가 2배로 닳음
- **프리즘**
  - **유산** — 고르는 즉시 팀의 도구·무기·방어구를 전부 잃고, 전멸하면 그것이 다음
    회차 시작 인벤토리로 넘어감
  - **도박꾼** — 등급을 가리지 않고 무작위 증강 2개를 즉시 얻음 / 20·25레벨의 증강이
    실버로 고정됨

### 새 효과 타입 7종 — 41종에서 46종으로

`legacy_gear`(회차를 넘겨 장비를 승계), `swap_explosion`(교환 시 폭발),
`gambler`(무작위 증강 지급과 이후 등급 고정), `staggered_swap`(순차 교환),
`swap_rally`(집합과 복귀), `paired_mining`(둘이 같은 블록을 캘 때),
`echo_mining`(팀원 자리에서 한 번 더 캐기).

전부 기존 훅과 바닐라 공개 API 로 되어 **새 Mixin 이 하나도 없습니다.** 위치 교환은
`PositionSwapManager`, 채굴은 `PerkBlockBreaks`, 증강 지급은 `PerkManager.commit` 의
기존 자리를 그대로 씁니다.

## 0.9.0-dev — 2026-08-30

증강만 늘어난 판입니다. **통신 규약은 13 그대로**라 0.8.0-dev 클라이언트와 악수는
그대로 통합니다. 다만 새 증강은 서버와 클라이언트를 이 jar 로 바꿔야 나옵니다.

> 서버에 `config/sharedfate-perks.json` 이 있으면 jar 안의 기본 풀 대신 그 파일이
> 쓰입니다. 예전 파일이 남아 있으면 새 증강이 하나도 나오지 않습니다.

### 증강 추가

- **골드 8개.** 골드가 18 → **26**, 전체가 58 → **66** 이 됐습니다. 비어 있던 갈래
  (무기·리듬)와 아무도 안 쓰던 속성을 메우는 것이 목적이었습니다
  - **외길 검사** — 검 공격력 ×1.5 / 방어력 −5
  - **곡괭이 전사** — 곡괭이 공격력 ×2 / 그 밖의 무기·도구 공격력 1
  - **수확자** — 다이아몬드 호미로 잡으면 약탈 V 급 / 공격 속도 −35%
  - **거인** — 몸 크기 ×1.2, 최대 체력 +6 / 받는 피해 ×1.3
  - **밀어내기** — 넉백 +2 (넉백 II 급) / 공격력 −25%
  - **달 위를 걷다** — 중력 −30% / 넉백 저항 −1
  - **엇박자** — 15초 힘 I → 10초 나약함 I 반복
  - **얼음 발** — 이동 속도 +25% / 땅이 얼음처럼 미끄러움
- **새 효과 타입 `loot_bonus`.** 정해진 무기를 들고 몹을 잡았을 때 약탈 등급을 얹습니다.
  효과 타입이 40 → **41종**이 됐습니다. 26.2 에서 약탈이 전리품을 늘리는 통로는 개수를
  늘리는 `enchanted_count_increase` 와 희귀 드롭 확률을 올리는
  `random_chance_with_enchanted_bonus` 둘인데, 등급을 묻는 자리가
  `EnchantmentHelper.getEnchantmentLevel` 하나로 같아 거기 한 곳만 잡았습니다.
  바닐라 약탈 등급에 **더합니다**
- **실버 11개.** 실버가 14 → **25**, 전체가 47 → **58** 이 됐습니다. 새 효과 타입도
  Java 코드도 필요 없고 **통신 규약도 그대로**입니다
  - **화살막이** — 화살과 던진 것에 받는 피해 ×0.7 / 폭발 피해 ×1.5
  - **익숙한 손목** — 모든 블록 채굴 속도 +15% / 공격력 −15%
  - **나무꾼의 욕심** — 원목 25% 확률로 하나 더 / 광석 채굴 속도 −30%
  - **전리품 방패** — 처치 시 흡수 I 8초 / 최대 체력 −2
  - **발맞춰 걷기** — 팀 전원 16칸 안이면 신속 I / 몹 체력 ×1.15
  - **가벼운 위장** — 움직일 때 허기 소모 −30% / 받는 피해 ×1.15
  - **맨살의 각오** — 방어력 +2 / 채굴 속도 −15%
  - **작아진 몸** — 몸 크기 ×0.85 / 공격력 −15%
  - **휩쓸기** — 검 휩쓸기가 온전한 피해 / 공격력 −20%
  - **숨죽인 걸음** — 웅크려도 거의 평소 속도 / 서서 걷는 속도 −10%
  - **운수 좋은 날** — 행운 +3 / 몹에게 받는 피해 ×1.15

## 0.8.0-dev — 2026-08-30

> **통신 규약이 12에서 13으로 바뀌었습니다.** 서버와 모든 클라이언트를 함께
> 갱신해야 하고, 예전 클라이언트는 악수 단계에서 거부됩니다.

### 추가

- **피격 알림·사망 알림을 팀 만들 때 정합니다.** `/shareteam` 창의 팀 탭이 증강까지
  **켜고 끄기 셋과 「팀 만들기」 단추 하나**로 바뀌었습니다. 두 알림의 기본값은 **꺼짐**
  이고, **한 번 정하면 바꿀 수 없습니다.** 바꾸려면 팀을 해체하고 다시 만들어야 합니다.
  회차가 넘어가도 정한 값이 그대로 따라옵니다
  - **피격 알림** — 좌하단 「OOO 피격」. 예전에는 늘 떠 있었고 끌 수 없었습니다.
    끄면 서버가 그 패킷을 아예 보내지 않습니다
  - **사망 알림** — 게임 오버 화면에 「OOO 님의 죽음으로 끝났습니다」 한 줄
- `/shareteam create` 가 셋을 모두 받습니다 —
  `create [perks on|off] [damagealert on|off] [deathalert on|off] <이름>`.
  적지 않은 것은 꺼지므로 예전 `create <이름>` 과 `create perks on <이름>` 은
  그대로 동작합니다. `/shareteam status` 에도 두 값이 나옵니다
- **`proximity` 효과 타입.** 팀원 전원이 정해진 거리 안에 있는 동안에만 효과를 얹습니다.
  붙였다 떼는 대신 조건이 참인 동안 짧은 지속시간으로 계속 다시 얹습니다. 떼는 방식은
  접속 종료·차원 이동·죽음처럼 떼는 시점을 놓치는 자리가 많습니다

### 변경

- **팀원의 바닐라 사망 메시지를 언제나 막습니다.** 전멸하면 팀원 전원이 죽으므로 사망
  메시지가 인원수만큼 채팅에 쌓였습니다. 사망 알림을 켜도 채팅은 나오지 않고, 대신
  게임 오버 화면에 **전멸을 부른 한 사람**만 적힙니다. 「'X' 팀이 전멸했습니다」 안내는
  그대로입니다
- 팀 이름을 적다가 켜고 끄기를 눌러도 **적던 이름이 지워지지 않습니다**

### 증강 조정

- **프리즘 「운명 공동체」** — 기준 거리 64칸 → **30칸**, 전원이 그 안에 있는 동안
  **재생 I·성급함 I·저항 I**. 벌만 있던 증강에 보상을 붙였습니다. 같은 거리 기준을 벌과
  보상이 함께 쓰므로 "떨어지지 말라"는 한 가지 요구가 됩니다
- **프리즘 「과부하」** — 나약함·구속 5초 → **8초**
- **골드 「암살자」** — 이동 속도 +25% → **+10%**
- **골드 「굴착기」** — 흙과 잔디를 대상에서 뺐습니다. **돌만** 빨라집니다
- **실버 「비상식량」** — 황금 사과 5개 → **10개**
- 프리즘 「밑 빠진 독」의 문서 표기를 실제 값에 맞췄습니다. 효과 자체는 처음부터
  허기 IV 였고 II 로 잘못 적혀 있었을 뿐입니다

## 0.7.0-dev — 2026-08-30

증강 선택에 연출을 넣고, 실제 플레이에서 나온 지적들을 반영했습니다.

**서버와 모든 클라이언트를 함께 갱신해야 합니다.** 통신 규약이 11에서 12로 바뀌었습니다.

### 추가

- **선택자를 뽑는 3.5초 연출.** 구간에 닿으면 곧바로 선택창이 뜨지 않고, 팀원 이름이
  굴러가다 한 사람에서 멈춥니다. 뒤로 갈수록 느려지고 딸깍 소리가 따라붙으며,
  멈추는 순간 소리가 달라집니다. **뽑기는 서버가 하고 클라이언트는 연출만 합니다**
- **고른 증강을 3초간 보여 주는 연출.** 예전에는 누가 누르면 창이 그냥 사라져서,
  고른 사람 말고는 무엇이 정해졌는지 모른 채 게임으로 돌아갔습니다. 이제 고른 카드
  하나만 남아 3초간 떠 있습니다. 그동안에도 시간은 멈춰 있고 무적이 유지됩니다.
  시간이 다 되어 자동으로 정해진 경우에도 똑같이 보여 줍니다
- `/shareteam` 창의 **증강 탭에 설명이 함께** 나옵니다. 이름만으로는 무엇을 들고
  있는지 알 수 없었습니다. 등급별로 색도 다릅니다. `/shareteam perk list` 도 같습니다

### 변경

- **실버 「본진이 바뀐다」** — 위치 교환 주기가 ×0.5 배율에서 **1분 고정**으로
  바뀌었습니다. 배율은 팀이 정한 주기에 따라 결과가 제각각이라, 2분으로 둔 팀은
  1분이 되지만 30분으로 둔 팀은 15분이었습니다
- **프리즘 「장님 거인」** — 최대 체력 +20 → **+15**, 허기 I → **허기 II**
- **실버 「원정 준비물」** — 화염 저항·수중 호흡 포션이 **30분짜리**가 됐습니다.
  바닐라에서 가장 긴 물약도 8분이라 그 길이의 사용자 효과로 담습니다
- 「장님 거인」이 체력 표시를 가리는 동안에는 **`/shareteam` 창에서도 공유 체력을
  감춥니다.** HUD 만 가리고 창에서 그대로 보여 주면 증강의 대가가 무의미해집니다
- 상자·화로의 **추가 27칸 색을 바닐라 인벤토리 칸과 통일**했습니다. 테두리와 안쪽
  색이 뒤집혀 있어 추가 칸만 유독 어둡게 보였습니다

### 수정

- **서버 콘솔에서 한글이 깨지던 문제.** Windows 콘솔이 CP949 인데 Java 는 UTF-8 로
  찍어서 생겼습니다. 실행 스크립트가 콘솔과 Java 출력을 모두 UTF-8 로 맞춥니다
- `scripts/start-sharedfate-server.bat` 이 재시작 루프를 쓰도록 했습니다. 이 스크립트를
  거치지 않고 `java -jar` 로 직접 띄우면 **팀 전멸 뒤 서버가 다시 켜지지 않습니다**

## 0.6.0-dev — 2026-08-29

`/shareteam` 화면을 넣고, 초대 절차를 없앴습니다.

**서버와 모든 클라이언트를 함께 갱신해야 합니다.** 통신 규약이 10에서 11로 바뀌어
0.5.x 클라이언트로는 접속할 수 없습니다.

### 추가

- **`/shareteam` 를 인자 없이 치면 팀 화면이 열립니다.** 탭 넷으로 나뉩니다
  - **현황** — 팀 이름, 접속 인원, 팀 레벨, 다음 증강까지 남은 레벨, 공유 체력,
    위치 교환 주기, 증강 사용 여부
  - **팀** — 팀이 없으면 이름을 적어 「증강 켜고 만들기」·「증강 없이 만들기」.
    있으면 팀원 목록과 접속자별 초대 단추, 나가기·해체
  - **설정** — 최대 체력, 위치 교환, 증강 켜고 끄기. **리더만** 보입니다
  - **증강** — 보유 증강 목록과 선택창 열기

  모드가 없는 클라이언트에는 예전처럼 도움말이 글로 나옵니다.
  `/shareteam help` 는 어느 쪽이든 도움말입니다
- `/shareteam perks <on|off>` — 팀을 만든 뒤에도 증강을 켜고 끕니다. 끄면 이미 붙어
  있던 속성·상태이상 효과까지 걷어내고, 보유 목록은 남겨 두어 다시 켜면 되살아납니다

### 변경

- **초대에 수락 절차가 없어졌습니다.** 리더가 `/shareteam invite <플레이어>` 를 치면
  상대가 곧바로 팀에 들어옵니다. 상대의 개인 아이템은 그 자리에 드랍되고 개인 경험치는
  공유 풀에 합쳐집니다
- 그래서 **`/shareteam accept`·`invites`·`decline` 을 없앴습니다.** 저장 파일에 남아 있던
  초대 기록은 더 읽지 않으며, 남아 있어도 오류가 나지 않습니다
- **팀 레벨 표시를 화면 오른쪽 위에서 아래 왼쪽으로 옮겼습니다.** 핫바 왼쪽 끝에 맞춰
  체력 칸 위에 붙습니다. 상태이상 아이콘 아래에 있을 때는 거기 있다는 것을 모르면
  아무도 보지 않는 자리였습니다
- 「뿌리내린 발」이 위치 교환을 막았을 때 뜨던 자막을 고쳤습니다. 예전 문구
  "발이 땅에 붙어…" 는 땅에 서 있는지가 조건인 것처럼 읽혔습니다. **위치 교환에는 땅
  접촉 조건이 없습니다.** 막는 것은 그 증강뿐입니다

### 수정

- 최대 체력과 위치 교환 주기를 바꿔도 클라이언트에 다시 알려주지 않던 문제.
  화면이 옛 값을 보여 줬습니다

## 0.5.1-dev — 2026-08-29

증강을 2인 플레이로 전부 확인하고, 그 결과로 값 하나를 고쳤습니다. 0.5.0-dev 와
통신 규약이 같아 클라이언트를 바꾸지 않아도 됩니다.

### 변경

- **프리즘 「운명 공동체」의 재발동 대기를 10초에서 1초로 줄였습니다.** 거리 64블록은
  그대로입니다. 흩어지면 유예 없이 곧바로 끌려옵니다. 이미 만들어진
  `config/sharedfate-perks.json` 에는 반영되지 않으니, 바꾸시려면 그 파일의
  `shared_fate_bond` 항목에서 `cooldown_ticks` 를 `20` 으로 고치거나 파일을 지우고
  서버를 다시 켜십시오

### 확인

- 「요새 탐지기」의 네더 나침반, 「허공답보」의 공중 점프, 「장님 거인」의 체력·허기 UI
  숨김, 「본진이 바뀐다」·「뿌리내린 발」의 위치 교환을 실제 플레이로 확인했습니다.
  모두 의도대로 동작합니다

## 0.5.0-dev — 2026-08-29

팀 증강 시스템을 넣고, 실제 플레이에서 나온 버그를 고쳤습니다.

**클라이언트를 반드시 함께 갱신해야 합니다.** 통신 규약이 5에서 10으로 바뀌어
예전 클라이언트로는 접속할 수 없습니다.

### 추가

- 팀 증강 시스템. 팀 공유 레벨 5·10·15·20·25·30·35 구간마다 무작위로 뽑힌
  팀원 한 명이 증강 3개 중 하나를 고르고, 효과는 팀 전체에 적용됩니다
- **구간에 도달하면 서버가 시간을 멈추고 팀 전원 화면에 선택창을 강제로 띄웁니다.**
  창이 떠 있는 동안 팀원은 용암·낙하·불·익사를 포함해 어떤 피해도 받지 않습니다.
  화면 위쪽에 60초 카운트다운이 뜨고, 시간이 다 되면 후보 중 하나가 무작위로
  선택된 뒤 시간이 다시 흐릅니다
- 창을 놓쳤을 때는 `/shareteam perk` 로 다시 볼 수 있습니다. 이 경로는 시간을
  멈추지도, 무적을 걸지도 않습니다
- 등급은 실버·골드·프리즘이며 한 라운드에 같은 등급만 나옵니다. 15렙은 프리즘 고정으로
  한 회차에 단 한 번입니다
- **중첩은 없습니다.** 한 번 고른 증강은 그 회차 동안 다시 후보로 나오지 않습니다.
  정의 파일의 `stackable` 과 `maxStacks` 는 없어졌고, 예전 파일에 남아 있어도
  오류 없이 무시됩니다
- 증강은 `config/sharedfate-perks.json` 에서 편집합니다. 파일이 없으면 기본 풀이
  자동으로 만들어집니다
- 효과 타입 11종: attribute, status_effect, damage_dealt, damage_taken,
  mob_health, mob_damage, conditional, periodic, on_kill, no_food_hunger, custom
- 화면 오른쪽 위에 팀 레벨과 다음 증강까지 남은 레벨이 뜹니다. 경험치를 써도
  목표가 흔들리지 않습니다
- `/shareteam create perks <on|off> <이름>` 으로 증강을 켜고 끕니다
- `/shareteam perk` 로 선택창을 열고, `/shareteam perk list` 로 보유 증강을 봅니다

### 수정

- **독·위더 피해가 팀 인원수만큼 배수로 들어가던 문제.** 상태 효과를 팀이
  공유하므로 네 명이 각자 피해를 받고 그것이 모두 합산됐습니다. 이제 1인분만 깎입니다
- **재생 회복과 허기 소모도 같은 이유로 배수였던 문제.** 재생이 앞으로 인원수만큼
  약해집니다
- 엔더 드래곤을 잡으면 곧바로 엔딩 크레딧이 뜨던 것을 바꿨습니다. 5초 뒤 화면에
  「엔더드래곤 토벌」이 뜨고, 다시 5초 뒤 팀원 위치에 폭죽이 터집니다
- 위치 교환 5초 전부터 액션바에 초읽기가 뜹니다

## 0.4.0-dev — 2026-08-12

사전 배포판입니다.

### 추가

- 공유 저장공간을 핫바 9 + 메인 54의 63칸으로 확장
- 플레이어 인벤토리, 제작대, 상자·화로 등 표준 메뉴의 추가 27칸 패널
- 팀별 최대 체력 20~40 명령과 주기적 무작위 위치 교환
- 음식 초과 영양 버퍼, 공유 피격음·모션, 회차별 피해 장부와 승리 책
- 팀 전멸 후 안전한 월드 초기화·다음 회차, 드래곤 승리·엔딩
- GitHub 사전 배포판 다운로드와 체크섬 제공

### 수정

- 차원 이동 뒤 추가 인벤토리 재활성화
- 크리에이티브 생존 탭 슬롯 겹침·클릭·서버 슬롯 상한
- 상자 패널의 화면 잘림과 바깥 클릭 판정
- 공유 도구 내구도 변경으로 진행 중인 채굴이 끊기는 문제
- 추가 슬롯을 줍기·제작·발전과제·발사체·아이템 틱 판정에 연결

### 알려진 미검증 항목

- 네더·엔드 왕복 후 마지막 3행 실제 조작
- 상자·화로 경계 쉬프트 클릭과 특수 메뉴
- 크리에이티브 숫자키·휠·드래그·더블클릭, GUI 배율
- `KEEP_INVENTORY`, 동시 엔더상자, 3~4인 장시간 플레이
