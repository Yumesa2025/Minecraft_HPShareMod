# 변경 내역

## 0.7.0-dev — 2026-08-30

증강 선택에 연출을 넣고, 실제 플레이에서 나온 지적들을 반영했습니다.

**서버와 모든 클라이언트를 함께 갱신해야 합니다.** 통신 규약이 11에서 12로 바뀌었습니다.

### 추가

- **선택자를 뽑는 5초 연출.** 구간에 닿으면 곧바로 선택창이 뜨지 않고, 팀원 이름이
  굴러가다 한 사람에서 멈춥니다. 뒤로 갈수록 느려지고 딸깍 소리가 따라붙으며,
  멈추는 순간 소리가 달라집니다. **뽑기는 서버가 하고 클라이언트는 연출만 합니다**
- **고른 증강을 3초간 보여 주는 연출.** 예전에는 누가 누르면 창이 그냥 사라져서,
  고른 사람 말고는 무엇이 정해졌는지 모른 채 게임으로 돌아갔습니다. 이제 고른 카드
  하나만 남아 3초간 떠 있습니다. 그동안에도 시간은 멈춰 있고 무적이 유지됩니다.
  시간이 다 되어 자동으로 정해진 경우에도 똑같이 보여 줍니다
- `/shareteam` 창의 **증강 탭에 설명이 함께** 나옵니다. 이름만으로는 무엇을 들고
  있는지 알 수 없었습니다. 등급별로 색도 다릅니다. `/shareteam perk list` 도 같습니다

### 변경

- **실버 「본진이 바뀐다」** — 위치 교환 주기가 ×0.5 배율에서 **1분 고정**으로
  바뀌었습니다. 배율은 팀이 정한 주기에 따라 결과가 제각각이라, 2분으로 둔 팀은
  1분이 되지만 30분으로 둔 팀은 15분이었습니다
- **프리즘 「장님 거인」** — 최대 체력 +20 → **+15**, 허기 I → **허기 II**
- **실버 「원정 준비물」** — 화염 저항·수중 호흡 포션이 **30분짜리**가 됐습니다.
  바닐라에서 가장 긴 물약도 8분이라 그 길이의 사용자 효과로 담습니다
- 「장님 거인」이 체력 표시를 가리는 동안에는 **`/shareteam` 창에서도 공유 체력을
  감춥니다.** HUD 만 가리고 창에서 그대로 보여 주면 증강의 대가가 무의미해집니다
- 상자·화로의 **추가 27칸 색을 바닐라 인벤토리 칸과 통일**했습니다. 테두리와 안쪽
  색이 뒤집혀 있어 추가 칸만 유독 어둡게 보였습니다

### 수정

- **서버 콘솔에서 한글이 깨지던 문제.** Windows 콘솔이 CP949 인데 Java 는 UTF-8 로
  찍어서 생겼습니다. 실행 스크립트가 콘솔과 Java 출력을 모두 UTF-8 로 맞춥니다
- `scripts/start-sharedfate-server.bat` 이 재시작 루프를 쓰도록 했습니다. 이 스크립트를
  거치지 않고 `java -jar` 로 직접 띄우면 **팀 전멸 뒤 서버가 다시 켜지지 않습니다**

## 0.6.0-dev — 2026-08-29

`/shareteam` 화면을 넣고, 초대 절차를 없앴습니다.

**서버와 모든 클라이언트를 함께 갱신해야 합니다.** 통신 규약이 10에서 11로 바뀌어
0.5.x 클라이언트로는 접속할 수 없습니다.

### 추가

- **`/shareteam` 를 인자 없이 치면 팀 화면이 열립니다.** 탭 넷으로 나뉩니다
  - **현황** — 팀 이름, 접속 인원, 팀 레벨, 다음 증강까지 남은 레벨, 공유 체력,
    위치 교환 주기, 증강 사용 여부
  - **팀** — 팀이 없으면 이름을 적어 「증강 켜고 만들기」·「증강 없이 만들기」.
    있으면 팀원 목록과 접속자별 초대 단추, 나가기·해체
  - **설정** — 최대 체력, 위치 교환, 증강 켜고 끄기. **리더만** 보입니다
  - **증강** — 보유 증강 목록과 선택창 열기

  모드가 없는 클라이언트에는 예전처럼 도움말이 글로 나옵니다.
  `/shareteam help` 는 어느 쪽이든 도움말입니다
- `/shareteam perks <on|off>` — 팀을 만든 뒤에도 증강을 켜고 끕니다. 끄면 이미 붙어
  있던 속성·상태이상 효과까지 걷어내고, 보유 목록은 남겨 두어 다시 켜면 되살아납니다

### 변경

- **초대에 수락 절차가 없어졌습니다.** 리더가 `/shareteam invite <플레이어>` 를 치면
  상대가 곧바로 팀에 들어옵니다. 상대의 개인 아이템은 그 자리에 드랍되고 개인 경험치는
  공유 풀에 합쳐집니다
- 그래서 **`/shareteam accept`·`invites`·`decline` 을 없앴습니다.** 저장 파일에 남아 있던
  초대 기록은 더 읽지 않으며, 남아 있어도 오류가 나지 않습니다
- **팀 레벨 표시를 화면 오른쪽 위에서 아래 왼쪽으로 옮겼습니다.** 핫바 왼쪽 끝에 맞춰
  체력 칸 위에 붙습니다. 상태이상 아이콘 아래에 있을 때는 거기 있다는 것을 모르면
  아무도 보지 않는 자리였습니다
- 「뿌리내린 발」이 위치 교환을 막았을 때 뜨던 자막을 고쳤습니다. 예전 문구
  "발이 땅에 붙어…" 는 땅에 서 있는지가 조건인 것처럼 읽혔습니다. **위치 교환에는 땅
  접촉 조건이 없습니다.** 막는 것은 그 증강뿐입니다

### 수정

- 최대 체력과 위치 교환 주기를 바꿔도 클라이언트에 다시 알려주지 않던 문제.
  화면이 옛 값을 보여 줬습니다

## 0.5.1-dev — 2026-08-29

증강을 2인 플레이로 전부 확인하고, 그 결과로 값 하나를 고쳤습니다. 0.5.0-dev 와
통신 규약이 같아 클라이언트를 바꾸지 않아도 됩니다.

### 변경

- **프리즘 「운명 공동체」의 재발동 대기를 10초에서 1초로 줄였습니다.** 거리 64블록은
  그대로입니다. 흩어지면 유예 없이 곧바로 끌려옵니다. 이미 만들어진
  `config/sharedfate-perks.json` 에는 반영되지 않으니, 바꾸시려면 그 파일의
  `shared_fate_bond` 항목에서 `cooldown_ticks` 를 `20` 으로 고치거나 파일을 지우고
  서버를 다시 켜십시오

### 확인

- 「요새 탐지기」의 네더 나침반, 「허공답보」의 공중 점프, 「장님 거인」의 체력·허기 UI
  숨김, 「본진이 바뀐다」·「뿌리내린 발」의 위치 교환을 실제 플레이로 확인했습니다.
  모두 의도대로 동작합니다

## 0.5.0-dev — 2026-08-29

팀 증강 시스템을 넣고, 실제 플레이에서 나온 버그를 고쳤습니다.

**클라이언트를 반드시 함께 갱신해야 합니다.** 통신 규약이 5에서 10으로 바뀌어
예전 클라이언트로는 접속할 수 없습니다.

### 추가

- 팀 증강 시스템. 팀 공유 레벨 5·10·15·20·25·30·35 구간마다 무작위로 뽑힌
  팀원 한 명이 증강 3개 중 하나를 고르고, 효과는 팀 전체에 적용됩니다
- **구간에 도달하면 서버가 시간을 멈추고 팀 전원 화면에 선택창을 강제로 띄웁니다.**
  창이 떠 있는 동안 팀원은 용암·낙하·불·익사를 포함해 어떤 피해도 받지 않습니다.
  화면 위쪽에 60초 카운트다운이 뜨고, 시간이 다 되면 후보 중 하나가 무작위로
  선택된 뒤 시간이 다시 흐릅니다
- 창을 놓쳤을 때는 `/shareteam perk` 로 다시 볼 수 있습니다. 이 경로는 시간을
  멈추지도, 무적을 걸지도 않습니다
- 등급은 실버·골드·프리즘이며 한 라운드에 같은 등급만 나옵니다. 15렙은 프리즘 고정으로
  한 회차에 단 한 번입니다
- **중첩은 없습니다.** 한 번 고른 증강은 그 회차 동안 다시 후보로 나오지 않습니다.
  정의 파일의 `stackable` 과 `maxStacks` 는 없어졌고, 예전 파일에 남아 있어도
  오류 없이 무시됩니다
- 증강은 `config/sharedfate-perks.json` 에서 편집합니다. 파일이 없으면 기본 풀이
  자동으로 만들어집니다
- 효과 타입 11종: attribute, status_effect, damage_dealt, damage_taken,
  mob_health, mob_damage, conditional, periodic, on_kill, no_food_hunger, custom
- 화면 오른쪽 위에 팀 레벨과 다음 증강까지 남은 레벨이 뜹니다. 경험치를 써도
  목표가 흔들리지 않습니다
- `/shareteam create perks <on|off> <이름>` 으로 증강을 켜고 끕니다
- `/shareteam perk` 로 선택창을 열고, `/shareteam perk list` 로 보유 증강을 봅니다

### 수정

- **독·위더 피해가 팀 인원수만큼 배수로 들어가던 문제.** 상태 효과를 팀이
  공유하므로 네 명이 각자 피해를 받고 그것이 모두 합산됐습니다. 이제 1인분만 깎입니다
- **재생 회복과 허기 소모도 같은 이유로 배수였던 문제.** 재생이 앞으로 인원수만큼
  약해집니다
- 엔더 드래곤을 잡으면 곧바로 엔딩 크레딧이 뜨던 것을 바꿨습니다. 5초 뒤 화면에
  「엔더드래곤 토벌」이 뜨고, 다시 5초 뒤 팀원 위치에 폭죽이 터집니다
- 위치 교환 5초 전부터 액션바에 초읽기가 뜹니다

## 0.4.0-dev — 2026-08-12

사전 배포판입니다.

### 추가

- 공유 저장공간을 핫바 9 + 메인 54의 63칸으로 확장
- 플레이어 인벤토리, 제작대, 상자·화로 등 표준 메뉴의 추가 27칸 패널
- 팀별 최대 체력 20~40 명령과 주기적 무작위 위치 교환
- 음식 초과 영양 버퍼, 공유 피격음·모션, 회차별 피해 장부와 승리 책
- 팀 전멸 후 안전한 월드 초기화·다음 회차, 드래곤 승리·엔딩
- GitHub 사전 배포판 다운로드와 체크섬 제공

### 수정

- 차원 이동 뒤 추가 인벤토리 재활성화
- 크리에이티브 생존 탭 슬롯 겹침·클릭·서버 슬롯 상한
- 상자 패널의 화면 잘림과 바깥 클릭 판정
- 공유 도구 내구도 변경으로 진행 중인 채굴이 끊기는 문제
- 추가 슬롯을 줍기·제작·발전과제·발사체·아이템 틱 판정에 연결

### 알려진 미검증 항목

- 네더·엔드 왕복 후 마지막 3행 실제 조작
- 상자·화로 경계 쉬프트 클릭과 특수 메뉴
- 크리에이티브 숫자키·휠·드래그·더블클릭, GUI 배율
- `KEEP_INVENTORY`, 동시 엔더상자, 3~4인 장시간 플레이
