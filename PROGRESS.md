# 진행 상황

마지막 갱신: 2026-08-30

이 문서는 **지금 어디까지 왔고 다음에 무엇을 해야 하는지**만 적습니다. 무엇이 바뀌었는지의
기록은 `CHANGELOG.md`, 증강 하나하나의 내용은 `docs/증강-작성표.md` 입니다.

## 한눈에

| | 상태 |
|---|---|
| 최신 릴리스 | **v0.9.0-dev** (사전 배포판) |
| 통신 규약 | **13** (배포된 판과 저장소가 같음) |
| 증강 풀 | 66개 — 실버 25 · 골드 26 · 프리즘 15 |
| 시험 | 675개 통과 |
| Minecraft / Fabric | 26.2 / Loader 0.19.3 / API 0.156.0+26.2 / JDK 25 |

## 먼저 알아야 할 것

### 배포된 것과 저장소가 같습니다

`v0.9.0-dev` 를 내면서 두 서버와 이 PC 의 클라이언트를 모두 그 jar 로 맞췄습니다.
지인들에게는 릴리스의 `SharedFate-0.9.0-dev-client.zip` 을 주면 됩니다.

**예전 클라이언트(0.7.0-dev 이하)는 접속되지 않습니다.** 0.8.0-dev 에서 통신 규약이
12에서 13으로 올랐고, 악수 단계에서 거부됩니다. 0.8.0-dev 클라이언트는 규약이 같아
접속은 되지만 **새 증강 19개를 보지 못합니다.**

### 증강이 안 바뀌면 `config/sharedfate-perks.json` 을 보십시오

서버에 이 파일이 있으면 **jar 안의 기본 풀 대신 그 파일이 쓰입니다.** 예전 47개짜리
파일이 남아 있어 실제로 겪었습니다. 서버가 뜰 때 나오는 줄로 알 수 있습니다.

```
증강 66개를 읽었습니다 (건너뜀 0개)   ← 정상
증강 47개를 읽었습니다 (건너뜀 0개)   ← 옛 파일을 읽고 있음
```

지우면 다음에 켤 때 jar 의 기본 풀로 다시 만들어집니다.

### 서버는 반드시 재시작 루프로 켜야 합니다

```
start.bat        →  sharedfate-server-loop.ps1  →  java -jar
```

모드는 팀 전멸 시 월드를 직접 지우지 않고 **표식만 남기고 종료**합니다. 월드를 지우고
서버를 다시 여는 일은 루프 스크립트가 합니다. `java -jar` 로 직접 띄우면 **전멸 뒤 서버가
다시 켜지지 않습니다.**

### 서버가 켜져 있는 동안 jar 를 바꾸면 안 됩니다

Fabric 은 클래스를 필요할 때마다 jar 에서 읽습니다. 실행 중에 파일을 바꾸면 아직 안 읽은
클래스를 찾는 순간 `ZipException` 으로 죽습니다. 실제로 한 번 겪었습니다.

### 빌드에는 `JAVA_HOME` 지정이 필요합니다

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-25.0.1.8-hotspot"
.\gradlew.bat build --no-daemon
```

시스템 기본이 JDK 17 이라 지정하지 않으면 loom 해석부터 실패합니다. PATH 의 `java` 는
25 라서 헷갈리기 쉽습니다.

## 다음에 할 일

### 1. 증강 풀 키우기 — 목표 125개

| 등급 | 지금 | 목표 | 남음 |
|---|---|---|---|
| 실버 | 25 | 50 | 25 |
| 골드 | 26 | 50 | 24 |
| 프리즘 | 15 | 25 | 10 |
| 합계 | 66 | 125 | **59** |

한 회차에 7번만 고르므로, 풀이 125개면 같은 조합이 두 번 나올 일이 거의 없습니다.
지금 66개로도 매 구간 후보 3개를 채우는 데는 문제가 없습니다. 이 목표는 **회차마다 다른
판이 되게** 하려는 것입니다.

**전담 워커가 있습니다** — `.claude/agents/perk-writer.md`. 효과 타입 41종, 표기 규칙,
id 오프라인 검증법, 빌드 주의사항이 들어 있습니다. 한 번에 5~10개씩 목록을 먼저
승인받고 넣는 방식입니다.

#### 2026-08-30 실버 11개 추가 — 끝났습니다

실버 15~25 를 넣어 14 → 25 가 됐습니다. 내용과 값을 정한 이유는
`docs/증강-작성표.md` 의 「2026-08-30 실버 11개 추가」 절에 있습니다.

새 효과 타입도 Java 코드도 필요 없었고 **통신 규약은 그대로 13** 입니다. 뒤의 다섯은
아무도 안 쓰던 바닐라 속성(`armor` `scale` `sweeping_damage_ratio` `sneaking_speed`
`luck`)을 꺼내 쓴 것입니다.

#### 2026-08-30 골드 8개 추가 — 끝났습니다

골드 19~26 을 넣어 18 → 26 이 됐습니다. 비어 있던 갈래를 메우는 것이 목적이었고,
값을 정한 이유는 `docs/증강-작성표.md` 의 「2026-08-30 골드 8개 추가」 절에 있습니다.

**효과 타입이 40 → 41 종이 됐습니다.** 「수확자」때문에 `loot_bonus` 를 새로 만들었습니다.
몹 전리품을 늘리는 타입이 하나도 없었기 때문입니다. `EnchantmentHelperLootingMixin` 이
`EnchantmentHelper.getEnchantmentLevel` 한 곳을 잡아 개수와 희귀 드롭 확률을 함께 올립니다.
**통신 규약은 그대로 13** 입니다.

#### 다음 증강을 만들 때

**아직 비어 있는 갈래** — 골드에 `proximity`·`gather`(팀 뭉침)가 한 자리도 없습니다.
실버에는 `holder`(한 명에게 몰아 주기)와 `swap_interval`(위치 교환 주기)이 없습니다.
전 등급을 통틀어 **바다·물**(`oxygen_bonus` `submerged_mining_speed`
`water_movement_efficiency`)은 아무도 안 씁니다.

**코드가 있어야 되는 것 셋.** 지금 형식으로는 표현할 수 없습니다.

- **조건부** — "밤에는 강해진다", "방어구를 안 입으면 이득". `conditional` 이
  `hunger_full` · `health_full` · `health_below` · `health_above` 넷만 지원합니다
- **거리 조건 속성** — "뭉치면 방어력 +4". `proximity` 는 짧은 지속시간으로 계속 다시
  얹는 방식이라 **상태 효과에만 안전**하고, 속성은 `conditional` 의 일인데 거리 조건이
  없습니다
- **궁수 갈래** — 활·화살만 강해지는 것. 투사체 피해만 올리는 타입이 없고
  `damage_dealt` 는 모든 피해에 걸립니다

### 2. 실제 플레이로 확인해야 하는 것

**확인 끝남 (2026-08-30)**

- `/shareteam` 팀 화면 — 네 탭, 위젯 배치, 팀 만들기·초대·설정 모두 정상

빌드와 시험은 통과했지만 **아직 눈으로 본 적이 없는 것들**입니다.

- 증강 선택자 **5초 추첨 연출** — 굴림 속도와 소리가 적당한지
- 증강 확정 **3초 연출** — 3초가 적당한지
- 「운명 공동체」의 30칸 보상·집합 (2인 필요)
- **피격·사망 알림 설정** — 팀 만들기 화면의 토글 셋과 경고문 배치, 알림을 끈 팀에서
  좌하단 「OOO 피격」이 사라지는지, 전멸 시 채팅에 사망 메시지가 하나도 없는지,
  사망 알림을 켠 팀의 게임 오버 화면에 이름 한 줄이 뜨는지.
  **채팅 차단과 부제는 실제로 죽어 봐야만 확인됩니다** — 단위 시험으로 닿지 않습니다

아래는 배포된 jar 에 아직 들어 있지 않아 확인 자체가 불가능합니다. 다음 판을 낸 뒤에
봐야 합니다.

- 「운명 공동체」 30칸 (위와 같은 항목)
- 조정한 증강 5건 — 과부하 8초, 암살자 +10%, 굴착기 돌만, 비상식량 10개

0.4.0-dev 부터 이어지는 미검증 항목도 남아 있습니다 — 네더·엔드 왕복 후 마지막 3행,
상자·화로 경계 쉬프트 클릭, `KEEP_INVENTORY`, 동시 엔더상자, 3~4인 장시간 플레이.

## 서버

| | 지인 서버 | 테스트 서버 |
|---|---|---|
| 경로 | `C:\project\Minecraft FateShareMod Server` | `C:\project\SharedFate-테스트서버` |
| 용도 | 실제 플레이 | 확인용 |
| 포트 | 25565 | 25565 |
| 증강 풀 | 66개 전체 | 66개 전체 |
| 월드 초기화 | 켬 | **끔** |
| 드래곤 엔딩 | 켬 | **끔** |
| 공유 최대 체력 | 20 | 20 |
| 하드코어 | 켬 | 켬 |

**같은 포트라 동시에 켤 수 없습니다.** 외부 접속은 playit 터널이 이 PC 의 25565 로
들어옵니다. 지인들은 늘 쓰던 주소를 그대로 씁니다.

`hardcore=true` 는 **월드를 만들 때 `level.dat` 에 새겨집니다.** 이미 만들어진 월드는
`server.properties` 를 바꿔도 그대로이고, 다음 회차의 새 월드부터 적용됩니다.
하드코어가 켜지면 난이도는 어려움으로 고정됩니다.

화이트리스트는 `online-mode=false` 라 **이름에서 만든 오프라인 UUID** 를 씁니다.
정품 UUID 를 넣으면 접속이 거부됩니다.

## 배포

```
https://github.com/Yumesa2025/Minecraft_HPShareMod/releases
```

지인에게는 `SharedFate-<버전>-client.zip` 하나만 주면 됩니다. 안에 SharedFate,
Fabric API, 설치 안내가 들어 있습니다. **예전 `sharedfate-*.jar` 를 반드시 지우고**
넣어야 합니다. 두 개가 함께 있으면 중복 로드로 게임이 켜지지 않습니다.

통신 규약이 바뀐 판은 서버와 클라이언트를 **함께** 갱신해야 합니다.

| 버전 | 규약 |
|---|---|
| 0.4.0-dev | 5 |
| 0.5.x | 10 |
| 0.6.0-dev | 11 |
| 0.7.0-dev | 12 |
| 0.8.0-dev | 13 |
| 0.9.0-dev | 13 |
