# 진행 상황

마지막 갱신: 2026-08-30

이 문서는 **지금 어디까지 왔고 다음에 무엇을 해야 하는지**만 적습니다. 무엇이 바뀌었는지의
기록은 `CHANGELOG.md`, 증강 하나하나의 내용은 `docs/증강-작성표.md` 입니다.

## 한눈에

| | 상태 |
|---|---|
| 최신 릴리스 | **v0.7.0-dev** (사전 배포판) |
| 통신 규약 | **12** |
| 증강 풀 | 47개 — 실버 14 · 골드 18 · 프리즘 15 |
| 시험 | 661개 통과 |
| Minecraft / Fabric | 26.2 / Loader 0.19.3 / API 0.156.0+26.2 / JDK 25 |

## 먼저 알아야 할 것

### 배포된 jar 가 최신 빌드보다 오래됐습니다

`v0.7.0-dev` 태그를 낸 뒤 증강 6건을 더 고쳤습니다(위 「미출시」). 그래서 지금 서버와
클라이언트에 깔린 `sharedfate-0.7.0-dev.jar` 에는 **그 변경이 들어 있지 않습니다.**

같은 버전 이름으로 내용만 바꿔 배포하면 누가 어느 판을 쓰는지 알 수 없게 됩니다.
다음에 낼 때는 **버전을 올려서** 내십시오. 그때까지는 배포된 것과 저장소가 다른 상태가
맞습니다.

### 서버는 반드시 재시작 루프로 켜야 합니다

```
start.bat        →  sharedfate-server-loop.ps1  →  java -jar
```

모드는 팀 전멸 시 월드를 직접 지우지 않고 **표식만 남기고 종료**합니다. 월드를 지우고
서버를 다시 여는 일은 루프 스크립트가 합니다. `java -jar` 로 직접 띄우면 **전멸 뒤 서버가
다시 켜지지 않습니다.**

### 서버가 켜져 있는 동안 jar 를 바꾸면 안 됩니다

Fabric 은 클래스를 필요할 때마다 jar 에서 읽습니다. 실행 중에 파일을 바꾸면 아직 안 읽은
클래스를 찾는 순간 `ZipException` 으로 죽습니다. 실제로 한 번 겪었습니다.

### 빌드에는 `JAVA_HOME` 지정이 필요합니다

```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-25.0.1.8-hotspot"
.\gradlew.bat build --no-daemon
```

시스템 기본이 JDK 17 이라 지정하지 않으면 loom 해석부터 실패합니다. PATH 의 `java` 는
25 라서 헷갈리기 쉽습니다.

## 다음에 할 일

### 1. 증강 풀 키우기 — 목표 125개

| 등급 | 지금 | 목표 | 남음 |
|---|---|---|---|
| 실버 | 14 | 50 | 36 |
| 골드 | 18 | 50 | 32 |
| 프리즘 | 15 | 25 | 10 |
| 합계 | 47 | 125 | **78** |

한 회차에 7번만 고르므로, 풀이 125개면 같은 조합이 두 번 나올 일이 거의 없습니다.
지금 47개로도 매 구간 후보 3개를 채우는 데는 문제가 없습니다. 이 목표는 **회차마다 다른
판이 되게** 하려는 것입니다.

**전담 워커가 있습니다** — `.claude/agents/perk-writer.md`. 효과 타입 40종, 표기 규칙,
id 오프라인 검증법, 빌드 주의사항이 들어 있습니다. 한 번에 5~10개씩 목록을 먼저
승인받고 넣는 방식입니다.

#### 승인 대기 중인 실버 10개

워커가 뽑아 두었고 **아직 JSON 에 넣지 않았습니다.** 새 효과 타입은 필요 없고 id 검증도
끝났습니다.

긴 팔 · 화살막이 · 익숙한 손목 · 나무꾼의 욕심 · 전리품 방패 · 발맞춰 걷기 ·
가벼운 위장 · 해녀 · 맨눈 · 성큼걸음

정하셔야 할 것 셋입니다.

- 「화살막이」의 폭발 ×1.5 가 실버치고 센지 (×1.3 안)
- 「긴 팔」의 공격 속도 −20% 가 센지 (−15% 안)
- 「성큼걸음」은 `step_height` 가 한 블록을 메우므로 실제 손해는 **틈 건너뛰기**뿐인데,
  그 대가가 의도한 것인지

### 2. 실제 플레이로 확인해야 하는 것

**확인 끝남 (2026-08-30)**

- `/shareteam` 팀 화면 — 네 탭, 위젯 배치, 팀 만들기·초대·설정 모두 정상

빌드와 시험은 통과했지만 **아직 눈으로 본 적이 없는 것들**입니다.

- 증강 선택자 **5초 추첨 연출** — 굴림 속도와 소리가 적당한지
- 증강 확정 **3초 연출** — 3초가 적당한지
- 「운명 공동체」의 30칸 보상·집합 (2인 필요)

아래는 배포된 jar 에 아직 들어 있지 않아 확인 자체가 불가능합니다. 다음 판을 낸 뒤에
봐야 합니다.

- 「운명 공동체」 30칸 (위와 같은 항목)
- 조정한 증강 5건 — 과부하 8초, 암살자 +10%, 굴착기 돌만, 비상식량 10개

0.4.0-dev 부터 이어지는 미검증 항목도 남아 있습니다 — 네더·엔드 왕복 후 마지막 3행,
상자·화로 경계 쉬프트 클릭, `KEEP_INVENTORY`, 동시 엔더상자, 3~4인 장시간 플레이.

## 서버

| | 지인 서버 | 테스트 서버 |
|---|---|---|
| 경로 | `C:\project\Minecraft FateShareMod Server` | `C:\project\SharedFate-테스트서버` |
| 용도 | 실제 플레이 | 확인용 |
| 포트 | 25565 | 25565 |
| 증강 풀 | 47개 전체 | 47개 전체 |
| 월드 초기화 | 켬 | **끔** |
| 드래곤 엔딩 | 켬 | **끔** |
| 공유 최대 체력 | 20 | 20 |
| 하드코어 | 켬 | 켬 |

**같은 포트라 동시에 켤 수 없습니다.** 외부 접속은 playit 터널이 이 PC 의 25565 로
들어옵니다. 지인들은 늘 쓰던 주소를 그대로 씁니다.

`hardcore=true` 는 **월드를 만들 때 `level.dat` 에 새겨집니다.** 이미 만들어진 월드는
`server.properties` 를 바꿔도 그대로이고, 다음 회차의 새 월드부터 적용됩니다.
하드코어가 켜지면 난이도는 어려움으로 고정됩니다.

화이트리스트는 `online-mode=false` 라 **이름에서 만든 오프라인 UUID** 를 씁니다.
정품 UUID 를 넣으면 접속이 거부됩니다.

## 배포

```
https://github.com/Yumesa2025/Minecraft_HPShareMod/releases
```

지인에게는 `SharedFate-<버전>-client.zip` 하나만 주면 됩니다. 안에 SharedFate,
Fabric API, 설치 안내가 들어 있습니다. **예전 `sharedfate-*.jar` 를 반드시 지우고**
넣어야 합니다. 두 개가 함께 있으면 중복 로드로 게임이 켜지지 않습니다.

통신 규약이 바뀐 판은 서버와 클라이언트를 **함께** 갱신해야 합니다.

| 버전 | 규약 |
|---|---|
| 0.4.0-dev | 5 |
| 0.5.x | 10 |
| 0.6.0-dev | 11 |
| 0.7.0-dev | 12 |
